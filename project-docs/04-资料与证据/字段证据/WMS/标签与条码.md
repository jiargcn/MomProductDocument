# WMS 标签与条码：字段与行为证据

> 内部证据页；适用基线：测试环境 / `dev` 分支 / 2026-07-15。不得从正式业务文档直接链接本页。

## 已定位证据

- 标签规则：`BarcodeController.java`、`BarcodeBaseVO.java`。
- 标签类型：`LabeltypeController.java`、`LabeltypeBaseVO.java`。
- 标签模板：`LabelTemplateController.java`、`LabelTemplateBaseVO.java`。
- 标签信息/打印：`BarbasicController.java`、`BarbasicBaseVO.java`。

## 已确认事实

| 对象 | 当前事实 |
| --- | --- |
| 规则 | 代码、顺序、长度、前缀长度、实体属性、截尾、加密和可用状态等受控。 |
| 类型/模板 | 覆盖数据协议、拆分/校验、模板文件、加密/压缩和启用状态。 |
| 标签信息 | 覆盖标签号、类型、模板、状态、关联号、条码字符串、打印次数与最后打印信息。 |
| 现场动作 | 支持生成、批量生成、补打，以及按库位代码取得库位标签打印数据。 |

## 待确认问题

- WMS-LABEL-001：采购件、制造件、器具、库位、叫料五个前端入口与后端对象的逐一映射未确认。
- WMS-LABEL-002：模板/规则的版本控制、在用修改保护和审批机制未确认。
- WMS-LABEL-003：补打权限、原因记录、打印失败重试和现场打印服务边界未确认。
- WMS-LABEL-004：平台能力迁移方案尚未形成，当前仍由 WMS 入口承载。
