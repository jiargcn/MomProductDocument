# 单据开关字段证据

> 内部证据页，不对产品站点发布。基线：测试环境 / `dev` 分支 / 2026-07-15。

## 已核证的输入范围

| 业务信息 | 当前要求 |
| --- | --- |
| 开关代码 | 必填。 |
| 说明 | 必填。 |
| 设定值 | 可维护，值域为布尔开关。 |
| 是否可用 | 必填。 |

## 当前证据结论

- 当前可确认的对象只包含代码、说明、设定值和可用状态。
- 当前控制器已配置导入模板，导入/导出对象包含代码、说明、设定值和可用状态；字段校验与更新方式尚未确认。
- 未确认每个开关的业务作用范围、即时生效方式、权限要求、代码唯一性和停用保护。

## 待验证问题

| 编号 | 问题 | 后续验证 |
| --- | --- | --- |
| DBC-SWITCH-001 | 各开关影响哪些业务能力未确认。 | 从菜单、前端配置和测试环境逐项建立作用范围表。 |
| DBC-SWITCH-002 | 设定值与可用状态的优先级、变更生效时点未确认。 | 在测试环境执行启停/禁用组合测试。 |
| DBC-SWITCH-003 | 导入校验、审计和回退机制未确认。 | 下载模板并检查页面入口、日志与服务行为。 |

## 证据来源

- 后端输入与校验：`win-module-dbc-biz/.../documentSwitch/vo/SwitchBaseVO.java`
- 后端导入/导出对象与模板配置：`win-module-dbc-biz/.../documentSwitch/vo/SwitchExcelVO.java`、`.../documentSwitch/SwitchController.java`
