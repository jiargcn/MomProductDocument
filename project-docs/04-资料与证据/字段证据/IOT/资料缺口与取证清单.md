# IOT / 数采 资料缺口与取证清单

> 盘点日期：2026-07-20
> 基线：测试环境目标 / `dev` 分支 / 2026-07-15
> 用途：内部证据层；**不参与** MkDocs 站点发布。

## 1. 本轮已完成（可写进正式页）

- 菜单五页 + Node-RED 菜单盘点
- 前端字段与 `/iotgw` 路径族
- 与 DBC/MES/WMS/AGV/EAM/ANDON 边界
- Node-RED 薄弱口径（无假字段表）
- 边缘接入分层模型页更新

## 2. 明确缺口

| 编号 | 缺口 | 建议取证动作 | 优先级 |
| --- | --- | --- | --- |
| IOT-GAP-001 | `win-module-iotgw` 源码/DDL 不在本仓 | 拉取网关仓库或导出库表；核对唯一约束与删除级联 | P0 |
| IOT-GAP-002 | 数采设备 ↔ DBC 台账同步 | 联调：同编码变更是否双向 | P1 |
| IOT-GAP-003 | 采集日志存储与保留策略 | 确认时序库/清理任务 | P1 |
| IOT-GAP-004 | MQTT 状态写入方与认证 | Broker 配置与网关写状态路径 | P1 |
| IOT-GAP-005 | Node-RED 菜单可达性 | 测试环境点菜单；确认组件是否未发布 | P0 |
| IOT-GAP-006 | Node-RED 现行日志落点 | 是否仍写 `mes_node_red_log` 或已迁网关/外部 | P0 |
| IOT-GAP-007 | MES 点检读参失败场景 | 对照错误码与空参行为 | P1 |
| IOT-GAP-008 | WMS 读参业务场景清单 | 哪些作业强制依赖 IoT | P1 |
| IOT-GAP-009 | AGV `iotCallback` 与数采配置关系 | 报文与点位/采集编码映射 | P2 |
| IOT-GAP-010 | 字典完整枚举值 | `iot_target_type` 等在测试环境导出 | P2 |
| IOT-GAP-011 | 接口层 `@PreAuthorize` | 仓外网关权限是否强制 | P1（挂 `GAP-014`） |
| IOT-GAP-012 | 截图与脱敏样例 | 五页列表/表单各一张 | P2 |

关联总账：`GAP-072`。

## 3. 回填顺序建议

1. 网关仓库/DDL → 补证据，必要时微调正式页约束表述。
2. Node-RED 菜单实测 → 更新 `03-NodeRed` 状态。
3. MES/WMS 消费场景联调 → 回写边缘模型与 API 集成场景表。
4. 截图进 P2 清单。

## 4. 本轮不做

- 不编造 Node-RED 维护与查询参考字段表。
- 不把 DBC 设备台账字段抄进数采设备页。
- 不声称已掌握 PLC 全量协议细节。
- 不单独展开完整 AGV 产品说明书（点位仍在 DBC；回调仅作边界）。
