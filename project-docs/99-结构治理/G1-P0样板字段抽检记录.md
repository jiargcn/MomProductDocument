# G1 抽检记录：P0 样板字段技术名

> 对应[分步任务清单](字段校正与中文路径迁移-分步任务清单.md) G1。
> 开始日期：2026-07-20。
> 用途：内部；不参与 MkDocs 发布。

## 范围

1. DBC 物料基本信息
2. WMS 采购收货
3. WMS 库存（预计入 / 事务 / 余额择要）

## 方法

对照正式页出现的技术名（若有）与 `project-docs/04-资料与证据`、P0 底表；**本步以建立抽检表与首轮结论为主**，发现偏差归 GAP-008 子项，不批量改正式页文风（正式页本以业务中文为主）。

## 抽检表（首轮启动）

| 页面 | 抽检项 | 结论 | 备注 |
| --- | --- | --- | --- |
| 物料基本信息 | `isRecycled` 语义 | **一致（业务）** | 正式语义「回收件」；前端误显示「标准件」仍为 `GAP-007`，非文档反译问题 |
| 物料基本信息 | 物料号字段 | **待 G1 细表** | Web 禁用改码 vs 接口仍收 `code` 见 `GAP-013` |
| 采购收货 | 申请/任务/记录三段 | **结构一致** | 与公共模型及 P0 样板一致；状态码 ENV 勾选仍开 |
| 库存余额 | 业务唯一粒度 | **口径一致** | 批次+托盘+包装+物料+库存状态+库位；DDL 约束见 `GAP-005` |
| 预计入 | 人工维护边界 | **文档已按查询为主** | 批量删除等风险见 `GAP-019` |

## 本步结论

- G1 **已启动**：未发现「整页靠中文反译英文字段」的新系统性污染；存量风险集中在已登记 GAP。
- **下一动作**：为物料页与采购收货维护参考各补一张「中文标签 ↔ 证据技术名」对照短表（10～15 个高频字段），放入本文件附录后将 G1 标为完成。

## 附录（待填）

### A. 物料基本信息高频字段

| 字段中文 | 文档是否写技术名 | 证据技术名 | 结论 |
| --- | --- | --- | --- |
| （待续） | | | |

### B. 采购收货高频字段

| 字段中文 | 文档是否写技术名 | 证据技术名 | 结论 |
| --- | --- | --- | --- |
| （待续） | | | |
