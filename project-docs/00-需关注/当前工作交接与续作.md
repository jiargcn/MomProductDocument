# 当前工作交接与续作

> 交接快照：2026-07-21（`project-docs` 信息架构重整完成）。
> 文档基线：测试环境 / `dev` 分支 / 2026-07-15。

## 1. 当前状态

| 项目 | 状态 | 说明 |
| --- | --- | --- |
| 第一阶段 | 已准入 | GATE 已闭合。 |
| BATCH-02～04 / 数采 | 主文档齐套 | EAM/ANDON/SCP/API/数采均已齐；PS/NodeRed 薄弱口径。 |
| 总体/运维空壳 | **已回填** | 中文叶路径见 G5 映射表。 |
| 结构治理 | **G0～G8 完成** | 过程稿已归档。 |
| 工作台目录 | **已重整** | 日常只看 [`00-需关注`](README.md)；证据在 `10-按需查阅`；过程在 `90-归档`。 |
| 缺口标记 | **规范已落地** | 见[缺口标记写法规范](缺口标记写法规范.md)；试点：标签打印、BOM。 |
| 截图 / 高价值联调 | 用户暂缓 | 不阻塞治理。 |
| 旁路 AGV/iotgw/API 样例 | 用户暂缓 | — |
| 推送 | **提交后自动 push** `origin/grok4.5` | 用户 2026-07-20 确认。 |

## 2. 必须遵守的约定

1. 正式 `docs/` 不写 DDL/DO/DTO/VO/源码路径。
2. 未证实登记总账/证据后继续。
3. 提交前 `git diff --check` + `mkdocs build`。
4. 加大单轮交付、少确认。
5. **每次成功提交后执行 `git push -u origin HEAD`。**
6. 字段名：禁止中文反译；见标准模板 §9/§10。

## 3. 建议的下一目标

```text
目标：问题驱动（本目录《问题与待确认总账》）或用户解除暂缓的截图/联调/旁路；
不必再整理 project-docs 目录结构。
```

## 4. 口径摘要

- 日常入口：本目录 README。
- 归档索引：`../90-归档/README.md`。

## 5. 最近提交

见 `git log`。与 `origin/grok4.5` 保持同步（自动推送）。
