# 当前工作交接与续作

> 交接快照：2026-07-22 收尾（选择器骨架并行波次 + 维护参考语义齐套；FTA 本波中止未完整闭合）。
> 文档基线：测试环境 / `dev` 分支 / 2026-07-15。

## 1. 当前状态

| 项目 | 状态 | 说明 |
| --- | --- | --- |
| 第一阶段 / 结构治理 / 缺口标记 | 已完成 | 日常入口仍为本目录。 |
| **字段业务语义建设** | **A/B/C/D/E 文档回填完成** | 计划见 [`字段业务语义建设计划.md`](字段业务语义建设计划.md)。 |
| DBC/WMS 维护参考语义专项表 | 高杠杆 + 第二波约 28 页已齐 | 行为模式总表 + P1–P14 专项表。 |
| 选择器范围骨架 | SCP 七组、QMS/EAM/ANDON、MES/计划工艺等高频页已补 | 未证实保留 `❓` / 总账编号。 |
| 图示与截图清单 | P0 Mermaid 已补；待截图清单已建 | 见 `待截图执行清单-2026-07-22.md`；实拍仍开。 |
| FTA-001 字段名校正 | **进行中（本波中止）** | 证据层有部分校正；正式站默认不写技术名。 |
| 截图 / 高价值联调 / RBAC 实测 | 仍开 | `FSEM-*` / `GAP-014`/`070`/`016`/`071` 等。 |

## 2. 必须遵守的约定

1. 正式 `docs/` 不写 DDL/DO/DTO/VO/源码路径/权限注解/未证实技术名。
2. 字段语义按 **P1–P14 行为模式** 写；选择器必须写清可选范围与依赖；未证实标 `❓` 或登记总账。
3. 提交前 `git diff --check` + `mkdocs build`（**仅用户明确要求时才 commit/push**）。
4. 共享通例：`12-通用选择器过滤惯例`、`13-库位与仓储级联惯例`；粒度见 `08`；策略见 `05`；P13 见数据权限页。
5. 字段名：禁止中文反译；见标准模板 §9/§10。

## 3. 建议的下一目标

```text
1. 测试环境闭合：FSEM-001～006 + GAP-014/070/016/071 + MES-*
2. 按待截图执行清单实拍（用户解除暂缓后）
3. 可选：续作 FTA-001；EAM 以外低频扫尾
4. 产品/研发决策与仓外资料（PS、IoT 等）
```

## 4. 口径摘要

- 日常入口：本目录 README。
- 执行计划：[`字段业务语义建设计划.md`](字段业务语义建设计划.md)
- 写作模板：[`实体说明页标准模板.md`](实体说明页标准模板.md)
- 正式规范：`docs/02-业务模型/04-页面数据字典规范.md`
- 待截图：`project-docs/10-按需查阅/标准与方法/待截图执行清单-2026-07-22.md`

## 5. 最近提交

见 `git log`。本轮收尾含选择器骨架并行波次与站点打包；推送以用户指示为准。
