# 正式业务文档标准模板（第一阶段）

> 状态：2026-07-22 按「字段行为模式 P1–P14」扩围；**2026-07-23 润色专节瘦身**（细节归方案，模板只留衔接）。原双样板结构与四层位置/拆分判定仍适用。
> 适用范围：DBC、WMS 及后续模块的模块首页、业务分组页、主文档和按需独立的维护与查询参考页。
> 首要目标：正式叶页写成给 **测试 + 实施 + 运维**（并列）用的业务行为说明书；**先愿读、后能用**。写法见 [文档润色与可读性优化方案](文档润色与可读性优化方案.md)；模板 §11 仅作衔接。
> 字段语义写作细则见正式站 [`页面数据字典规范`](../../docs/02-业务模型/04-页面数据字典规范.md)；历史建设计划（已归档）见 [`字段业务语义建设计划`](../90-归档/01-历史总控过程/字段业务语义建设计划.md)。

## 1. 一套内容的四层位置

| 层次 | 面向谁 | 回答什么 | 放置位置 |
| --- | --- | --- | --- |
| 模块首页 | 新人、实施人员、跨模块协作人员 | 这个模块解决什么问题、与谁协作、先学什么。 | `docs/<模块>/index.md` |
| 业务分组页 | 使用者、培训人员 | 这一组业务包含什么、各页面怎样配合、学习顺序是什么。 | `docs/<模块>/<分组>/index.md` |
| 主文档 | 业务使用者、培训人员 | 为什么要做、怎样完成一次典型业务、出现异常怎么办、做完影响什么。 | 业务功能页或分组首页 |
| 维护与查询参考页（按需独立） | 日常维护、查询、测试人员 | 新增、编辑、导入、动作、查询、详情和联查怎样执行。 | 与主文档并列，命名为`<主文档名称>-维护与查询参考` |
| 内部证据与问题 | 文档维护、产品、研发人员 | 结论依据、字段技术名、源码差异、未解决问题。 | `project-docs/`，不作为培训正文链接 |

每个业务都必须覆盖维护与查询信息，但不必须拆成两份文档。主文档讲清为什么做、怎样完成一笔典型业务及其影响；维护与查询信息讲清怎样维护、怎样查询和怎样避免操作错误。简单业务将这些内容合并在主文档中；规则较多的业务再独立为`<主文档名称>-维护与查询参考`，不能以字段技术清单替代。

### 主文档与维护与查询参考的拆分判定

满足下表任意 **两项** 时，使用“主文档 + 维护与查询参考”；未满足时采用“单文档（维护与查询内容合并）”。判定结论必须登记在分组页或迁移清单中，避免按个人偏好拆页。

| 判定项 | 说明 |
| --- | --- |
| 导入或批量处理 | 有独立导入模板、错误回执、批量更新或批量执行规则。 |
| 状态与动作限制 | 有状态流转、动作前置条件、审批、撤回、关闭或不可逆操作。 |
| 多入口执行 | 同一业务存在 Web、PDA、线边端或其它终端入口，且规则或操作差异需要说明。 |
| 多对象追溯 | 需要串联三个及以上业务对象、单据或库存/质量结果才能完成查询。 |
| 内容密度 | 单页已超过约 1,500～2,000 字，或维护查询规则已明显打断业务主线。 |

物料、供应商、采购收货和库存管理作为已确认的双文档样板；仓库、库区、月台、班次等简单对象可先采用单文档，并在同页覆盖维护与查询内容。

## 2. 先定阅读路径，再建立完整大纲

正式页应优先满足下列阅读路径：

```text
我为什么需要它 → 我需要先准备什么 → 我如何完成一次业务 →
什么情况下能做什么 → 做完会影响什么 → 如何查询和处理异常
```

任何页面第一次建立时，必须一次性列出与页面类型相适应的完整章节。尚不能写实的内容使用**可分派的站位段落**（见[缺口标记写法规范](缺口标记写法规范.md)），不得只留空标题或空表。

```markdown
## 典型业务流程

!!! tip "📝 待补充"
    用一笔典型业务说明触发原因、参与角色、操作顺序和完成结果。

!!! example "📐 图示占位"
    一张“触发 → 处理 → 结果”的流程图；说明起点、终点、关键分支和图要回答的问题。

!!! example "📷 截图占位"
    给出需要准备的脱敏单据、物料、数量或状态，以及读者应看到的结果（也可用示例数据占位同级 example）。
```

站位段落至少要写清三件事：后续要补什么、为什么需要图/截图/示例、需要从何处取得资料。技术取证状态不属于站位内容。

### 既有页面迁移的强制规则

对已有正文进行重构时，**不得先清空页面再只留下占位**。应先逐段分类：

| 原有内容类型 | 正确去向 |
| --- | --- |
| 已能由当前菜单、源码、DDL 或测试环境支撑的业务结论 | 改写为正式页中的业务目的、流程、关键判断、查询、异常或限制。 |
| 字段技术名、表名、源码路径、接口调用、完整字段表、权限注解 | 按业务对象迁入 `project-docs/10-按需查阅/资料与证据/字段证据/`。 |
| 中文反译出的错误技术名、历史设计假设、互相冲突或未证实的结论 | 不写成当前规则；登记问题或写为明确的待确认事项。 |

占位只用于当前没有可靠资料、但已经知道后续需要补什么的部分。正式页至少应保留读者现在可依赖的业务内容；清除技术表达不能成为清空业务内容的理由。

## 3. 模块首页模板

模块首页是入门地图，不是业务分组和代码证据的堆叠。建议采用以下结构：

```markdown
# <模块名称>

> 适用基线：……
> 阅读对象：……

## 模块解决什么问题

!!! tip "📝 待补充"
    用两段以内说明模块的业务边界、输入、输出和不负责什么。

## 谁会使用本模块

| 角色 | 常见目标 | 从哪里开始 |
| --- | --- | --- |
| …… | …… | …… |

## 典型业务全景

!!! example "📐 图示占位"
    模块与 DBC/WMS/其它上下游模块的业务关系图。

## 业务分组与学习顺序

| 建议顺序 | 业务分组 | 先学什么 | 学完能做什么 |
| --- | --- | --- | --- |

## 使用本模块前需要准备什么

!!! tip "📝 待补充"
    前置主数据、权限、配置和上游单据。

## 常见跨模块协作与异常

!!! tip "📝 待补充"
    只列会影响使用者判断的协作边界和求助路径。

## 术语与相关文档

!!! tip "📝 待补充"
    链接共享模型、关键分组和公共能力页面。

## 图示、截图与示例任务

!!! example "📷 截图占位"
    明确后续素材清单和资料来源。
```

## 4. 业务分组页模板

业务分组页承担目录和学习导航职责，应让读者知道“这一组页面为什么放在一起、先看哪页、能完成什么”。

```markdown
# <业务分组名称>

## 这一组业务解决什么问题

!!! tip "📝 待补充"
    说明本分组的共同目标、开始和结束边界。

## 建议学习与操作顺序

| 顺序 | 页面/业务对象 | 先解决什么 | 与下一步怎样衔接 |
| --- | --- | --- | --- |

## 关键业务对象与关系

!!! example "📐 图示占位"
    对象关系或主流程图；不要画数据库 ER 图。

## 页面清单与写作状态

| 页面 | 文档形态 | 已说明内容 | 后续需补 |
| --- | --- | --- | --- |

## 常见问题与相关分组

!!! tip "📝 待补充"
    说明典型入口、异常去向和跨分组联查。

## 图示、截图与示例任务

!!! example "📷 截图占位"
    明确每项素材用于哪一页、需从测试环境取得什么。
```

## 5. 主文档模板：把业务讲成一条完整的线

适用于主数据、配置、业务单据、申请、任务、记录和终端业务。以下章节按业务适用性选择，不适用时写清“不适用及原因”，不要机械堆表。

```markdown
# <功能或业务名称>

> 适用基线：……
> 阅读对象：……

## 业务目的与适用范围

!!! tip "📝 待补充"
    **开篇承诺（两句内）**：本页解决什么问题；读完能掌握什么（功能范围 / 逻辑 / 配置影响）。谁在什么场景使用、从哪里开始到哪里结束。

## 如何使用本组文档

| 你的目的 | 建议阅读 |
| --- | --- |
| 理解业务主线与关键判断 | 本页。 |
| 落地或核对配置如何改变行为 | 本页中讲配置影响的段落；策略类通例按需链接。 |
| 现场答疑 / 排障 | 本页影响与异常；细节步骤见维护参考。 |
| 具体维护、执行或查询 | <维护与查询参考；未拆页则本页维护与查询章节>。 |

## 使用前准备

!!! tip "📝 待补充"
    前置数据、配置、权限、上游单据和现场条件。

## 一笔典型业务如何完成

!!! tip "📝 待补充"
    按时间顺序说明角色、操作和每一步结果。

!!! example "📐 图示占位"
    流程图；复杂业务应包括来源、任务、记录和下游影响。

!!! example "📝 示例数据占位"
    一笔正常业务的脱敏样例，包含关键数量/状态/关联号。

## 关键判断、角色与动作

!!! tip "📝 待补充"
    什么条件下能做什么、谁负责判断、差异或失败如何转向。业务逻辑要点（状态/对象/跨模块边界）写在这里。

## 配置如何起作用

!!! tip "📝 待补充"
    业务类型、单据设置、开关、规则、策略等如何改变流程/可选范围/现场行为。
    若本页无独立配置，写明「受某某通例/上游配置约束」。

## 建议验证点

!!! tip "📝 待补充"
    给测试/实施/运维的短列表（配置差异、状态门禁、异常、跨模块影响）；不是完整测试用例集。

## 关键字段业务角色

> 只列主线关键项（建议约 8～12 行内）。先按 P1–P14 归类，再填表。完整语义与专项表放维护参考（未拆页则并入本页「字段业务语义」章节）。无法见[页面数据字典规范](../../docs/02-业务模型/04-页面数据字典规范.md)。正文标题不要出现 P1–P14 编号。

| 字段/配置点 | 行为模式 | 在系统中的作用 | 关键行为要点（取值/范围/联动/门禁） | 维护或操作时要警惕什么 |
| --- | --- | --- | --- | --- |
| …… | P? | …… | …… | …… |

无行为相关字段的配置展示页，可写一句「本页无可维护行为字段，仅展示/跳转」并跳过本表。

## 做完后会影响什么

!!! tip "📝 待补充"
    上游/下游单据、库存、质量、设备、接口或报表的业务结果。

## 查询、追溯与快速跳转

!!! tip "📝 待补充"
    先用什么定位、详情怎样分组、能跳到哪些关联对象。可链维护参考。

## 常见问题与处理

!!! tip "📝 待补充"
    业务人员最常遇到的异常、不要绕过的限制和求助路径。

## 当前限制与待确认事项

!!! tip "📝 待补充"
    已确认但会影响使用的限制；`❓`/GAP 短标，不要写源码取证过程、不要打断主线。

## 图示、截图与示例任务

!!! example "📷 截图占位"
    明确素材、目的、脱敏要求和资料来源。无图时可用写实示例（给定配置 → 期望行为）顶上。
```

> 主文档须讲清的信息见 [润色方案 §3.1](文档润色与可读性优化方案.md)；上表为推荐脊骨，**信息可嵌入现有节**，不必机械各占一级标题。配置影响与建议验证点不得为讨好售前而删掉。

### 主数据与配置页的补充重点

主文档中的“关键判断”至少覆盖：编码/名称识别、分类和单位、用途与管控、启停/替代/归档的影响。导入规则和列表细节优先放到维护与查询参考；未拆分的页面则在主文档中集中说明，主线部分只说明何时适合批量维护及其风险。配置页尤须写清「改哪个配置 → 行为如何变」。

### 申请、任务与记录页的补充重点

主文档先按“来源 → 申请 → 任务 → 现场执行 → 记录 → 下游结果”讲故事。公共概念只引用[申请、任务与记录模型](../../docs/02-业务模型/01-申请任务记录模型.md)，本页只说明该业务的来源、现场差异、库存/质量/接口影响和异常分支。

## 6. 维护与查询参考页模板：按需独立，让人能完成具体操作

当页面符合拆分判定时，建立维护与查询参考。该页只补“怎样做、怎样查、怎样定位”，不复述主文档的完整业务故事；即使当前资料不足，也要先建立完整大纲和可分派站位。文件名统一为`<主文档名称>-维护与查询参考.md`。未拆分时，将下列适用章节合并到主文档的“查询、追溯与快速跳转”“关键判断”或独立的“维护与查询说明”章节。

```markdown
# <主文档名称>-维护与查询参考

> 适用基线：……
> 用途：配合[主文档](…)使用；说明本页适合的读者和任务边界。

## 快速定位

| 你要做什么 | 先看哪里 |
| --- | --- |

## 业务对象或操作范围

!!! example "📐 图示占位"
    仅在对象关系难以用文字说明时使用。

## 新增/发起/维护

| 需要准备或选择什么 | 为什么 | 当前限制或核对方式 |
| --- | --- | --- |

## 字段业务语义

> 每页一张语义总表；再按**本页实际出现的模式**选用专项节。无某模式时显式写「本页无 P3/P8」等，避免误判遗漏。专项表过长打断操作说明时，可拆 `<主文档名称>-字段业务语义.md`，本页保留摘要 + 链接。

### 字段业务语义总表

| 字段（中文业务名） | 行为模式 | 业务作用 | 谁在何时维护/选择 | 业务约束摘要 | 影响哪些功能 | 变更或错选风险 |
| --- | --- | --- | --- | --- | --- | --- |
| …… | P? | …… | …… | …… | …… | …… |

### 取值影响矩阵（有 P1 时）

| 取值 | 业务含义 | 影响的功能或流程 | 系统行为变化 | 错选/漏选后果 |
| --- | --- | --- | --- | --- |

### 选择器范围表（有 P2/P3/P13 时）

| 选择字段 | 选择对象 | 可选范围（必须写清） | 范围依赖的前置字段/状态/权限 | 选中后带回或锁定什么 | 选不到时通常原因 |
| --- | --- | --- | --- | --- | --- |

### 联动链表（有 P4 时）

| 上游字段变化 | 受影响下游字段 | 系统行为（清空/重算/禁止/保持） | 业务原因 |
| --- | --- | --- | --- |

### 回填与锁定表（有 P5 时）

| 触发选择 | 回填字段 | 可改/锁定 | 锁定解除条件 | 与手工录入冲突时以谁为准 |
| --- | --- | --- | --- | --- |

### 数量与换算 / 精度与采集 / 状态-动作（有 P7/P8/P9 时）

按[页面数据字典规范](../../docs/02-业务模型/04-页面数据字典规范.md)对应表头选用；可与主文档流程节配合，避免空表重复。

## 修改、启停、提交或执行

| 操作 | 何时使用 | 先检查什么 | 结果或风险 |
| --- | --- | --- | --- |

## 导入或批量处理（不适用则说明原因）

| 模板内容 | 用户填写/系统填充 | 校验 | 错误处理 |
| --- | --- | --- | --- |

## 列表、详情与联查

| 查询目标 | 建议默认看什么 | 常用筛选 | 详情/跳转 |
| --- | --- | --- | --- |

## 终端操作（不适用则说明原因）

!!! tip "📝 待补充"
    终端与 Web 的差异、扫码/离线/拍照等特有规则。

## 操作前快速核对

!!! tip "📝 待补充"
    按最易出错的动作列出最少核对项。

## 仍待业务确认

!!! tip "📝 待补充"
    只列会改变操作结论的未确认项及验证方式。

```

### 维护与查询参考页的表格规则

| 需要说明什么 | 推荐写法 | 不要写成什么 |
| --- | --- | --- |
| 关键字段业务语义 | 按 P1–P14 归类；业务作用、谁维护、约束、影响、错选风险。 | 全量技术字段清单、DDL/DO/DTO 映射。 |
| 选择器（P2/P3） | 选择对象、**可选范围**、依赖、带回/锁定、选不到原因。 | 只写“弹窗选择××”；接口路径和请求参数。 |
| 字典/开关（P1） | 取值 → 功能影响矩阵。 | “来自字典”而无行为落点。 |
| 联动/回填（P4/P5） | 上游变化如何处理下游；回填可改/锁定。 | 控件实现细节。 |
| 系统限制 | 只写会影响录入/导入的长度、格式、重复或数值范围。 | 字段类型/数据库列定义的完整翻译。 |
| 状态与动作（P9） | 当前能做什么、先检查什么、结果是什么。 | 未验证的固定状态机。 |
| 列表和详情 | 推荐字段顺序、筛选、分组、联查目的。 | 为覆盖而展示所有字段。 |

## 7. 图示、截图与示例的最低要求

| 内容 | 何时需要 | 站位必须写清 |
| --- | --- | --- |
| 流程图 | 多角色、多单据或跨模块流转。 | 起点、终点、关键分支和读者要理解的关系。 |
| 状态图 | 动作受状态限制。 | 状态、允许动作、回退/关闭分支及验证资料。 |
| 对象关系图 | 三个以上业务对象需要串联。 | 业务对象和关系，不画表名、字段和服务调用。 |
| 页面截图 | 需要解释表单、列表、按钮、分组或异常提示。 | 页面区域、要标出的内容、脱敏要求。 |
| 示例数据 | 数量、状态、关联关系、导入或差异规则复杂。 | 场景、关键数据、预期结果和查询入口。 |

## 8. 写作与验收流程

1. 先判断页面类型，并按拆分判定选择“单文档（维护与查询内容合并）”或“主文档 + 维护与查询参考”。
2. 一次性建立完整大纲和站位；先不填技术字段。
3. 用一笔典型业务写出主线，再把配置影响、验证点、查询和异常**嵌进脊骨**（见润色方案 §3；不必先堆齐一级标题）。
4. **字段语义：** 列出行为相关字段 → 按 P1–P14 归类 → 主文档关键角色表 → 参考页语义总表 + 实际需要的专项表。
5. 仅把确实影响操作的字段和限制写进维护与查询参考；其余证据留在 `project-docs/`。
6. 检查维护与查询信息是否完整：成对页面应互补，单文档应在同页提供可操作的维护与查询说明。
7. **叶页润色：** 按 [润色方案](文档润色与可读性优化方案.md) **先愿读、后能用** 自检（§5）；通过后再审阅。

## 9. 发布前自检

- 前三屏能否说清“这是什么、谁来用、怎样完成一次典型业务”？（愿读）
- 每个业务是否都覆盖维护与查询信息；已拆分的页面，读者能否区分两页各自解决的问题？
- 配置影响与建议验证点是否已讲清（可嵌入现有节；或明确无独立配置及所受约束）？
- 叶页是否通过 [润色方案 §5](文档润色与可读性优化方案.md)（先 A 愿读，后 B 能用）？是否触犯方案 §4 / §6？
- 未填内容是否都按[缺口标记写法规范](缺口标记写法规范.md)使用告示框/徽章（`📝 待补充` / `📐 图示占位` / `📷 截图占位` / `🔍 待核验` / `❓ 待确认`）？
- 是否把表名、字段技术名、源码路径、取证冲突和完整证据留在 `project-docs/`？
- **字段名防回归（G4）：** 若正文必须出现技术名/枚举名，是否能在对应字段证据页找到同一写法？是否出现仅由中文标签臆造的英文名（如 `materialCode`、`unit` 代替已证实的 `code`、`uom`）？
- **GAP 边界：** 已知高风险差距是否在「限制与待确认」用编号短列表点出，且未写成可操作功能？`❓`/GAP 是否未打断主线？
- 是否只在确有必要时使用流程图、状态图、截图和示例，而不是把素材当装饰？
- 是否集中说明当前限制，而没有打断主流程？
- 正文是否避免「见上表 P1/P6」等对读者无感的内部代号？

### 9.1 字段业务语义验收清单（每页复用）

- [ ] 是否先做了行为模式归类（不是只盯着字典）？
- [ ] 每个 P2/P3 选择器是否写清可选范围与依赖？
- [ ] 每个 P1 分流字段是否有取值 → 功能影响？
- [ ] 每个 P4 是否写清上游变化对下游的处理？
- [ ] 每个 P5 是否写清回填 / 可改 / 锁定？
- [ ] P7 / P8 / P9 若存在是否有对应表或等价说明？
- [ ] 影响是否点名到具体模块 / 动作？
- [ ] 有无技术证据泄漏？未证实是否标记？

## 10. 禁止事项

- 不把 DDL、DO、DTO、VO、Controller、接口调用链当作业务正文。
- 不以“代码已证实”“DDL/DO 已证实”“当前实现事实卡”等技术标签开篇。
- 不用空表、空标题代替站位段落；句中缺口用徽章，整段用 admonition。
- 不为了覆盖字段而打断一条完整业务流程。
- 不把历史设计、个人推断或未验证实现写成用户可依赖的规则。
- **不根据中文标签反译英文字段名**；技术名必须以 DDL/DO/DTO/VO/前端证据为准（见结构治理 G0～G4）。
- **不在未完成命名迁移方案确认前**批量重命名 `docs/` 路径（G5→G6）。

## 11. 润色约定（薄专节）

> **写法与验收以 [文档润色与可读性优化方案](文档润色与可读性优化方案.md) 为准**（成功标准、手法、硬禁令、清单）。本节不重复长文，只锁定与模板骨架的衔接。  
> 不推翻 §1 四层位置、拆分判定、§3～§6 骨架与字段语义写法。

1. **读者：** 叶页服务测试 + 实施 + 运维（并列）；运维含答疑/维护/排障。售前只走浅层入口；研发是质量约束不是文风 KPI。
2. **验收顺序：** 先 **愿读**（更好懂、愿意用），再 **能用**（配置/验证/排障）。只加义务节勾清单、页变更长 = 未达标。
3. **与 §5 骨架：** 方案 §3.1 的信息须讲清；可嵌入现有节，不必机械堆一级标题。推荐脊骨见方案 §3.2。
4. **硬禁令：** 禁短地图整页覆盖；禁掏空既有信息；允许改写/压缩/合并同义节；禁删截图/示例/待确认等站位（方案 §4）。
5. **发布：** 结构/字段语义仍走 §9 / §9.1；润色另按方案 §5 勾选。
6. **过程进度：** 见 [文档润色-波次计划](文档润色-波次计划.md) 与 [交接](当前工作交接与续作.md)，不写进业务页。
