# MOM 产品文档建设总任务清单与续作指南

> 快照日期：2026-07-20
> 文档基线：测试环境目标 / `dev` 分支 / 2026-07-15 快照。
> 当前状态：第一阶段已准入；BATCH-01～04 与数采主文档齐套；总体/运维空壳页已收敛回填。下一刀默认结构治理 G1（字段抽检）或问题驱动；截图与高价值联调另排。
> 续作原则：资料缺失、证据冲突或实现不完整时，先登记问题再继续可独立推进的部分；不猜测、不把历史设计写成当前事实。
> 推送约定：用户确认后**每次 `docs:` 提交自动 push** `origin/grok4.5`。

本页是恢复工作的任务总控。详细证据、字段规则和问题记录仍以链接的原始清单为准。

> 最新完成范围、提交状态和下一目标建议见[当前工作交接与续作](当前工作交接与续作.md)。
> 字段校正与路径迁移见[分步任务清单](../99-结构治理/字段校正与中文路径迁移-分步任务清单.md)。

!!! warning "当前阶段：主线齐套后进入治理与补洞"

    沿用[两阶段文档建设路线图](两阶段文档建设路线图.md)、[P0双样板复审与定稿说明](P0双样板复审与定稿说明.md)和[正式业务文档标准模板](../02-标准与方法/实体说明页标准模板.md)。正式页面先讲业务场景、角色、流程、状态、异常、查询与示例；技术字段映射和源码证据留在内部底稿。

    业务模块第二轮主线已齐；优先结构治理分步执行与空壳页维护，不并行铺开旁路新模块。

## 1. 当前可恢复状态

| 项目 | 已确认状态 | 恢复时动作 |
| --- | --- | --- |
| 工作区 | 文档建设相关变更均已本地提交；用户自己的 `.gitignore` 修改仍未提交。 | 开始前先运行 `git status --short`，不覆盖、暂存或提交用户修改。 |
| 分支 | `main`；最近一次统计为本地领先 `origin/main` 12 个提交。 | 提交后重新核对领先/落后数。 |
| 推送 | 尚未完成。当前环境对 Codeup 远端推送要求单独授权。 | 未获得授权前只做本地提交，不反复尝试绕过限制。 |
| 构建 | 每次本轮改动后均已通过 `mkdocs build`。 | 继续把 `mkdocs build` 和 `git diff --check` 作为提交前必跑项。 |
| 既有构建告警 | `system-menu-structure.md` 未进导航，及 EAM/ANDON/SCP/PS 既有链接告警。 | 不将既有告警误判为本轮失败；新增告警必须定位并处理或登记。 |

## 2. 总体任务清单

### 2.1 治理、样板与批量闸门

- [x] 确认文档目标：培训材料 + 业务字典 + 当前实现状态 + 产品差距输入。
- [x] 确认资料基线、目录约定和证据优先级：测试环境目标、`dev` 源码、DDL、菜单、历史规划书。
- [x] 建立宏观说明框架：申请/任务/记录、库存挂接、接口挂接、System/Infra、RBAC、公共能力。
- [x] 建立并定稿正式业务文档标准模板、RBAC 取证模板、图示截图与示例任务清单、字段技术名校正任务。
- [x] 建立并持续维护产品差距总账和问题不停工规则。
- [x] 完成 P0 样板闭环审计与批量占位前检查；允许复制页面结构和取证方法，不允许复制未经核验的业务结论。
- [x] 完成 BATCH-01 至 BATCH-04 的首轮占位、执行清单与覆盖审计。

### 2.2 P0 样板闭环

- [x] 物料基本信息：完成“主文档 + 维护与查询参考”二次复审；新增、编辑、导入、列表、详情、标签入口的技术证据保留在内部底稿。
- [x] 采购收货：完成“主文档 + 维护与查询参考”二次复审；申请—任务—记录、预计入、库存事务、PDA 入口、详情跳转和图示/示例样板已形成。
- [x] 预计入、库存事务、库存余额：查询/追溯定位、服务挂接、列表/详情/跳转和 RBAC 证据边界已形成。
- [x] 公共模型已抽取到 [申请、任务与记录模型](../../docs/02-业务模型/01-申请任务记录模型.md)，业务页只写特有差异。
- [ ] 测试环境端到端状态、权限显示条件、真实截图和脱敏示例数据。此项不阻塞其它页面取证。

### 2.3 BATCH-01：DBC 主数据 + WMS 核心库存链路

- [x] DBC 与 WMS 目标页面完成首轮结构占位。
- [x] DBC 高复用页面完成重点回填：物料、BOM、物料包装、包装规格、客户物料、工厂建模、业务类型、单据设置、单据开关、规则管理等。
- [x] WMS 核心链路完成重点回填：采购收货/退货/上架、发料、生产收料、生产管理、库存、销售出库、库内作业、盘点、终端操作。
- [x] 已将 `isRecycled` 正式语义校正为“回收件”，并显著登记前端错误（GAP-007）。
- [x] 已确认库存余额业务唯一粒度：批次、托盘、包装、物料、库存状态、库位；DDL 缺少复合唯一约束已登记（GAP-005）。
- [ ] P2/P3 低频 DBC/WMS 页面的逐实体字段、导入、RBAC、终端和接口回填。按问题驱动补充，不阻塞 BATCH-02。
- [ ] BATCH-01 相关测试环境回归与截图补充。

### 2.4 BATCH-02：System + Infrastructure

- [x] System/Infrastructure 首轮占位、执行清单与覆盖审计已建立。
- [x] **RBAC 权限模型第二轮回填**：用户—角色—菜单—权限标识、超管逻辑、角色状态、菜单/接口分层已业务化（主文档+维护参考+内部证据）；全站实测矩阵仍见 GAP-014。
- [x] **数据权限与决策权限第二轮回填**：角色部门/本人五档、System 挂接、与岗位库位边界、决策分析框架已业务化；挂接清单见 GAP-070；工厂/仓库/货主非角色通用范围。
- [x] **岗位、任务分配与审批主体第二轮回填**：岗位主数据、用户—岗位、岗位库位/物料、WMS 消费与审批引擎边界已业务化；旧版“岗位优先派发”不写成通用引擎。
- [x] **组织架构第二轮回填**：部门树、用户归属、与工厂/仓库/岗位边界已业务化。
- [x] **租户与认证第二轮回填**：租户/套餐、登录会话与权限信息主链已业务化；SSO/短信等环境启停待测。
- [x] **标签、条码与打印边界页第二轮回填**：基础设施页明确当前 WMS 主入口 vs 平台归属目标；操作细则仍指向 WMS 标签页。
- [x] **导入导出、消息、日志第二轮回填**：通用导入导出口径与 WMS 日志、消息渠道分层、System/Infra 日志分层已业务化。
- [x] **文件、报表、查询第二轮回填**：文件存储与业务关联、积木报表嵌入、列表/高级查询口径已业务化；统一用户视图中心未证实。
- [x] **定时任务与数据交换第二轮回填**：XXL-Job 调度边界、接口调用信息与异步重试已业务化；开发平台入口已回填。
- [x] **系统参数/字典与运维管理第二轮回填**：字典/参数/个性化口径与运维入口地图已业务化。
- [x] **BATCH-02 主文档齐套**：后续以抽样矩阵与 BATCH-03 为主，部署/安全专项按需。
- [x] **BATCH-03 启动**：MES 概述 + 工艺管理第二轮业务化样板已完成。
- [ ] Handler/接口幂等、报表数据权限、RBAC 实测等抽样：问题驱动，不阻塞主线。
- [x] 导入导出/文件/消息/日志/定时任务/数据交换等主文档与内部证据已齐（细则抽样仍开）。

### 2.5 BATCH-03：MES + QMS + EAM + ANDON

- [x] 首轮占位、执行清单和覆盖审计已建立。
- [x] **MES 工艺管理第二轮回填**：路线头/版本、图形节点、转序与并行/分派、产品与工位、运行快照边界已业务化（主文档+维护参考+内部证据）；状态字典与快照时机待测。
- [x] **MES 计划管理第二轮回填**：ERP/生产订单、工单下发与状态机、并行组、运行快照挂接已业务化；生产订单字典/待点检/退料闭环待测。
- [x] **MES 基础建模 / 终端 / 追溯 / 报表第二轮回填**：四组主文档+维护参考+证据已齐；MES 分组主文档齐套。
- [x] **MES Track-M 收口（2026-07-17）**：过期互链修正；六组待确认入总账 `MES-BASE`～`MES-TERM` + `GAP-071`；P2 截图清单已建；完工→WMS 边界按已证实范围补强。截图实拍与 ENV 勾选仍开，不阻塞主文档齐套。
- [ ] MES：P2 截图实拍、环境字典 ENV 勾选、`GAP-071` 联调回写（见 P2 清单）。
- [x] **QMS 主文档齐套（2026-07-17）**：G0 事实层 + 五组第二轮（配置/来料/生产/客户/评审）+ 维护参考 + 证据 + mkdocs + 概述齐套宣布。P2：截图、评审→WMS 自动开单、类型码展示联调。
- [x] **EAM 主文档齐套**：基础数据/设备/备件/工装/巡检保养/终端六组第二轮（主文档+维护参考+证据）。
- [x] **ANDON 主文档齐套**：故障记录/问题响应两组第二轮。
- [ ] 按实体补齐字段、动作状态、导入、列表详情、终端、RBAC（问题驱动；P2 截图另排）。

### 2.6 BATCH-04：SCP + PS + API

- [x] 首轮占位、执行清单和覆盖审计已建立。
- [x] SCP：采购订单、预测、计划、发货协同、跟踪、结算与 WMS/MES 的单据/接口关系（主文档齐套；P2 联调仍开）。
- [x] PS：资料薄弱口径已落（无实现）；缺口见证据清单，不编造第二轮字段页。
- [x] API：调用约定、模块索引、跨系统集成场景第二轮已齐；全量 OpenAPI 字段手册未做。

### 2.7 旁路：数采管理

- [x] 概述、采集点、设备管理第二轮业务化（主文档 + 维护参考）。
- [x] 边缘接入模型按已证实分层改写；NodeRed 薄弱口径。
- [x] 证据目录 `字段证据/IOT/`；总账 `GAP-072`；API 索引/集成场景增补。
- [ ] 仓外 `win-module-iotgw` DDL/权限与 Node-RED 环境实测（P2）。

### 2.8 收尾与结构治理

- [x] **G0**：建立[字段校正与中文路径迁移-分步任务清单](../99-结构治理/字段校正与中文路径迁移-分步任务清单.md)。
- [x] **G1**：P0 样板字段名抽检通过（见[G1 抽检记录](../99-结构治理/G1-P0样板字段抽检记录.md)）。
- [x] **G2（本波）**：物料/库存高风险 GAP 正文边界复核（见[G2 复核记录](../99-结构治理/G2-高风险GAP页正文边界复核.md)）。
- [ ] **G3～G4**：模块抽样扩展与防回归规则。
- [ ] **G5～G6**：统一中文文件名/目录迁移（G5 方案须确认后再 G6 实施）。
- [ ] 为复杂业务补真实截图、脱敏示例数据（用户暂缓）。
- [ ] 按差距总账推动产品与技术评审；文档只记录证据和待办，不直接修改前后端源码。
- [ ] **G7**：修复或单独治理既有 MkDocs 导航与跨模块链接告警。
- [x] **总体/运维空壳页回填（2026-07-20）**：tech-stack、architecture、业务模型 overview/process-flow、deployment、security、monitoring、reporting。

## 3. 下一轮的可执行步骤

1. [x] 第一阶段 DBC/WMS 业务化与 GATE-001 收尾完成；GATE-002 于 2026-07-17 书面通过（试讲掠过）。
2. [x] BATCH-02～04、数采主文档齐套；总体/运维空壳页已回填；结构治理 G1/G2（本波）完成。
3. **下一项：**结构治理 **G3**（跨模块抽样）或 **G4**（防回归规则）；高价值联调与截图按用户排期。
4. 旁路（AGV/iotgw 深挖/API 样例）暂缓。
5. 发现状态、权限、字段或实现冲突时，登记到[问题与待确认总账](../03-问题与待确认/问题与待确认总账.md)或内部证据页，不停止其它不依赖该结论的工作。
6. 每个切片结束后运行 `git diff --check`、`mkdocs build`；聚焦中文 `docs:` 提交后**自动 push**。

## 4. 证据规则与注意事项

### 4.1 证据优先级

1. 测试环境实际行为（后续补充）。
2. `dev` 分支前后端源码。
3. DDL。
4. 菜单清单与前端路由。
5. 历史产品规划书：仅提取稳定设计理念或发现待核验点，不能单独证明当前实现。

### 4.2 必须遵守

- 只修改本仓库的 Markdown、`mkdocs.yml` 和必要链接配置；不修改前后端源码、DDL 或测试数据。
- 字段技术名必须由 DDL/DO/DTO/VO/前端字段共同核对；没有证据写“待核验”。
- 前端 `hasPermi`、菜单可见或按钮展示，只能证明前端配置，**不能**证明后端接口已强制授权。
- 业务状态、动作、导入、终端、第三方接口均逐业务取证；只复用章节结构，不复用具体结论。
- 标签/条码/打印当前可有 DBC/WMS 入口，但产品目标归属为 INFRA；必须同时写明“当前入口”和“目标归属”。
- `isRecycled` 的业务语义为“回收件”；前端显示“标准件”是已确认错误。
- 不确定事项默认登记差距总账后继续。仅在大规模重命名/移动、修改源码、使用敏感凭据、不可逆操作，或两种方案都会显著改变文档架构时再请求确认。

### 4.3 提交与推送

- 每个提交聚焦一组文档变更，提交信息使用 `docs: 中文摘要`。
- 提交前至少执行 `git diff --check` 与 `mkdocs build`；涉及 `.linkmap.json` 时先运行 `python scripts/link-interfaces.py` 并人工复查 diff。
- 当前远端推送尚需授权；不要因推送失败而回退、重置或破坏本地提交。

## 5. 关键参考文档

| 用途 | 文档 |
| --- | --- |
| 总体执行与资料要求 | [产品文档建设执行方案](产品文档建设执行方案.md)、[资料准备清单与填写模板](../04-资料与证据/资料准备清单与填写模板.md) |
| 当前逐页顺序与问题规则 | [逐页取证与回填任务清单](逐页取证与回填任务清单.md) |
| P0 样板与第一阶段闸门 | [P0双样板复审与定稿说明](P0双样板复审与定稿说明.md)、[第一阶段大纲建设执行清单](第一阶段大纲建设执行清单.md)、[两阶段文档建设路线图](两阶段文档建设路线图.md) |
| 页面结构和权限写法 | [实体说明页标准模板](../02-标准与方法/实体说明页标准模板.md)、[RBAC与动作权限取证模板](../02-标准与方法/RBAC与动作权限取证模板.md) |
| 图示与示例 | [图示截图与示例内容任务清单](../02-标准与方法/图示截图与示例内容任务清单.md) |
| 问题与产品不足 | [问题与待确认总账](../03-问题与待确认/问题与待确认总账.md) |
| 字段技术名治理 | [字段技术名真实性校正任务清单](字段技术名真实性校正任务清单.md)、[问题与待确认总账](../03-问题与待确认/问题与待确认总账.md)、[P0字段证据底表](../04-资料与证据/P0字段证据底表.md) |
| 四批次范围 | [BATCH-01占位执行清单](../90-阶段执行档案/BATCH-01/占位执行清单.md)、[BATCH-02占位执行清单](../90-阶段执行档案/BATCH-02/占位执行清单.md)、[BATCH-03占位执行清单](../90-阶段执行档案/BATCH-03/占位执行清单.md)、[BATCH-04占位执行清单](../90-阶段执行档案/BATCH-04/占位执行清单.md) |
| 公共业务模型 | [申请、任务与记录模型](../../docs/02-业务模型/01-申请任务记录模型.md) |

## 6. 关键资料位置

| 资料 | 路径 |
| --- | --- |
| 菜单 | `reference/menu.csv` |
| DDL | `reference/dbddl/`（`base.sql`、`wms.sql`、`mes.sql`、`qms.sql`、`eam.sql`、`scp.sql` 等） |
| 历史设计参考 | `reference/闻荫智慧工厂管理套件（SFMS）产品规划书.docx` |
| Web 前端 | `sourcecode/frontend/ui` |
| PDA 前端 | `sourcecode/frontend/pda` |
| 线边端 | `sourcecode/frontend/front` |
| System 后端 | `sourcecode/backend/win-module-system` |
| Infra 后端 | `sourcecode/backend/win-module-infra` |
| WMS / DBC 后端 | `sourcecode/backend/win-module-wms`、`sourcecode/backend/win-module-dbc` |

## 7. 恢复提示词

> 请按 `project-docs/01-工作总控/文档建设总任务清单与续作指南.md` 恢复 MOM 产品文档建设。先检查工作区与当前第一阶段状态，然后执行《第一阶段大纲建设执行清单》中的下一项：先 DBC 模块首页，再 WMS 模块首页，随后按分组建设完整大纲。严格遵守“问题登记、不阻塞推进”、字段不猜测、每轮 `git diff --check` + `mkdocs build`、仅修改文档仓库的规则。完成一项后更新任务清单和产品差距总账，再继续下一项。
