# 当前工作交接与续作

> 交接快照：2026-07-17（PS 排程薄弱盘点与口径纠偏后更新）。<br>
> 文档基线：测试环境 / `dev` 分支 / 2026-07-15。

## 1. 当前状态

| 项目 | 状态 | 说明 |
| --- | --- | --- |
| 第一阶段 | 已准入 | GATE 已闭合。 |
| 第二阶段 BATCH-02 | 主文档齐套 | System + Infra 已业务化。 |
| 第二阶段 BATCH-03 | **MES + QMS + EAM + ANDON 主文档齐套** | 四模块主文档+维护参考+字段证据+导航已齐；P2 截图/联调仍开。 |
| 第二阶段 BATCH-04（PS） | **仅能做到薄弱口径** | 见下文「PS 仅能做到 X，缺口 Y」。 |
| 建议下一刀 | SCP 取证 / BATCH-03 P2 / 其它有源码模块 | PS 在菜单与源码补齐前不要做字段级第二轮。 |
| 问题管理 | 已收敛 | [问题与待确认总账](../03-问题与待确认/问题与待确认总账.md)。 |

## 2. 必须遵守的约定

1. 正式 `docs/` 不写 DDL/DO/DTO/VO/源码路径。
2. 未证实登记总账或证据页后继续，不阻塞可独立项。
3. 提交前 `git diff --check` + `mkdocs build`。
4. **节奏**：用户要求每轮多做、少确认；续作默认自主选取下一切片并直接交付。
5. **PS 特例**：无菜单/无后端模块前，禁止恢复旧稿假字段表与假 ER；禁止把 MES 计划管理抄成 PS。

## 3. PS 仅能做到 X，缺口 Y

### 仅能做到（X）

- 保留 `docs/11-PS-排程管理/` 导航骨架（首页 + 6 分组），与 `mkdocs.yml` 一致。
- 用业务化文风写清：产品意图、与 [MES 计划管理](../../docs/06-MES-生产管理/03-计划管理/index.md) / DBC 路线·产线的边界、已证实（弱）与未证实对照。
- 撤销旧稿未取证字段表、ER、指标公式，避免继续当培训事实。
- 建立内部证据入口：[PS/资料缺口与取证清单](../04-资料与证据/字段证据/PS/资料缺口与取证清单.md)。

### 明确缺口（Y）

- `reference/menu.csv` 未检出排程/PS/APS 相关菜单。
- `sourcecode/backend` 无 `win-module-ps`；`frontend/ui/src/views` 无 `ps`。
- 无逐对象字段证据，故**不能**做第二轮「主文档字段 + 维护参考 + 对象证据」。
- 算法参数、版本发布、甘特、与 MES/ERP 挂接均属未证实（`GAP-017` / PS-GAP-001～009）。

## 4. 建议的下一目标

```text
优先：有源码证据的模块（如 SCP 逐对象取证）或 BATCH-03 P2 联调勾选。
PS：等测试环境菜单或仓外 APS 工程出现后，再按资料缺口清单回填字段。
MES/QMS/EAM/ANDON 主文档勿大回改；仅补联调结论短句。
```

## 5. EAM / ANDON 已落口径（摘要）

- **vs DBC**：设备/工装台账主数据在 DBC；EAM 菜单嵌入 DBC 台账页；EAM 管验收/移动/变更/停机/报修维修/巡检保养执行。
- **vs WMS**：备件出/入库可同步非计划发料/收货；同步状态含已同步 WMS / 失败；库存余额以 WMS 为准。
- **vs MES**：设备点检执行在 EAM；MES 开工点检为配置/卡点侧；勿与 QMS 过程巡检混淆。
- **ANDON**：问题主记录 + 过程到岗；来源含安东/巡检/质量；响应链配岗位时长上级消息；不自动等同维修工单。
- **报修来源**：保养/巡检/点检/开拉工单可转报修。

## 6. 最近提交

见 `git log`。推送需授权。
