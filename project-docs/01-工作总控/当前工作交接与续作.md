# 当前工作交接与续作

> 交接快照：2026-07-17。<br>
> 文档基线：测试环境 / `dev` 分支 / 2026-07-15。

## 1. 当前状态

| 项目 | 状态 | 说明 |
| --- | --- | --- |
| 第一阶段 | 已准入 | GATE 已闭合。 |
| 第二阶段 BATCH-02 | 主文档齐套 | System + Infra 公共能力与基础服务已业务化。 |
| 第二阶段 BATCH-03 | **已启动** | MES 概述 + **工艺管理**第二轮业务化已完成（主文档+维护参考+内部证据）。 |
| 建议下一刀 | MES 计划管理 | 按源码重写订单/工单旧稿；或启动 QMS 首分组。勿一次铺开全 MES。 |
| 问题管理 | 已收敛 | [问题与待确认总账](../03-问题与待确认/问题与待确认总账.md)。 |

## 2. 必须遵守的约定

1. 正式 `docs/` 不写 DDL/DO/DTO/VO/源码路径。
2. 未证实登记总账后继续。
3. 提交前 `git diff --check` + `mkdocs build`。
4. MES 旧稿含未核验字段/状态，**不得当培训事实**；以第二轮回填页为准。

## 3. 建议的下一目标

```text
目标：BATCH-03 续作 — MES 计划管理（生产订单/工单）业务化重写（主文档+维护参考+字段证据）；
或并行启动 QMS 检验配置首分组样板。
```

## 4. 本刀关键证据口径（工艺管理）

- 维护主体在 MES（菜单 `mes/processroute/index`），非 DBC 只读同步。
- 对象：路线头+版本、图形→节点明细、产品关联、工序—工位、运行快照。
- 节点控制：最小合格转序、批量转序策略（工单累计 / 按任务立即）、并行、抢单/派单。
- 状态枚举文案、快照生成时机、nodeData↔QMS 映射仍待确认，不阻塞续作。

## 5. 最近提交

见 `git log`。推送需授权。
