# 当前工作交接与续作

> 交接快照：2026-07-20（数采管理第二轮后更新）。
> 文档基线：测试环境 / `dev` 分支 / 2026-07-15。

## 1. 当前状态

| 项目 | 状态 | 说明 |
| --- | --- | --- |
| 第一阶段 | 已准入 | GATE 已闭合。 |
| BATCH-02 | 主文档齐套 | System + Infra。 |
| BATCH-03 | 主文档齐套 | MES/QMS/EAM/ANDON；P2 截图/联调仍开。 |
| BATCH-04 | **SCP 齐套**；**PS 薄弱口径已落**；**API 框架+索引齐套** | 全量 OpenAPI 字段级手册未做。 |
| 数采管理 | **第二轮主文档齐套** | 采集点/设备管理+维护参考；NodeRed 薄弱；边缘模型已更新；`GAP-072`。 |
| 建议下一刀 | P2 截图/联调；或 AGV 任务说明书（点位已在 DBC）；或 API 集成样例；或拉取 iotgw 仓补 DDL | 少打断确认。 |
| 推送 | 本地提交后需用户授权再 push | 保持与授权节奏一致。 |

## 2. 必须遵守的约定

1. 正式 `docs/` 不写 DDL/DO/DTO/VO/源码路径。
2. 未证实登记总账/证据后继续。
3. 提交前 `git diff --check` + `mkdocs build`。
4. 加大单轮交付、少确认。

## 3. 建议的下一目标

```text
目标：P2（截图与 GAP 联调）或 iotgw 仓外取证闭合 GAP-072；
旁路：AGV 任务层文档（回调已有索引，勿与数采/DBC 点位重复编造）。
```

## 4. 口径摘要

- **SCP / PS / API**：见既有 BATCH-04 结论。
- **数采**：菜单 `/iot` 五页已业务化；接口前缀 `/iotgw`；与 DBC 台账/EAM/ANDON/WMS/AGV 划界；Node-RED 仅薄弱说明。
- **AGV**：点位在 DBC；`/agv/.../iotCallback` 已在 API 索引；完整任务模块未单开。

## 5. 最近提交

见 `git log`。推送需保持与用户授权一致。
