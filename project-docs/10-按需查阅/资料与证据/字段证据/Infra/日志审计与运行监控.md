# Infra/System：日志、审计与运行监控证据

> 对应正式页面：[日志、审计与运行监控](../../../../docs/03-基础设施/07-日志、审计与运行监控.md)。
> 基线：测试环境 / `dev` 分支 / 2026-07-15。内部证据页。

## 1. System 日志（已证实）

| 能力 | 证据 | 菜单 |
| --- | --- | --- |
| 操作日志 | `system_operate_log` / `OperateLogDO` | `system/operatelog` |
| 登录日志 | `system_login_log` / `LoginLogDO` | `system/loginlog` |

## 2. Infra API 日志（已证实）

| 能力 | 证据 | 菜单 |
| --- | --- | --- |
| 访问日志 | `ApiAccessLogController` | `infra/apiAccessLog` |
| 错误日志 | `ApiErrorLogController`（含处理状态更新） | `infra/apiErrorLog` |
| 异步失败/成功记录 | 菜单 `api-mq-error`、`api-mq-his` | 细则待专项 |

## 3. WMS 批量日志

见[导入导出与批量操作](导入导出与批量操作.md)证据页。

## 4. 未证实

- 独立 APM/告警大盘作为文档已闭合能力
- 全站统一 traceId 关联矩阵

## 问题登记

- 操作日志覆盖率抽样、保留周期：随运维规范续作（`GAP-015`）。
