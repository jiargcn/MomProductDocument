# WMS 科目账户配置：字段与行为证据

> 内部证据页；适用基线：测试环境 / `dev` 分支 / 2026-07-22 第二轮。不得从正式业务文档直接链接本页。

## 已定位证据

- 服务：`SubjectAccountServiceImpl.java`
- VO：`SubjectAccountBaseVO.java`、`SubjectAccountExcelImportVO.java`
- 关联：`QadCostcentreService.queryCostcentreType`、`QadCostcentreMapper`（导入校验默认成本中心）
- DDL：`wms.sql` / `base.sql` → `basic_subject_account`

## 已确认事实

| 业务信息 | 当前事实 |
| --- | --- |
| 创建唯一 | 按 `code` 查重（`costcentreType` 条件被注释）。 |
| 更新锁定 | `updateSubjectAccount` 回填原 `code`、`costcentreType`。 |
| 产品类规则 | `isFromPlMstr=TRUE` 时 `plMstrType` 不能为空；为 FALSE 时清空类型。 |
| 导入预检 | 代码、类型（须属 QAD 成本中心类型）、会计账户/名称、是否来自产品类、项目是否必填、成本中心默认范围。 |
| 导入写入闸门 | `errorList.size()==0` 才写入整批；否则本批不落库。 |
| 导入匹配 | 写入阶段按 `code` 定位（与预检中代码+类型表述不完全一致）。 |
| 删除 | 无下游引用检查。 |

## 第二轮结论摘要

正式页已写清创建/编辑/导入分层差异与「有错整批不写」行为；唯一性维度冲突归 `WMS-SA-001`。

## 问题登记

相关待确认事项统一登记至《问题与待确认总账》的 `WMS-SA` 项。
