# WMS 产品类：字段与行为证据

> 内部证据页；适用基线：测试环境 / `dev` 分支 / 2026-07-22 第二轮。不得从正式业务文档直接链接本页。

## 已定位证据

- 服务：`MstrServiceImpl.java`
- VO：`MstrBaseVO.java`、`MstrExcelVO`（导入复用）
- DDL：`wms.sql` → `basic_pl_mstr`

## 已确认事实

| 业务信息 | 当前事实 |
| --- | --- |
| 必填 | 域代码、产品类、物料差异账户、可用状态、并发戳。 |
| 账户字段 | 重估/报废/盘点账户及对应成本中心可维护。 |
| 新增 | `createMstr` 直接插入，未见产品类查重。 |
| 导入 | 缺产品类进错误列表；按 `plProdLine` 匹配既有行后按 mode 插入/更新。 |
| 删除 | 仅校验存在；无物料引用拦截。 |

## 第二轮结论摘要

正式页已写清：新增无查重、编辑可改产品类、导入按产品类匹配、删除无引用保护；对应 `WMS-MSTR-001/002`。

## 问题登记

相关待确认事项统一登记至《问题与待确认总账》的 `WMS-MSTR` 项。
