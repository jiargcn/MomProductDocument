# WMS 标签与条码：字段与行为证据

> 内部证据页；适用基线：测试环境 / `dev` 分支 / 2026-07-22 第二轮。不得从正式业务文档直接链接本页。

## 已定位证据

- 标签规则：`controller/barcode`
- 标签类型：`controller/labeltype`
- 标签模板：`controller/labelTemplate`
- 标签信息/打印：`controller/labelBarbasic`
- 菜单与五类对象入口：WMS 基础数据前端目录

## 已确认事实

| 对象 | 当前事实 |
| --- | --- |
| 规则 | 代码、顺序、长度、前缀长度、实体属性、截尾、加密和可用状态等受控。 |
| 类型/模板 | 覆盖数据协议、拆分/校验、模板文件、加密/压缩和启用状态。 |
| 标签信息 | 覆盖标签号、类型、模板、状态、关联号、条码字符串、打印次数与最后打印信息。 |
| 现场动作 | 支持生成、批量生成、补打，以及按库位代码取得库位标签打印数据。 |
| 在用保护 | 未见完整的“在用模板不可改/不可删”服务闭环证据。 |
| 平台归属 | 当前挂 WMS；目标 INFRA 见 `GAP-015` / `WMS-LABEL-004`。 |

## 第二轮结论摘要

正式页补「当前可证实行为」表与 `WMS-LABEL-001`～`004` 短列表；不把未证实的入口映射写成既定矩阵。

## 问题登记

相关待确认事项统一登记至《问题与待确认总账》的 `WMS-LABEL` 项。
