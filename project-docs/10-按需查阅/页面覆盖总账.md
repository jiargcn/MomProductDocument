# MOM 页面覆盖总账

> **按需查阅**：不是日常盯梢材料；查某页是否已建、INV 治理项时再打开。日常入口见 [`../00-需关注/`](../00-需关注/README.md)。
>
> 状态：第一轮盘点完成；DBC 的第一阶段业务化页面建设已完成并等待产品负责人试讲复核，WMS 仍在按相同标准回填；其它模块页面回填继续暂停。
> 分析基线：测试环境 / dev 分支 / 2026-07-15。
> 目的：将菜单、现有文档和分析任务对齐；本页不替代具体业务说明。

## 1. 证据来源与使用原则

| 来源 | 路径 | 用途 | 结论等级 |
|------|------|------|----------|
| 当前菜单配置 | `reference/menu.csv` | 确认页面范围、路由、组件、可见性和菜单层级 | 当前已确认 |
| 前端源码 | `sourcecode/frontend/` | 确认表单、列表、详情、选择器和交互限制 | 待读取 |
| 后端源码 | `sourcecode/backend/` | 确认业务规则、状态、权限、导入、事务和接口 | 待读取 |
| 数据库结构 | `reference/dbddl/` | 确认字段类型、长度、约束和数据关系 | 待读取 |
| SFMS 产品规划书 | `reference/闻荫智慧工厂管理套件（SFMS）产品规划书.docx` | 提供模型、术语和设计理念参考 | 历史设计参考 |

历史规划书中“订单—计划—申请—任务—记录”、策略驱动、库存精度、RBAC、标签、报表看板、日志监控等内容可作为候选设计理念；其具体实现必须由当前菜单、源码或 DDL 验证。

## 2. 菜单范围快照

`menu.csv` 当前包含 874 条菜单配置：

| 菜单类型 | 数量 | 说明 |
|----------|------|------|
| 目录/分组（`type=1`） | 173 | 用于组织菜单层级 |
| 页面（`type=2`） | 701 | 需要经过业务聚合后进入页面总账 |
| 合计 | 874 | 配置条目，不等于 874 个独立业务功能 |

同一业务通常会拆分为申请、任务、记录、撤销记录、查询和终端页面。因此后续文档按**业务链和读者任务**组织，而不是机械地一菜单一篇。

## 3. 模块覆盖概览

下表的“菜单页面”来自 `type=2`；“现有文档”只表示 Markdown 文件数量，不代表已完成业务字典覆盖。

**字段语义列约定（2026-07-22）：** 后续盘点可增列 `字段语义` = `无` / `骨架` / `完备`。判定：无行为模式归类=无；仅有关键角色或总表缺选择器范围=骨架；按 P1–P14 该有的专项表齐全且选择器写清范围=完备。

**约定执行状态（2026-07-22 续）：** 不逐菜单 701 行填满；下表按模块给出完备度摘要。DBC 高杠杆页（物料/BOM/仓区位/供应商客户/策略等）与包装单文档、WMS 主链维护参考多数已达完备或近完备；MES/QMS/EAM/SCP 等仍以骨架或无为主；通例页锚点已通过 `toc.slugify=pymdownx` 与页内宣称对齐以降低 mkdocs 锚点 INFO。

| 菜单根模块 | 菜单页面 | 现有文档 | 字段语义（模块摘要） | 初步判断 | 下一步 |
|------------|---------:|---------:|----------------------|----------|--------|
| WMS | 213 | 13+ | 主链维护参考多为完备/近完备；子域细节与终端仍有 ❓ | 页面范围远大于现有文档；主链语义已回填 | 子域/终端选择器实测与截图 |
| 基础数据（DBC） | 50 | 38+ | 高杠杆与包装单文档完备/近完备；货主/承运商/生产商等仍有总账开项 | 第一阶段业务化与语义专项表第二波已齐 | 产品试讲复核；引用/导入开项闭合 |
| 系统管理 | 39 | 6 | 骨架（数据权限边界已写清） | RBAC 实测矩阵未齐 | `GAP-014`/`GAP-070` 投影矩阵 |
| 基础设施 | 23 | 4+ | 骨架 | 公共能力入口已立 | 标签/打印/报表归属与消费方 |
| MES | 69 | 6+ | 多为骨架；执行/追溯规则开项多 | 模块概览+维护参考骨架 | `MES-*` / `GAP-071` 环境核对 |
| QMS | 37 | 6+ | 多为骨架或无 | 与 WMS 检验联动待深化 | 来料/生产检验选择器与状态 |
| EAM | 94 | 6+ | 多为骨架 | 设备/工装链索引已立 | 与 DBC 生产商边界勿混写 |
| PDA 菜单管理 | 131 | 0 | 无（差异写入所属业务终端节） | 终端未独立成产品模块 | 随 WMS/MES/EAM 终端节补差异 |
| 线边客户端 | 11 | 0 | 无（同上） | 同上 | 同上 |
| ANDON | 4 | 3 | 骨架 | 页面少 | 微观规则与响应闭环 |
| 报表、数采及其他根菜单 | 80 | 12+ | 数采边界已写；报表多为无/骨架 | 根菜单与目录映射不完全一致 | 一对一映射，不按名推断 |

## 4. 第一条纵向试点链路

试点固定为“物料—采购收货—预计入—库存事务—库存余额”。该链路同时验证主数据、申请任务记录、库存挂接、查询以及主文档/维护与查询参考的写法，适合用来校验页面模板是否实用。RBAC 取证保留为第二阶段的 System 任务，不作为第一阶段页面大纲的前置条件。

| 试点对象 | 当前菜单证据 | 当前文档入口 | 需要从源码/DDL确认的重点 |
|----------|--------------|--------------|--------------------------|
| 物料基本信息 | `基础数据 / itembasic` | `04-DBC-主数据管理/01-物料管理/01-物料基本信息.md` | 字段、选择器、编辑限制、导入和引用约束 |
| 采购收货申请 | `WMS / purchasereceipt-request-main` | `05-WMS-库房管理/03-采购收货/index.md` | 创建来源、编辑条件、状态与任务生成 |
| 采购收货任务 | `WMS / purchasereceipt-job-main` | 同上 | 分配、执行、关闭、数量与扫码规则 |
| 采购收货记录 | `WMS / purchasereceipt-record-main` | 同上 | 记录生成、作废/冲销、库存与外部单据影响 |
| 预计入库存 | `WMS / expectin` | `05-WMS-库房管理/09-库存管理/index.md` | 创建、更新、消耗和关闭时点 |
| 库存事务 | `WMS / transaction` | 同上 | 事务类型、来源单据、幂等和冲销关系 |
| 库存余额 | `WMS / balance` | 同上 | 唯一粒度、余额更新、冻结与可用量 |
| 用户、角色和菜单 | `系统管理 / user、role、menu` | `12-系统管理/03-用户与权限/index.md` | 第二阶段确认用户、角色、菜单、数据范围和动作权限 |

## 5. 当前任务清单

| 编号 | 任务 | 主要证据 | 产出 | 状态 |
|------|------|----------|------|------|
| INV-001 | 建立菜单—文档映射 | `menu.csv`、`mkdocs.yml` | 模块覆盖矩阵 | 进行中 |
| INV-002 | 验证 WMS 采购收货前后端与 DDL | WMS 前后端、`wms.sql` | 采购收货业务链事实清单 | 进行中：申请、任务、记录页面与主表已取证 |
| INV-003 | 验证库存模型 | WMS 前后端、`wms.sql` | 预计入、事务、余额挂接模型 | 进行中：已证实任务→预计入、记录→事务→余额调用关系；余额业务唯一粒度已确认 |
| INV-004 | 验证物料主数据维护规则 | DBC 前后端、DDL | 物料页面字典试稿 | 进行中：列表、字段校验、编辑和导入实现已完成首轮取证 |
| INV-005 | 验证 RBAC 与数据范围 | System 前后端、`base.sql` | 权限模型试稿 | 第二阶段冻结 |
| INV-006 | 对照历史规划书与当前实现 | 全部当前证据 | 保留理念、变更点和失效规则清单 | 待开始 |
| INV-007 | 字段技术名真实性校正 | DDL、后端 DO/DTO、前端类型、既有字段表 | 字段映射表、已修订页面、冲突清单与防回归规则 | **P0 进行中：已完成初扫清单与 P0 字段证据底表** |
| INV-008 | 文档文件名中文化迁移 | `docs/` 路径、`mkdocs.yml`、`.linkmap.json`、内部链接 | 命名规范、迁移映射表、导航与链接校验 | **完成**：G5 九叶中文化 + G8 PS/NodeRed 展平；其余按 §6.1 保留 |
| INV-009 | 实体说明页占位段落批量补齐 | 标准模板、菜单清单、模块优先级 | 每个重点实体页具备统一占位结构 | DBC 已完成业务化回填与占位；WMS 保留为后续回填范围，待负责人准入复核 |
| INV-010 | 图示、截图与示例内容占位 | 复杂业务页、测试环境、源码、示例数据 | 图示/截图/示例占位清单和页面占位段落 | 第一阶段进行中：P0 样板已建立，后续随 DBC/WMS 大纲同步补充 |

### 6.1 文件名中文化迁移补充规则

执行 `INV-008` 时，同步处理“一个目录下只有一个 Markdown 文件”的冗余层级。原则如下：

| 场景 | 处理方式 | 判断标准 |
| --- | --- | --- |
| 目录只包含一个 `index.md`，且该目录只是叶子功能页 | 可以上移一层，并将文件命名为对应模块或功能名。 | 上移后语义更清晰，且不会破坏未来分组。 |
| 目录只包含一个普通 `.md` 文件，且无 assets、图片、附件、子目录 | 可以上移一层，并按导航名称重命名。 | 同步更新 `mkdocs.yml`、`.linkmap.json` 和所有内部链接。 |
| 目录虽然只有一个 `.md` 文件，但未来明确会继续拆子页 | 保留目录，将文件命名为 `index.md`。 | 适用于模块首页、业务分组首页、终端操作首页。 |
| 目录包含 assets、截图、图示或页面专属附件 | 默认保留目录。 | 避免资源路径混乱；如确需上移，必须同步迁移资源并校验引用。 |
| 目录层级对应菜单分组或导航分组 | 默认保留目录。 | 即使暂时只有一个页面，也承担信息架构作用。 |

执行顺序：

1. 先生成迁移映射表：旧路径、建议新路径、处理类型、是否保留目录、原因。
2. 再批量更新 `mkdocs.yml`、`.linkmap.json`、正文链接和图片链接。
3. 运行链接脚本和 `mkdocs build`。
4. 对无法确定是否未来扩展的目录，先保留目录，不做上移。

## 6. 终端操作范围（已确认）

PDA 菜单管理（131 个页面）和线边客户端（11 个页面）纳入本次文档工程，但不新建孤立的产品模块。它们按业务归属补充到 WMS、MES、EAM 等模块的“终端操作”分组或页面中。

| 原则 | 说明 |
| --- | --- |
| 业务规则只描述一次 | 以所属业务的 Web 页面和后端规则为主来源；终端页不重复定义单据、状态、库存或权限规则。 |
| 终端页只补充差异 | 说明扫码、拍照、离线、简化录入、设备能力、按钮与异常处理等终端特有内容。 |
| 同一业务链关联展示 | 终端操作页必须链接到对应的 Web 业务页、单据/任务/记录页和相关主数据。 |
| 先纳入 WMS 试点 | 首轮在采购收货等 WMS 链路中验证写法；MES、EAM 在其模块轮次中按同一模板补齐。 |
