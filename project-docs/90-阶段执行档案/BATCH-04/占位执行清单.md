# BATCH-04 占位执行清单

> 状态：已启动。
> 范围：SCP + PS + API。
> 原则：只建立供应链协同、排程管理和 API 参考的结构与取证任务；不复制未经取证的订单字段、排程算法参数、接口路径、权限码或第三方回写规则。

## 1. 执行边界

| 项目 | 本批处理方式 |
| --- | --- |
| SCP | 供应商协同、采购订单、要货预测、要货计划、发货协同、采购跟踪、发票结算。 |
| PS | 排程基础数据、维护订单、执行生产排程、结果对比、结果查询、生产计划查询。 |
| API | 统一定义接口文档结构、字段真实性、权限、幂等、错误码、日志和第三方挂接口径。 |
| 业务模块引用 | SCP/PS 页面只写业务规则和挂接对象；接口细节在 API 参考或专项接口页维护。 |
| 第三方系统 | ERP、SAP、供应商门户、报表平台、排程引擎等只写待核验挂接矩阵，不猜测真实协议。 |

## 2. 首轮页面清单

| 页面 | 页面类型 | 当前动作 | 首轮目标 |
| --- | --- | --- | --- |
| SCP 首页 | 模块入口 | 已有框架 | 后续补供应链协同总模型和跨模块接口。 |
| SCP 基础数据 | 主数据/协同配置 | 已有较多内容 | 后续校正字段、供应商权限、导入和引用关系。 |
| 采购订单 | 协同单据 | 已有较多内容 | 后续核验订单字段、状态、供应商确认和 WMS 收货挂接。 |
| 要货预测 | 预测业务 | 已有较多内容 | 后续核验预测版本、发布、供应商可见范围。 |
| 要货计划 | 计划业务 | 已有较多内容 | 后续核验计划生成、调整、确认和发货协同关系。 |
| 发货协同 | 协同单据/ASN | 已有较多内容 | 后续核验发货、到货、包装、物流、WMS 收货关系。 |
| 采购跟踪 | 查询/追踪 | 已有较多内容 | 后续核验订单、发货、收货、异常和报表口径。 |
| 发票结算 | 结算业务 | 已有较多内容 | 后续核验对账、发票、金额、税率、状态和财务接口。 |
| PS 首页 | 模块入口 | **2026-07-17 已改为薄弱口径** | 无源码/菜单前只保留意图与边界；见资料缺口清单。 |
| PS 基础数据 | 排程配置 | 薄弱口径 | 勿恢复未取证字段表；待菜单与 DDL 取证。 |
| 维护订单 | 排程输入 | 薄弱口径 | 禁止抄 MES 计划字段；待证实独立对象或映射。 |
| 执行生产排程 | 算法执行 | 薄弱口径 | 不写未取证公式；待引擎/服务取证。 |
| 排程结果对比 | 查询/分析 | 薄弱口径 | 不写未取证优劣阈值。 |
| 查询排程结果 | 查询/追踪 | 薄弱口径 | 待明细 API/表取证。 |
| 生产计划查询 | 查询/计划输出 | 薄弱口径 | 待甘特/可视化取证；边界指向 MES 计划。 |
| API 参考 | 接口入口 | 已补 BATCH-04 占位 | 后续补真实接口、字段、权限、错误码、幂等和示例。 |

## 3. 每页首轮占位最低输出

| 章节 | 首轮最低要求 |
| --- | --- |
| 页面定位 | 明确页面属于供应链协同、排程输入、排程执行、结果查询或接口参考。 |
| 字段真实性 | 标记 DDL、DO、DTO、VO、前端配置、接口 DTO 待校正。 |
| 状态动作 | 保留订单确认、计划发布、发货、收货、排程执行、结果发布等动作占位。 |
| 接口挂接 | 标记 ERP/SAP/SCP/WMS/MES/报表/排程引擎的同步方向和待核验点。 |
| 权限与数据范围 | 区分采购方、供应商、计划员、管理员、接口账号和数据范围。 |
| 图示与示例 | 保留协同流程图、排程输入输出图、接口调用示例和结果样例。 |
| 差距记录 | 对字段失真、状态不明、接口不明、实现归属不明写入差距总账。 |

## 4. 当前风险

| 编号 | 风险 | 处理方式 |
| --- | --- | --- |
| B4-RISK-001 | SCP 页面已有字段和流程可能来自历史草稿，未必对应当前 dev 实现。 | 保留内容但标记后续字段/状态/接口取证。 |
| B4-RISK-002 | PS 在本仓库无菜单/无后端模块；旧稿字段与公式易被误当事实。 | 2026-07-17 已改为薄弱口径并建缺口清单；取证前禁止恢复假字段表。 |
| B4-RISK-003 | API 参考缺少真实接口路径和字段。 | 先建立接口取证模板，后续从 Controller、前端 API、网关和 Swagger 取证。 |
| B4-RISK-004 | 第三方系统接口触发时机和补偿机制不明。 | 建立挂接矩阵，不猜测协议、频率和失败处理。 |
| B4-RISK-005 | 供应商门户数据范围与内部角色权限不同。 | 后续单独核验供应商账号、租户/组织、供应商数据隔离。 |

## 5. 执行记录

| 日期 | 动作 | 范围 | 结果 |
| --- | --- | --- | --- |
| 2026-07-16 | BATCH-04 启动与 API 入口补强 | SCP、PS、API | 已补 API 参考页接口取证结构，并建立本批执行清单。 |
| 2026-07-17 | PS 盘点与薄弱口径纠偏 | PS | menu/backend/frontend 均未检出实现；正式页去掉假字段/ER；新增 `字段证据/PS/资料缺口与取证清单.md`；交接写明仅能做到 X / 缺口 Y。 |
