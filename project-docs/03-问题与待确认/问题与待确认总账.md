# 问题与待确认总账

> 本文件是项目唯一的问题、产品差距和待确认事项台账。原“产品差距总账”“问题记录清单”“字段技术名问题初扫清单”已合并至此。<br>
> 规则：不把推测写成缺陷；每条均需有证据、影响和处理结论。业务事实页只保留问题编号或简短提示，不再重复维护问题清单。

## 使用规则

| 类别 | 编号规则 | 使用方式 |
| --- | --- | --- |
| 产品/实现差距 | `GAP-xxx` | 已有证据表明设计、页面、服务或数据层存在不闭合。 |
| 待确认事项 | `WMS-xxx`、`FTA-xxx` 等 | 当前证据不足以形成业务结论，需测试、产品或研发确认。 |
| 历史问题编号 | `ISSUE-...`、`P0-FIELD-...` | 已迁入相应总账项；不再单独新建问题文档。 |

默认策略：问题可隔离时，登记后继续推进；仅当会导致大范围返工、不可逆变更、生产数据风险或授权边界不清时暂停确认。

| 编号 | 主题 | 当前发现 | 证据基线 | 影响 | 状态 |
| --- | --- | --- | --- | --- | --- |
| GAP-001 | 采购收货与库存挂接文档 | 现有采购收货页面未明确“任务创建预计入、记录创建库存事务、事务更新库存余额”的实现链路。 | `dev` WMS 后端服务，2026-07-15 | 培训、问题定位和库存追溯无法形成闭环。 | P0 样板已补文档，待测试复核 |
| GAP-002 | 采购收货流程状态 | 前端已存在提交、同意、驳回、处理等动作入口，源码也存在自动提交、自动通过等配置字段；但尚未以测试环境验证状态编码、前置条件与自动策略。 | 现有文档、WMS 前后端 | 不能将具体审批路径或状态含义作为已确认的统一规则。 | 源码已取证，待测试验证 |
| GAP-003 | 终端操作说明 | PDA 菜单和线边端菜单尚未形成与 WMS/MES/EAM 业务页的对应文档。 | `menu.csv`、终端前端源码目录 | 终端培训与 Web 规则容易脱节。 | 采购收货 PDA 已取证，待扩展到其它业务 |
| GAP-004 | 详情页分组与快速跳转 | 现有页面文档尚未形成统一的详情分组、关联单据跳转和 Tab 规范。 | 页面覆盖总账 | 查询效率和业务追溯体验不一致。 | P0 模板已补规则，待页面批量落地 |
| GAP-005 | 库存余额唯一粒度 | 业务唯一粒度已确认：批次、托盘、包装、物料、库存状态、库位。服务层也按相关组合条件查询和合并余额；但已提供 DDL 未见对应的复合唯一约束，名称含“unique”的索引实际仅为物料编码普通索引。 | 产品确认、`BalanceMapper`、`BalanceLookupKey`、`wms.sql` | 并发或旁路写入时可能无法由数据库保证余额唯一粒度。 | 已确认业务口径，待技术评估 |
| GAP-006 | 物料主数据唯一性与导入模板 | 创建、追加导入会校验物料号重复，但更新服务未见同等显式校验；DDL 仅有主键，不见物料号唯一约束。现有 Excel VO 还包含创建者、创建时间、更新者、更新时间等审计列。 | `ItembasicService`、`ItembasicExcelVO`、`base.sql` | 物料号变更或并发写入的唯一性需要测试；人工导入模板可能承担不应由用户维护的审计数据。 | 待测试验证与模板优化 |
| GAP-007 | `isRecycled` 前端显示错误 | **已确认正式语义为“回收件”**。但 DBC 物料页、MES 正向追溯和反向追溯页面将其显示为“标准件”；MES 工艺 SOP 配置及后端/DDL 使用“回收件”。 | 产品确认；`itembasic.data.ts`、MES 追溯页面、`ItembasicBaseVO`、`ItembasicExcelVO`、`base.sql` | 标签打印分组、培训、导入模板、字典命名和追溯展示可能误导用户。 | **已确认前端错误，待修复** |
| GAP-008 | 历史文档技术字段名失真 | 部分既有字段表疑似仅根据前端中文标签自行翻译英文字段名，未与后端 DTO、DO、数据库列核对，不能作为接口、导入、测试或数据排查依据。 | 用户发现；待按 FTA-001 至 FTA-008 全面取证 | 业务字典可能给出错误字段名，导致培训、接口联调、导入模板和测试用例失效。 | **P0 待系统校正** |
| GAP-009 | 采购收货任务撤销状态校验 | 前端任务撤销按钮当前在待处理状态展示；后端 `cancelPurchasereceiptJobMain` 第一轮源码片段直接置为撤销，未见与通用任务状态机一致的显式状态校验。 | `purchasereceiptJobMain/index.vue`、`PurchasereceiptJobMainService.java`，2026-07-15 | 如果接口被非预期状态调用，可能绕过前端限制并删除预计入/预计出、生成撤销记录。 | 待开发确认或测试验证 |
| GAP-010 | 采购收货任务执行入口多端差异 | Web 任务列表中执行按钮被注释；PDA 详情页承担扫码收货、自动承接、提交执行等能力。 | `purchasereceiptJobMain/index.vue`、`pda/src/pages/purchaseReceipt/job/receiptDetail.vue` | 培训材料若只看 Web 页面，会遗漏真实现场执行入口。 | 已在采购收货页补 PDA 入口，待截图和测试验证 |
| GAP-011 | 通用接口继承与子类注释代码混杂 | 部分采购收货 Controller 子类中通用接口代码存在注释痕迹，但服务继承通用 Request/Job/Record 能力。 | WMS Controller、BaseRequestMainService、BaseJobMainService | 仅从子类 Controller 阅读可能误判接口是否可用，需结合运行时路由和前端 API 文件确认。 | 待接口专项核验 |
| GAP-012 | 库存查询对象人工维护边界 | 预计入、库存事务、库存余额前端均存在部分新增/编辑/导入/删除组件或 API Hook，但当前关键按钮被注释；库存事务和余额从业务模型看也不应作为普通人工维护对象。 | `expectin/index.vue`、`transaction/index.vue`、`balance/index.vue`，2026-07-15 | 培训材料和业务字典若只按代码 Hook 描述，会误导用户认为可日常手工维护预计入、事务和余额。 | 已按“查询/追溯为主”写入 P0 样板，待权限和测试环境复核 |
| GAP-013 | 物料编码变更与删除引用影响 | Web 编辑表单会禁用物料号 `code`，正常操作不能改码；但通用更新接口仍接收 `code`，`ItembasicService` 也保留改码时同步 `itemexpand` 的逻辑，且更新前未见同等的物料号重复校验。删除接口存在。BOM、库存、采购、生产、质量等跨模块引用是否限制、同步或禁止，尚未完成端到端验证。 | `itembasic/index.vue`、`ItembasicService`、物料前端页面，2026-07-16 | 页面规则与接口/服务兼容逻辑不一致；若绕过页面调用接口，可能造成物料引用或编码唯一性风险。培训、测试和现场维护不能把服务兼容分支当作可操作功能。 | 已补 P0 文档，待技术评估与测试验证 |
| GAP-014 | RBAC 权限结论缺少逐页实测矩阵 | 已建立 RBAC 与动作权限取证模板，但当前尚未形成按页面、角色、按钮、接口、数据范围、状态前置条件汇总的实测矩阵。 | `RBAC与动作权限取证模板.md`、前端 `hasPermi`/`v-hasPermi` 初扫、System 权限源码初扫，2026-07-15 | 如果只按前端按钮权限写文档，可能误导培训和授权配置，无法回答“某角色在某状态下到底能否执行”。 | 模板已建立，待 BATCH-01 起逐页填证据 |
| GAP-015 | System / Infra 公共能力实现边界待核验 | 标签、条码、打印、报表、看板、查询、导入导出、消息、日志、定时任务等公共能力已建立文档入口，但真实菜单、服务、表结构、组件归属和业务引用尚未逐项取证。 | BATCH-02 占位执行清单、BATCH-02 覆盖审计，2026-07-16 | 若不核验实现边界，培训材料容易把当前入口、目标归属和实际运行机制混为一谈。 | 已登记，BATCH-02 后续逐项取证 |
| GAP-016 | MES/QMS/EAM/ANDON 现场执行规则待逐页核验 | BATCH-03 已补齐 EAM、ANDON 和终端操作薄弱入口，但生产执行、质量判定、设备巡检维修、异常响应的状态机、终端规则和跨模块回写尚未完成源码与测试环境取证。 | BATCH-03 占位执行清单、BATCH-03 覆盖审计，2026-07-16 | 如果直接复用 WMS 或历史草稿规则，会误导现场培训、权限配置和异常处理闭环。 | 已登记，BATCH-03 后续逐项取证 |
| GAP-017 | SCP/PS/API 字段、状态与接口挂接待逐项取证 | BATCH-04：SCP 七组第二轮已齐套（主文档+维护参考+证据）；剩余为截图/DBC 同步/收货同实例/门户权限/ERP 付款等 P2。本仓库 **无** PS 后端模块、**无** menu.csv 排程菜单、**无** frontend `views/ps`；PS/API 仍待第二轮或仓外取证。 | BATCH-04 清单、SCP 字段证据、[PS 资料缺口与取证清单](../04-资料与证据/字段证据/PS/资料缺口与取证清单.md)，2026-07-17 | 若把未证实同步/付款写成事实，可能误导联调。 | SCP 主文档齐套；PS/API 与 SCP P2 仍开 |
| GAP-018 | 物料导入模板与保存校验双层不一致 | 导入模板读取使用 `ItembasicImportExcelVo`，其中替代计量单位、是否脱离 ERP 等字段为必填；随后转换为 `ItembasicExcelVO` 保存，后者对这些字段并非必填。两层还都将 `isRecycled` 标为“标准件”，与已确认的“回收件”语义冲突。 | `ItembasicImportExcelVo`、`ItembasicExcelVO`、`WinBaseController`，2026-07-16 | 用户填写规则、错误提示、模板维护和测试用例可能互相矛盾，且会延续前端错误术语。 | 已在物料 P0 页标记，待产品/开发确认模板口径 |
| GAP-019 | 预计入库存页面的批量物理删除 | 预计入列表的新增、导入、逐行编辑/删除按钮已注释，但页头“批量删除”按钮未注释，并调用 `deleteExpectinIds`。服务把 ID 列表直接传入 `physicsDelete`；Controller 的 `wms:expectin:delete` 权限注解被注释，未见任务状态、来源任务或下游关联检查。 | `wms/inventoryManage/expectin/index.vue`、`ExpectinController`、`ExpectinServiceImpl`，2026-07-16 | 预计入原则上应由任务创建/释放。当前链路可物理清除记录，可能破坏任务与预计入的可追溯关系，也缺少可复用的 RBAC 证据。 | 已在库存 P0 页标记，待测试环境验证并由产品/开发确定保留、改为受控释放或移除入口 |
| GAP-020 | 预计入单条删除接口参数错位 | 前端 API 将单条删除请求发送到 `/wms/expectin/delete?id=...`；Controller 的 `/delete` 实际要求 `jobNumber`，而名为 `/deleteByJobNumber` 的接口实际要求 `id`。当前单条删除按钮虽已注释，接口定义仍不一致。 | `api/wms/expectin/index.ts`、`ExpectinController`，2026-07-16 | 若后续重新暴露单条删除或外部调用此 API，可能发生请求参数错误、删除范围误解或联调失败。 | 已登记，待接口专项修正与回归验证 |
| GAP-021 | WMS 库存关键接口的后端权限证据缺失 | 预计入、库存事务和库存余额页面的导出按钮均带前端 `hasPermi`；但对应 Controller 的导出 `@PreAuthorize` 注解均已注释。库存事务创建、预计入删除等关键接口也存在注释的权限注解。 | `expectin/index.vue`、`transaction/index.vue`、`balance/index.vue` 及三个 Controller，2026-07-16 | 当前只能证明前端按钮层的权限配置，不能证明接口层强制鉴权；直接调用接口、菜单权限绕过和 RBAC 审计均存在待确认风险。 | 已在库存 P0 页标记，待 System/RBAC 与测试环境联合核验 |
| GAP-022 | 库存余额详情的事务页签未按完整余额粒度过滤 | 余额详情调用 `TransactionController.page_balance` 时，后端只将余额的 `itemCode`、`packageNumber`、`batchId` 写入事务查询条件；未传入托盘、库存状态、库位等已确认的余额业务粒度。 | `TransactionController.page_balance`、余额业务唯一粒度，2026-07-16 | 同一物料/包装/批次在不同托盘、状态或库位下的事务可能混入同一余额详情，影响追溯准确性和培训结论。 | 已在库存 P0 页标记，待测试环境复现并由开发评估查询条件 |
| GAP-023 | BOM 业务键缺少数据库约束且更新边界不一致 | BOM 创建服务按父项物料、子项物料、版本校验重复，DDL 未见该组合的复合唯一约束；Web 编辑锁定父/子物料，但服务未覆盖 `beforeUpdate` 实施同等锁定。 | `BomService`、`bom/index.vue`、`base.sql`，2026-07-16 | 并发或绕过页面调用可能产生重复/冲突 BOM 关系，且与页面培训规则不一致。 | 已在 BOM 页标记，待技术评估与测试验证 |
| GAP-024 | BOM 导入模型与历史字段口径不一致 | BOM 模板读取使用 `BomImportExcelVo`，随后转换为 `BomExcelVO` 保存；两者的必填/导出注解不完全一致。历史文档还把单表 BOM 关系写成表头/表体模型并使用伪字段名。 | `BomImportExcelVo`、`BomExcelVO`、`basic_bom`、`BomService`，2026-07-16 | 导入模板、字段字典、培训和接口联调可能采用错误口径。 | 已在 BOM 页标记，待逐段字段校正与导入专项验证 |
| GAP-025 | 物料包装业务键与编辑边界不一致 | 物料包装创建服务按 `itemCode + packageUnit` 校验重复，但 DDL 未见复合唯一约束；Web 编辑锁定物料代码、包装规格，更新服务仍接收并写回包装规格，且完整更新校验被注释。 | `ItempackageServiceImpl`、`itempackage/index.vue`、`base.sql`，2026-07-16 | 并发或绕过页面调用可能出现重复/冲突记录，也使 Web 培训规则与接口边界不一致。 | 已在物料包装页标记，待技术评估与测试验证 |
| GAP-026 | 物料包装显示/导入模型与持久化字段不一致 | 前端/响应 VO 中存在包装名称、包装类型和替代包装字段；DDL/DO 未存储这些字段，名称/类型由包装规格补充。导入读取 VO、保存/导出 VO 也使用不同字段集合，普通新增与导入的存在性/数量校验层级不同。 | `ItempackageBaseVO`、`ItempackageImportExcelVO`、`ItempackageExcelVO`、`ItempackageDO`、`ItempackageController`，2026-07-16 | 若将显示字段或导出审计字段作为人工维护字段，会造成模板、接口和业务字典口径错误。 | 已在物料包装页标记，待导入专项与字段字典校正 |
| GAP-027 | 物料包装动作权限命名及接口保护不一致 | Web 按钮使用 `dbc:itempackage:*`；后端启用/禁用接口使用 `wms:itempackage:*`，创建/更新/删除接口的权限注解被注释。 | `itempackage/index.vue`、`ItempackageController`，2026-07-16 | 前端按钮可见性不能证明接口层授权，角色配置与越权风险无法仅从页面判断。 | 纳入 RBAC 实测矩阵，待 System/RBAC 与测试环境联合核验 |
| GAP-028 | 包装规格业务键、层级和编辑边界未在各层闭合 | 包装规格 `code` 仅由服务查重，DDL 未见唯一约束；Web 编辑锁定代码、名称、类型，但服务更新可整体写回；创建未见父子同码校验，更新仅限制直接自引用。 | `PackageunitServiceImpl`、`packageunit/index.vue`、`base.sql`，2026-07-16 | 并发或绕过页面调用可能产生重复代码、关键字段变更或包装层级循环。 | 已在包装规格页标记，待技术评估与测试验证 |
| GAP-029 | 包装规格模板、数据类型与导入行为不一致 | DDL 尺寸/重量为 `numeric(18,6)`，DO/前端按整数处理；Controller 实际读取 `PackageunitimportNewExcelVO`，另有同名但物料字段集合的旧导入类。覆盖模式下不存在代码未见错误记录，导入更新接口登记仍使用 `/create`。 | `basic_packageunit`、`PackageunitDO`、`PackageunitimportNewExcelVO`、`PackageunitImportExcelVo`、`PackageunitServiceImpl`，2026-07-16 | 数据精度、模板字段、导入成功/失败语义及第三方同步可能被误解。 | 已在包装规格页标记，待导入、接口和数据精度专项核验 |
| GAP-030 | 包装规格动作权限命名及接口保护不一致 | Web 按钮使用 `dbc:packageunit:*`；后端启用/禁用使用 `wms:packageunit:*`，创建、更新、删除、导入、导出权限注解被注释。 | `packageunit/index.vue`、`PackageunitController`，2026-07-16 | 页面显示不能证明服务端授权，角色配置与越权风险无法仅从页面判断。 | 纳入 RBAC 实测矩阵，待 System/RBAC 与测试环境联合核验 |
| GAP-031 | 物料仓库默认配置的前后端包装字段与对象不一致 | Web 表单/API 使用 `packageUnit` 并从物料包装信息选择；后端 VO/DO 使用 `packUnit`，服务按包装规格主数据校验。 | `itemwarehouse.data.ts`、`api/dbc/itemwarehouse`、`ItemwarehouseBaseVO`、`ItemwarehouseServiceImpl`，2026-07-16 | Web 新增/编辑可能无法提交或保存空包装字段，且物料包装关系与包装规格主数据的语义混淆。 | 已在物料库区配置页标记，待前后端接口联调与修正 |
| GAP-032 | “物料库区配置”页面实际边界与名称/历史草稿不一致 | 当前 `basic_itemwarehouse` 主表单只维护物料、仓库、管理精度、包装规格；历史草稿的库区、库位、默认、优先级未接入当前 VO/页面。DDL 中的策略扩展列也多数未接入。 | `basic_itemwarehouse`、`ItemwarehouseBaseVO`、`itemwarehouse.data.ts`，2026-07-16 | 培训与需求说明可能把“仓库默认配置”误解为已实现的库区/库位策略能力。 | 后续明确当前/目标两层文档边界，并逐项取证扩展字段 |
| GAP-033 | 物料仓库默认配置的唯一性、编辑与删除边界不足 | DDL 未见 `itemCode + warehouseCode` 唯一约束；普通创建未见重复校验，Web 锁定物料/仓库而服务更新可整体写回，删除未见下游引用检查。 | `base.sql`、`ItemwarehouseServiceImpl`、`itemwarehouse/index.vue`，2026-07-16 | 并发或旁路调用可能产生重复、关键关联变更或下游影响未受控。 | 已在页面标记，待技术评估与测试验证 |
| GAP-034 | 物料仓库默认配置导入术语与行为不清晰 | 模板写“区域代码”但服务/DDL为仓库代码；导入用 `inPackUnit` 同步写入入/出库包装，模式 3 对不存在记录未见错误，且导入未见普通维护同等 SCP 接口登记。 | `ItemwarehouseImportVO`、`ItemwarehouseServiceImpl`、`ItemwarehouseController`，2026-07-16 | 模板字段、导入结果与外部同步语义可能被误解，导致配置漏导或错配。 | 纳入导入与接口专项回归 |
| GAP-035 | 生产线物料关系的页面字段与真实模型不一致 | 历史草稿中的主产线、默认用量不在当前表；实际原料库位、完工收货库位在主 Web 页面又被注释，导入模板也不包含。 | `basic_productionlineitem`、`ProductionlineitemBaseVO`、`productionlineitem.data.ts`，2026-07-16 | 字段字典、培训和导入模板可能错误描述当前配置能力。 | 已在页面标记，待以存储/已启用/目标三层持续校正 |
| GAP-036 | 生产线物料关系的唯一性、编辑与库位校验不完整 | 产线+物料只在服务层查重，DDL 无复合唯一约束；Web 锁定产线/物料但服务更新可处理；库位校验方法未接入创建/更新流程。 | `ProductionlineitemService`、`productionlineitem/index.vue`、`base.sql`，2026-07-16 | 并发或旁路操作可能产生重复、变更关键关联或写入无效库位。 | 待技术评估与测试验证 |
| GAP-037 | 生产线物料关系的导入与分类视图边界不清 | PC/YCL 视图按 `CCP`、`BCP` 分类并有专用导出，但主导入模板包含物料类型；模式 2/3 对不符合预期的存在/不存在记录未见明确错误写入。 | `ProductionlineitemService`、`ProductionlineitemPCController`、`ProductionlineitemYCLController`，2026-07-16 | 导入模板、成功反馈与原材料/产成品业务范围可能混淆。 | 纳入 PC/YCL 入口和导入专项回归 |
| GAP-038 | 标准成本价格单的供应商与唯一性模型不一致 | `supplier_code` 在 DDL/VO 中存在，但 Web 表单和导入模板未录入，服务唯一校验与导入查找均只按 `item_code`；DDL 未设置业务唯一索引。 | `basic_stdcostprice`、`StdcostpriceServiceImpl`、`stdcostprice.data.ts`，2026-07-16 | 供应商维度是否应参与标准成本价格、并发下是否可能重复，均无法得到一致结论。 | 待产品口径和技术方案确认 |
| GAP-039 | 标准成本价格期间、调用返回与导入反馈不完整 | 未见生效/失效期间校验或旧价自动失效；单条取价在不可用/不存在时返回价格 0，批量查询返回全部记录；导入模式 2/3 对跳过行未见明确错误回执。 | `StdcostpriceServiceImpl`、`StdcostpriceController`，2026-07-16 | 成本使用、导入成功判断和价格有效性可能产生歧义。 | 登记问题并继续逐页取证；纳入成本与导入专项回归 |
| GAP-040 | 采购退货预计出创建链路未闭合 | 采购退货申请生成任务时，当前服务中创建预计出的调用被注释；任务执行后仍按任务号删除预计出。 | `PurchasereturnRequestMainService`、`PurchasereturnJobMainService`，2026-07-16 | 预计出是否存在、可用库存扣减和执行后清理可能不一致。 | 待技术评估与测试验证；不阻塞采购退货其它事实取证 |
| GAP-041 | “生产收料”页面混合两组实现对象 | 当前同时存在制品收货 `productreceipt`（申请/任务/记录）和生产收料 `productionreceipt`（任务/记录）对象族；现有页面将完工、质检、分流、退料汇为单一流程。 | `Productreceipt*Service`、`ProductionreceiptJobMainService`、Web/PDA 路径，2026-07-16 | 培训、字段字典、菜单对应和跨 MES/QMS 的接口说明可能产生错误映射。 | 先按对象族建立事实边界；后续按菜单与测试环境决定拆页或补子章节 |
| GAP-042 | 库存转移的预计入创建在执行分支间不一致 | 库存转移人工任务生成路径明确创建预计出，预计入调用被注释；其它分支仍可创建预计入，记录完成时则统一清理两类预计数据。 | `InventorymoveRequestMainService`、`InventorymoveRecordMainService`，2026-07-16 | 库内转移的可用库存预占、目标库存预期及库存看板可能因执行路径不同而表现不一致。 | 待按业务类型、AGV/人工路径和测试数据专项回归；不阻塞其它库内作业取证 |
| GAP-043 | 盘点历史流程与当前差异库实现不一致 | 历史盘点页面将差异库存单独建账、再出库/报废作为标准流程；当前盘点调整服务中 DIFF 差异库清账事务整段为注释，实际可证实链路为盘点调整正负数生成入/出库存事务。 | `CountadjustRequestMainService`、`CountadjustRecordMainService`、`12-盘点管理/index.md`，2026-07-16 | 培训、盘点差异处理、库存报表与接口设计若沿用差异库存流程，可能与当前运行逻辑不符。 | 已在页面校正；待产品确认差异库能力是否废弃、重启或另有运行入口 |
| GAP-044 | 供应商主数据的唯一性、导入与权限边界不一致 | 创建服务按 `code` 查询重码，但 DDL 仅有普通代码索引；普通创建只要求代码、简称、可用状态，导入额外要求邮件并强制写可用；Web 使用 `dbc:supplier:*`，多数后端权限注解被注释，停用接口却保留 `wms:supplier:disable`。 | `basic_supplier`、`SupplierServiceImpl`、`SupplierImportExcelVo`、`supplier/index.vue`、`SupplierController`，2026-07-16 | 并发或旁路操作的唯一性、导入模板口径、角色授权及接口保护无法形成一致结论。 | 已在供应商页标记；待技术评估、RBAC 实测与导入回归，不阻塞其它主数据取证 |
| GAP-045 | 供应商物料的关联存在性、业务键与导入范围未闭合 | 创建服务按供应商+物料代码查重，但 DDL 无复合唯一约束；代码校验仅判断非空，未见强制验证供应商/物料主数据存在；导入类将包装单位、包装数量、结算方式注释掉，创建/更新则仍接收这些字段。 | `basic_supplieritem`、`SupplieritemServiceImpl`、`SupplieriteamImportExcelVo`、`supplieritem.data.ts`，2026-07-16 | 旁路调用或并发可能形成无效/重复关联；人工导入无法完整表达当前采购包装与结算配置。 | 已在供应商物料页标记；待技术评估、导入回归与 SCP 接口联调，不阻塞其它主数据取证 |
| GAP-046 | 客户主数据的业务键、导入与数据库约束未闭合 | 创建服务要求代码、名称、简称、可用状态，Web 编辑锁定代码/名称/简称；客户子类更新流程未见同等回填或校验，导入又只显式要求代码、简称、可用。DDL 对代码仅有普通索引。 | `basic_customer`、`CustomerServiceImpl`、`CustomerImportExcelVo`、`customer/index.vue`，2026-07-16 | 接口绕过页面时可能修改关键身份字段；导入和创建的最低数据口径不同；并发唯一性无法由数据库保证。 | 已在客户页标记；待更新接口、导入与 RBAC 的专项回归，不阻塞客户月台/客户物料等后续取证 |
| GAP-047 | 客户月台的唯一性、关联验证与导入边界不闭合 | 服务按月台代码查重，DDL 无唯一约束；客户代码只校验非空，创建后强制可用；Web 编辑锁定月台代码和客户代码，导入却忽略仓库和可用字段。 | `basic_customerdock`、`CustomerdockServiceImpl`、`CustomerdockImportExcelVo`、`customerdock/index.vue`，2026-07-16 | 并发/旁路操作可能产生冲突月台或无效客户关联；导入不能表达完整仓库映射，且与页面状态规则不一致。 | 已在客户月台页标记；待技术评估、导入回归与销售出库引用取证，不阻塞其它主数据工作 |
| GAP-048 | 仓库编码的唯一性、编辑与删除保护未闭合 | DDL 中代码仅有普通索引；普通新增服务只校验代码非空和可用状态非空，未见重码查询。Web 编辑锁定代码，服务更新仍可写回；删除仅校验记录存在，未见下游引用检查。 | `basic_warehouse`、`WarehouseServiceImpl`、`warehouse/index.vue`，2026-07-16 | 并发或旁路调用可能产生重复编码、修改关键仓储范围或删除被库区/库位/库存业务引用的仓库。 | 已在仓库页标记；待技术评估、RBAC 实测与下游引用回归，不阻塞库区、库位等后续取证 |
| GAP-049 | 库区普通保存与导入规则的校验闭合不足 | 库区服务继承通用 CRUD，未覆写新增/更新前置钩子；类内代码、仓库等私有校验方法不能仅由当前调用链证明会在普通保存时执行。Web 锁定代码和仓库，通用更新仍整体写入；DDL 无代码或仓库+代码唯一约束。导入则显式校验代码、仓库、类型、可用状态和部门。 | `AreabasicServiceImpl`、`WinBaseService`、`basic_area`、`areabasic/index.vue`，2026-07-16 | 普通保存、导入和页面操作可能有不同约束；旁路调用可能修改关键归属或产生重复库区，培训和接口规则不能混写。 | 已在库区页标记；待通用框架调用、RBAC、下游引用和测试环境回归，不阻塞库位等后续取证 |
| GAP-050 | 库位通用入口与专用服务规则分离，且 ERP/QAD 字段口径不一致 | 当前 Controller 的通用新增、更新、删除调用 `WinBaseService`；服务中包含更完整的 `createLocation/updateLocation/deleteLocation` 规则，但对应自定义路由被注释。页面把 `erpLocationCode` 称为“ERP库位类型”，VO 又称“ERP库位代码”，导入“ERP库位代码”实际映射 `qadLocationCode`。DDL 的库位代码仅为普通索引。 | `LocationController`、`WinBaseController`、`LocationService`、`LocationImportExcelVO`、`basic_location`，2026-07-16 | 当前保存路径的唯一性、空间归属、容量、标签清理和改码保护无法与导入或专用分支混写；ERP/QAD 字段可能导致培训、模板和集成误配。 | 已在库位页标记；待路由、测试环境、接口和标签链路专项核验，不阻塞库位组等后续取证 |
| GAP-051 | 库位组业务键、编辑与删除保护未闭合 | 专用服务按 `code + areaCode` 检查重复，Web 编辑锁定代码和仓库但允许改库区，服务可整体更新；DDL 未设置该组合唯一约束，删除未见库位引用检查。Controller 导入权限注解被注释。 | `LocationgroupServiceImpl`、`LocationgroupController`、`locationgroup/index.vue`、`basic_locationgroup`，2026-07-16 | 并发或旁路调用可能产生同库区重复组、修改已有库位的空间归属或删除被引用的分组；前端权限标识不能证明接口保护。 | 已在库位组页标记；待技术评估、RBAC 实测和下游引用回归，不阻塞月台等后续取证 |
| GAP-052 | 内部月台业务键、编辑与导入匹配键未闭合 | 内部月台 `basic_dock` 与客户月台 `basic_customerdock` 为不同对象。内部月台保存会校验类型、仓库及默认库位归属，但代码只校验非空，DDL 仅有普通索引；Web 编辑允许改关键归属，删除未见引用检查。通用导入已接入字段/关联校验，但未见该页面既有记录匹配键覆写证据。 | `DockServiceImpl`、`DockController`、`DockImportExcelVO`、`basic_dock`，2026-07-16 | 可能产生重复月台代码、改变在用月台的仓储范围或误把两类月台混为一谈；导入覆盖行为及接口授权需专项验证。 | 已在月台页标记；待业务单据引用、RBAC、导入与测试环境回归，不阻塞其它工厂建模取证 |
| GAP-053 | 车间模型历史字段失真，导入与普通保存规则不一致 | 当前 `basic_workshop` 及页面仅有代码、名称、类型、可用、有效期、备注等字段，不含工厂/组织、负责人、父车间。代码唯一性只在服务层实现；普通保存校验可用状态，导入只显式校验代码/名称，并将新增记录强制可用，未见错误整批回滚。 | `basic_workshop`、`WorkshopServiceImpl`、`WorkshopImportExcelVo`、`workshop.data.ts`，2026-07-16 | 历史字段会误导组织和审批培训；导入状态、类型和失败处理可能与页面口径不一致，且并发唯一性无法由数据库保证。 | 已在车间页标记；待技术评估、测试与 MES/QMS/ANDON 关联取证，不阻塞生产线等后续工作 |
| GAP-054 | 生产线代码、车间关联与库位配置在分层间未闭合 | `basic_productionline` 存储车间、原料/成品库位，主页面/模板却未维护库位字段。专用新增按代码查重，普通校验仅检查代码和车间代码非空且车间存在性检查被注释；导入才查询可用车间并回填名称。DDL 无业务唯一约束。 | `ProductionlineServiceImpl`、`ProductionlineImportExcelVO`、`productionline.data.ts`、`basic_productionline`，2026-07-16 | 保存入口、车间名称同步、库位配置、并发唯一性与培训口径可能不一致；不可将注释掉的库位 UI 或历史节拍/班次视为当前功能。 | 已在生产线页标记；待路由、测试、MES/工位/工序关联及 RBAC 专项核验，不阻塞工位等后续取证 |
| GAP-055 | 工位归属、库位、模板与真实校验未闭合 | 工位代码仅在服务层查重，DDL 无唯一约束；前端通过已启用车间、按车间筛选的生产线及库区类型筛选库位构造选择器，服务普通保存却只做空值判断，关联存在性与库位校验均已注释。Web 必填类型，导入模板却不含类型且仍含当前 Web 已注释的工序列。 | `basic_workstation`、`WorkstationServiceImpl`、`WorkstationImportExcelVo`、`workstation.data.ts`，2026-07-16 | 旁路调用或导入可能写入不一致归属；模板、现场培训与实际页面能力不能采用同一未校正口径；并发唯一性无法由数据库保证。 | 已在工位页标记；待技术评估、导入/终端/MES 联调与 RBAC 实测，不阻塞其它工厂建模取证 |
| GAP-056 | DBC 模具历史入口与当前 EAM 工装实现边界不明 | 菜单仍含 `dbc/pattern`、`dbc/patternType`，但当前 DBC 前后端及 `base.sql` 未发现对应实现或表。可见运行线索集中于 EAM 的 `basic_tool_accounts`、工装记录和移动端模具查询；移动端调用 `/eam/mold-accounts/*`，当前提供的服务端台账 Controller 路径则为 `/eam/item-accounts`。 | `menu.csv`、`ui`、`win-module-dbc`、`base.sql`、`eam.sql`、EAM 移动端及 `ItemAccountsController`，2026-07-16 | 历史模具字段、寿命、状态、导入和流程无法作为当前功能说明；PC/PDA 接口路径及模具/工装/备件对象边界可能不一致。 | 已将 DBC 页面校正为历史入口说明；待 EAM 工装专项、菜单清理/迁移决策和接口联调，不阻塞点位等其它 DBC 取证 |
| GAP-057 | 点位关键编码、页面表单与导入更新规则不一致 | `basic_point.code` 是非空持久化字段，创建服务按代码查重，导入也要求点位代码；当前 Web 却注释点位代码字段。创建同时检查代码+库位，DDL 无唯一约束。导入更新分支只更新旧对象的库位名称和可用状态，未写回导入行的其它业务字段。 | `basic_point`、`PointServiceImpl`、`PointImportExcelVO`、`point.data.ts`，2026-07-16 | Web 新增可能无法提供服务所需关键键；导入结果可能显示成功却未更新业务属性；代码全局唯一与代码+库位匹配口径不一致。 | 已在点位页标记；待测试复现、明确业务键、修复表单/导入并完成 AGV/RBAC 联调，不阻塞潜伏式 AGV 点位等后续取证 |
| GAP-058 | 潜伏式 AGV 点位的业务键查重引用旧字段 | 当前模型的非空配置点位为 `point_code_config`，导入匹配键也使用配置点位；但创建/更新服务的查重查询仍将该参数写入 `port_code`，而 `port_code` 是保留的可空旧列，料口关联逻辑已注释。DDL 未设置 `point_code_config + point_code + type` 复合唯一约束。 | `basic_latent_agv_point`、`LatentAgvPointServiceImpl`、`LatentAgvPointImportExcelVO`、`agvPoint.data.ts`，2026-07-16 | 新增/编辑与导入可能采用不同唯一性口径，重复配置难以稳定拦截；料口模式与双点位模式混杂会误导 AGV 配置和培训。 | 已在页面标记；待技术修正、测试验证与 AGV 调度联调，不阻塞其它 BATCH-01 工作 |
| GAP-059 | 客户物料的包装校验、导入字段与权限标识不一致 | 客户+物料关系仅由服务查重，DDL 无复合唯一约束；普通保存仅检查包装规格非空，导入才校验包装规格类型。导入 VO 使用 `packUnit/packQty`，而页面/DO 使用 `packageUnit/packageStdQty`；Web 新增权限拼写为 `dbc:customeitem:create`，其它动作为 `dbc:customeritem:*`。 | `basic_customeritem`、`CustomeritemServiceImpl`、`CustomeritemImportExcelVO`、`customeritem.data.ts`、`customeritem/index.vue`，2026-07-16 | 旁路调用与导入可能有不同包装约束；导入映射、角色配置和接口保护无法形成一致结论。 | 已在客户物料页标记；待导入映射、RBAC、销售/SCP 引用和测试环境回归，不阻塞后续主数据取证 |
| GAP-060 | 业务类型策略的唯一性与分层校验未闭合 | `document_businesstype.code` 仅由服务层查重，DDL 仅有普通代码索引；VO/DDL 对分组、事务类型等的声明与服务显式校验不完全一致。当前导入未见对全部库区、库位、流水号、模板和事务类型的完整服务端引用校验，模板还包含审计列且“更新者”列标题为“创建者”。打印模板当前由 WMS 标签模板入口提供，产品目标归属为 INFRA。 | `document_businesstype`、`BusinesstypeBaseVO`、`BusinesstypeExcelVO`、`BusinesstypeServiceImpl`、`businesstype` Web 设置页，2026-07-16 | 策略配置可能在普通保存、导入和运行时表现不一致；并发或旁路写入的唯一性、模板填写和平台能力归属不能作为已闭合规则。 | 已在业务类型页标记；待技术评估、导入/引用/RBAC/测试环境回归和 INFRA 归属治理，不阻塞后续页面取证 |
| GAP-061 | 单据设置的业务类型与编号规则保护未闭合 | `document_documentsetting.code` 仅由服务层查重，DDL 仅有普通索引；`businessType` 为自由文本，未见业务类型存在性/可用性校验。号码流水长度允许 0、日期格式未见服务端解析校验，当前未见已使用单据的改码/禁用/删除保护。导入按钮被注释，保留的导入 URL 为 `/wms/documentsetting/import`，与 Controller 根路径 `/dbc/documentsetting` 不一致。 | `document_documentsetting`、`DocumentsettingServiceImpl`、`DocumentsettingImportExcelVO`、`documentsetting.data.ts`、`documentsetting/index.vue`，2026-07-16 | 编号规则、业务关联和导入入口可能在页面、接口与运行时不一致；并发或旁路写入可能产生冲突，培训不应把当前可编辑页面视作在用配置的安全变更流程。 | 已在单据设置页标记；待编号服务、引用关系、导入/RBAC 与测试环境回归，不阻塞后续页面取证 |
| GAP-062 | 单据开关受控维护与消费方闭环缺失 | `document_switch.code` 只有普通索引，服务创建不见非空、唯一性或值域校验，更新只检查主键存在；当前 Web 无新增/导入/导出/删除入口，代码只读，但四个特定代码仅在前端隐藏编辑。`available=false` 或代码不存在时，各调用方的默认行为尚未形成矩阵。 | `document_switch`、`SwitchBaseVO`、`SwitchServiceImpl`、`switch.data.ts`、`switch/index.vue`，2026-07-16 | 关键校验或自动动作可能因旁路更新、停用、缺失或前端隐藏失效而产生不可预期行为；培训、权限与变更管理无法形成闭环。 | 已在单据开关页标记；待消费方全量检索、后端授权/变更保护和测试环境回归，不阻塞后续页面取证 |
| GAP-063 | 规则管理的后端模型、Web 字段与导入入口不一致 | 当前 `strategy_rule` 后端模型以业务类型、分类、版本、状态、条件、节点数据为核心；Web 却使用策略代码、优先级、名称、配置等字段，VO 中 `configuration` 也不在 DDL。服务另有部分 `strategy_code/priority` 残留查询；Web 有导入/导出按钮，Controller 未见对应端点。已发布不可更新、非草稿不可删除的限制仍为注释代码。 | `strategy_rule`、`RuleBaseVO`、`RuleServiceImpl`、`RuleController`、`rule.data.ts`、`rule/index.vue`，2026-07-16 | 规则配置、版本治理、导入模板和前端操作可能指向不同模型，错误变更或错误字段培训会直接影响库存精度与业务自动化。 | 已在规则管理页标记；待前后端版本迁移、调用方、导入/RBAC 与测试环境回归，不阻塞 BATCH-01 其它页面 |
| GAP-064 | 采购退货执行分支与预计出/取消闭环待验证 | 采购退货任务执行已证实会校验数量、余额与执行锁，生成记录和出库库存事务后清理任务号对应的预计出；但当前未定位普通任务创建预计出的具体路径。退货还存在按来源类型直接生成记录、独立取消记录主明细及多套申请导入 VO，Web/PDA 状态动作、反向库存事务、预计出恢复和外部接口补偿尚未闭合。 | `PurchasereturnJobMainService`、采购退货 VO/DDL、WMS 前端目录，2026-07-16 | 将采购收货或普通申请—任务—记录规则直接套用于退货，可能误导现场执行、库存追溯、取消处理和接口对账。 | 已在采购退货页标记；待预计出创建调用链、PDA、取消服务、RBAC、导入模板和测试环境回归，不阻塞后续 WMS 链路取证 |
| GAP-065 | 采购上架双向事务与预计库存/多入口边界待验证 | 采购上架申请生成任务时会按目标批次、库存状态、库位创建预计入；任务执行按来源余额校验，生成出、入双向库存事务后清理任务预计入和预计出。服务还存在直接记录和规则配置分支，但目标库位/状态选择器、取消恢复、Web/PDA/直接入口状态与权限、导入字段未闭合。 | `PutawayRequestMainService`、`PutawayJobMainService`、`PutawayRecordMainService`、上架前端目录，2026-07-16 | 若只按“收货后入库”描述，会遗漏来源库存扣减、状态/库位转换和预计库存释放；不同入口可能形成不同操作边界。 | 已在采购上架页标记；待规则、终端、撤销、导入、RBAC 与测试环境回归，不阻塞后续 WMS 链路取证 |
| GAP-066 | 发料管理多业务链的库存、终端与接口边界待逐项拆分 | 标准生产发料已证实：申请推荐来源余额并创建预计出，任务/记录执行按物料、包装/父包装、批次、库位、库存状态校验余额，生成库存事务后清理预计出；补料、备料、委外、计划外发料为独立服务域，补料还受低储配置、余额和预计入影响。 | `IssueRequestMainService`、`IssueJobMainService`、`IssueRecordMainService`、`repleinsh`/`preparetoissue`/`outissue` 服务，2026-07-16 | 将所有发料类业务写成同一状态、字段或库存流程会误导仓储和生产现场；补料触发、终端扫描和外部回写需要分别验证。 | 已在发料管理页标记；待子业务、PDA/线边端、撤销、接口、RBAC 和测试环境回归，不阻塞后续 WMS 链路取证 |
| GAP-067 | 生产收料的预计入、质量分流与 MES 消息闭环待验证 | 当前服务已证实：生产收料任务执行创建库存事务并清理任务预计入；WMS 发料执行/PDA 直接发料可向 MES 发送创建生产收料任务消息，任务完成再向 MES 发完成消息，并建立库存移动接口信息。预计入创建来源、QMS 合格/隔离分流、上架后续、消息幂等/失败补偿及多端状态/权限仍未闭合。 | `ProductionreceiptJobMainService`、`ProductionreceiptJobRedisMqService`、`ProductionreceiptRecordMainService`，2026-07-16 | 若将生产收料直接描述为单向入库，会遗漏 MES 异步协作、预计入释放、质量分流和接口追溯边界。 | 已在生产收料页标记；待 MES/QMS/WMS 上架链、PDA、接口/RBAC 与测试环境回归，不阻塞后续 WMS 链路取证 |
| GAP-068 | 销售出库的多链路、终端与外部接口完成态待验证 | 标准发货申请进入处理后生成任务并创建预计出；任务执行按库存管理精度、物料、包装/父包装、批次、来源库位和库存状态核验余额，生成发货记录和出库事务后清理预计出。冲销只有在记录后规则开启且接口类型匹配 WMS—ERP 库存移转时才异步生成接口信息。备货/计划、客户退货、寄售/在途、结算、取消/部分执行、PDA/线边端、导入、RBAC、接口送达与补偿均未闭合。 | `DeliverRequestMainService`、`DeliverJobMainService`、`DeliverRecordMainService`，2026-07-16 | 若将“已发货”视作统一库存、物流、结算和外部入账完成状态，会误导客户交付、对账和异常处理；不同发货子域不能共用未经证实的状态机。 | 已在销售出库页标记；待子域、终端、撤销、接口可靠性、RBAC 和测试环境回归，不阻塞后续 WMS 链路取证 |
| GAP-069 | 生产管理对象族与条件后续动作未形成可执行矩阵 | 制品收货任务执行可创建库存事务并清理预计入；回冲、外部完工/库存移转接口、制品上架申请和 QMS 检验申请分别受单据开关、执行后规则、上架规则和 QMS 开关控制。完工撤销、拆解、返修、生产退料均为独立服务对象，不能以制品收货主链替代。 | `ProductreceiptJobMainService`、`ProductredressJobMainService`、`ProductdismantleRecordMainService`、`Productrepair`/`productionreturn` 服务，2026-07-16 | 若把条件动作写成固定流程，会误导现场库存、质量、回冲与外部对账；不同对象的状态、终端、撤销和补偿可能完全不同。 | 已在生产管理页标记；待开关/规则矩阵、各子域、终端、RBAC、接口可靠性和测试环境回归，不阻塞后续 WMS 链路取证 |
| GAP-070 | 业务模块部门数据权限挂接清单未建立 | System 角色五档数据范围（全部/指定部门/本部门/本部门及下级/仅本人）已证实；System 已挂接用户/部门表。WMS 的部门列登记为注释状态；DBC/MES/QMS 等存在 Customizer 但未建“哪些业务表真正过滤”的清单。工厂/仓库/货主不是角色通用范围；库位走岗位库位机制。 | `DataScopeEnum`、`getDeptDataPermission`、各模块 `DataPermissionConfiguration`、`DataScopeLocationAspect`，2026-07-17 | 培训若宣称“配角色数据范围即全站隔离”，会误判 WMS 等业务可见性。 | 已在数据权限正式页与证据页标明边界；待逐模块挂接清单，不阻塞岗位页 |
| GAP-071 | MES 报工 NG → QMS 检验单据映射未证实 | MES 终端可产生 OK/NG 报工结果，正式页仅写「不良为质量协同线索」。尚未从源码/联调证实 NG 是否自动创建 QMS 检验申请/任务、关联键与状态回写。工艺节点扩展与 QMS 检验的精确映射同属未闭合。 | MES 终端/工艺正式页与证据、`WoWorkstationJob` 报工能力、QMS 生产检验待第二轮取证，2026-07-17 | 若写成固定自动建单，会误导现场不良处理与质量培训。 | 已登记；正式页短列表引用本号；待 QMS 生产检验与联调闭合 |
| GAP-072 | 数采 IoT 网关仓外；Node-RED 页面薄弱 | 前端五页与 `/iotgw` 路径已取证；`win-module-iotgw` 源码/DDL 不在本仓。NodeRed 菜单指向 `mes/noderedlog`，仓库无对应视图；`mes_node_red_log` 在遗留下线清单。MES/WMS 对网关 RPC、AGV `iotCallback` 仅为消费/回调线索。 | menu.csv、`views/iot`、`api/iot`、MES/WMS iotgw 依赖、[IOT 证据](../04-资料与证据/字段证据/IOT/数采管理总览.md)，2026-07-20 | 若写成完整边缘平台或可用 Node-RED 运维台，会误导实施与联调。 | 正式页已按边界+薄弱口径齐套；待网关仓/环境联调与 Node-RED 实测 |

## 处理记录

| 日期 | 编号 | 处理结论 | 处理人 |
| --- | --- | --- | --- |
| 2026-07-15 | GAP-001 至 GAP-004 | 由 WMS 纵向试点先验证写法；涉及实际状态口径的事项等待测试环境验证。 | 文档建设工作 |
| 2026-07-15 | GAP-001、GAP-003、GAP-004 | 已在 P0 样板中补采购收货库存挂接、PDA 终端入口、详情分组和快速跳转样板；仍需测试环境截图和端到端验证。 | 文档建设工作 |
| 2026-07-15 | GAP-009 至 GAP-011 | 由采购收货源码取证发现，先登记为待确认，不直接判定为缺陷。 | 文档建设工作 |
| 2026-07-15 | GAP-012 至 GAP-013 | P0 样板补强时发现，按“问题登记 + 继续推进”处理：正文不写成最终结论，差距总账保留后续验证任务。 | 文档建设工作 |
| 2026-07-15 | GAP-014 | 已建立 RBAC 与动作权限取证模板；后续页面只能复制模板结构，具体权限结论需逐页取证。 | 文档建设工作 |
| 2026-07-17 | GAP-070 | 数据权限第二轮回填确认：角色部门五档与 System 挂接已证实；WMS 部门挂接注释；工厂/仓库/货主非角色通用范围。正式页按边界写，挂接清单后续补。 | 文档建设工作 |
| 2026-07-17 | GAP-071、MES-BASE～MES-TERM | Track-M：MES 六组待确认收敛入总账；正式页改为编号短列表；完工→WMS 边界按 GAP-067 已证实范围写清；NG→QMS 仅登记。 | 文档建设工作 |
| 2026-07-20 | GAP-072 | 数采第二轮：五页+边缘模型齐套；Node-RED 薄弱；网关仓外与消费场景登记本号。 | 文档建设工作 |
| 2026-07-16 | GAP-015 | BATCH-02 先建立 System / Infra 公共能力结构，无法确认的实现细节保留待核验，不阻塞后续批次。 | 文档建设工作 |
| 2026-07-16 | GAP-016 | BATCH-03 先补齐 MES/QMS/EAM/ANDON 现场执行结构，具体状态、终端规则和跨模块回写后续取证。 | 文档建设工作 |
| 2026-07-16 | GAP-017 | BATCH-04 先补齐 SCP/PS/API 结构，具体字段、状态、算法参数和接口挂接后续逐项取证。 | 文档建设工作 |
| 2026-07-17 | GAP-017 | SCP 七组第二轮齐套；GAP 收敛为 SCP P2 + PS/API 仍开。 | 文档建设工作 |
| 2026-07-16 | GAP-013、GAP-018 | 物料页第二轮取证确认：Web 编辑禁用物料号，但服务保留改码兼容分支；导入模板读取与保存 VO 的必填口径存在差异。正文按用户实际页面规则写，接口边界差异登记后续处理。 | 文档建设工作 |
| 2026-07-16 | GAP-048 | 仓库页第二轮取证确认：前端、服务与数据库对编码唯一性和编辑保护的边界不一致；按“登记问题、继续推进”规则保留后续技术与测试任务。 | 文档建设工作 |
| 2026-07-16 | GAP-049 | 库区页第二轮取证确认：普通 CRUD 与导入走不同校验路径，私有校验方法的实际普通保存调用仍待证实；正文不将其写为既成规则。 | 文档建设工作 |
| 2026-07-16 | GAP-050 | 库位页第二轮取证确认：通用 Controller 直接走通用 CRUD，而专用库位服务方法未由当前自定义路由调用；同时发现 ERP/QAD 导入与页面字段标签差异。 | 文档建设工作 |
| 2026-07-16 | GAP-051 | 库位组页第二轮取证确认：专用保存和导入路径可取证，但业务键仅在服务层实现，编辑/删除的下游保护与接口授权仍待验证。 | 文档建设工作 |
| 2026-07-16 | GAP-052 | 月台页第二轮取证确认：内部月台与客户月台为不同模型；正文按内部月台的真实字段、选择器和校验写入，映射及导入边界留待验证。 | 文档建设工作 |
| 2026-07-16 | GAP-053 | 车间页第二轮取证确认：历史组织、负责人和上下级字段不在当前模型；导入与普通保存的校验/状态口径分层不一致。 | 文档建设工作 |
| 2026-07-16 | GAP-054 | 生产线页第二轮取证确认：车间与库位字段的存储、页面和导入边界不一致；正文只描述已启用的当前入口，未把注释代码写为功能。 | 文档建设工作 |
| 2026-07-16 | GAP-055 | 工位页第二轮取证确认：前端选择器、普通保存校验和导入模板字段未闭合；正文按已启用页面与实际服务分别描述，未将注释校验或历史字段写成当前能力。 | 文档建设工作 |
| 2026-07-16 | GAP-056 | 模具信息页第二轮取证确认：现存 DBC 菜单/历史草稿未能在当前 DBC 源码和 DDL 中闭合；已改为历史入口与 EAM 工装线索说明，具体字段和流转留待 EAM 专项取证。 | 文档建设工作 |
| 2026-07-16 | GAP-057 | 点位页第二轮取证确认：实际模型为库位—AGV 点位映射，历史坐标模型已移除；点位编码、Web 表单和导入更新规则未闭合，按问题登记后继续推进。 | 文档建设工作 |
| 2026-07-16 | GAP-058 | 潜伏式 AGV 点位页第二轮取证确认：双点位映射模型已取证，新增/编辑的查重仍使用历史料口字段；正文按现行字段与导入路径分别说明，问题登记后继续推进。 | 文档建设工作 |
| 2026-07-16 | GAP-059 | 客户物料页第二轮取证确认：当前模型是客户—物料—客户包装映射，不含历史价格/币种字段；普通保存、导入和权限标识存在分层差异，按问题登记后继续推进。 | 文档建设工作 |
| 2026-07-16 | GAP-060 | 业务类型页第二轮取证确认：当前对象是单据策略档案，不是单纯事务字典；字段声明、服务校验、导入模板和打印入口存在分层边界，按问题登记后继续推进。 | 文档建设工作 |
| 2026-07-16 | GAP-061 | 单据设置页第二轮取证确认：当前模型是单据类型与编号参数档案；业务类型关联、编号参数、在用配置保护和导入入口未闭合，按问题登记后继续推进。 | 文档建设工作 |
| 2026-07-16 | GAP-062 | 单据开关页第二轮取证确认：当前对象是代码驱动的全局布尔策略；页面隐藏、服务约束、导入入口和消费方闭环未完成，按问题登记后继续推进。 | 文档建设工作 |
| 2026-07-16 | GAP-063 | 规则管理页第二轮取证确认：版本化规则生命周期已由后端实现，当前 Web/VO/残留服务与 DDL 存在多套字段口径；正文分层说明并登记问题后继续推进。 | 文档建设工作 |
| 2026-07-16 | GAP-064 | 采购退货页第二轮取证确认：任务执行、记录、库存事务和预计出清理主链已取证；直接记录、预计出创建、取消和多端入口保留为后续核验项。 | 文档建设工作 |
| 2026-07-16 | GAP-065 | 采购上架页第二轮取证确认：任务生成预计入、执行双向事务和执行后清理预计库存的主链已取证；规则、多入口与取消/导入边界留待验证。 | 文档建设工作 |
| 2026-07-16 | GAP-066 | 发料管理页第二轮取证确认：标准生产发料的来源推荐、预计出、余额校验、库存事务和预计出清理主链已取证；其它发料子链按问题登记后继续推进。 | 文档建设工作 |
| 2026-07-16 | GAP-067 | 生产收料页第二轮取证确认：任务执行事务、预计入清理、MES 消息与接口信息主链已取证；质量分流和异步消息可靠性保留后续验证。 | 文档建设工作 |
| 2026-07-16 | GAP-068 | 销售出库页第二轮取证确认：任务生成预计出、执行余额校验、出库事务与预计出清理主链已取证；冲销接口受规则开关控制，多业务子链和多端闭环保留后续验证。 | 文档建设工作 |
| 2026-07-16 | GAP-069 | 生产管理页第二轮取证确认：制品收货执行、预计入清理及回冲/接口/上架/QMS 条件动作已取证；独立子域与异步可靠性按问题登记后继续推进。 | 文档建设工作 |
| 2026-07-16 | GAP-019、GAP-020 | 库存页第二轮服务取证确认：批量删除执行物理删除，未见状态、关联或后端权限校验；单条删除 API 前后端参数错位。正文按可证实的当前行为描述，差距进入后续技术评估。 | 文档建设工作 |
| 2026-07-16 | GAP-021 | 库存页第二轮 RBAC 取证确认：前端导出权限标识存在，但对应后端 Controller 权限注解被注释；不将前端显示规则写成接口安全结论。 | 文档建设工作 |
| 2026-07-16 | GAP-022 | 库存事务与余额服务取证确认：事务服务先更新余额再写流水；余额详情事务页签仅按物料、包装、批次过滤，未覆盖完整余额粒度。正文保留实现顺序与追溯边界，差距进入后续验证。 | 文档建设工作 |
| 2026-07-16 | GAP-023、GAP-024 | BATCH-01 BOM 第二轮取证确认：当前单表父子关系模型与历史表头/表体草稿不符；业务键和编辑边界只在部分层实现，导入采用双层 VO。 | 文档建设工作 |
| 2026-07-16 | GAP-025 至 GAP-027 | BATCH-01 物料包装第二轮取证确认：物料包装的业务键、编辑锁定、显示字段、导入模型和权限标识在 DDL、服务、前端之间存在分层差异。正文以已证实当前行为为准，差距保留后续技术和测试验证。 | 文档建设工作 |

## 迁入的待确认事项

| 编号 | 主题 | 当前发现与后续核查 | 证据位置 | 状态 |
| --- | --- | --- | --- | --- |
| FTA-001 | 字段技术名真实性校正 | 原“字段技术名问题初扫清单”已迁入：历史字段可能混用显示名、前端键、接口字段和数据库列；按 P0 WMS/DBC、P1 主数据与系统能力、P2 低频模块分批建立证据并回填。特殊事实：`isRecycled` 正式语义为“回收件”；库存余额粒度和唯一索引风险已由 GAP-005 跟踪。 | 原字段初扫清单、字段真实性校正任务 | 进行中 |
| ISSUE-20260715-001 | 唯一业务索引缺失 | 与 GAP-005 合并处理；后续按实体补业务唯一键、导入校验和重复数据治理方案。 | 原问题记录清单 | 待确认 |
| ISSUE-20260715-002 | 历史字段英文名失真 | 与 GAP-008、FTA-001 合并处理。 | 原问题记录清单 | 进行中 |
| ISSUE-20260715-003 | 标签、条码、打印能力归属 | 与 GAP-015、WMS-LABEL 汇总项合并处理；当前按 WMS 入口说明，未来平台归属待架构确认。 | 原问题记录清单 | 待确认 |
| WMS-SP | 销售价格单 | `WMS-SP-001` 唯一性/有效期重叠；`002` 订单/发货/结算读取优先级；`003` 已使用后的编辑、停用和删除保护。 | WMS 销售价格单事实页 | 待确认 |
| WMS-MSTR | 产品类 | `WMS-MSTR-001` 物料引用后的编辑/删除保护；`002` 各账户在差异、报废和重估中的读取时机。 | WMS 产品类事实页 | 待确认 |
| WMS-SA | 科目账户配置 | `WMS-SA-001` 代码唯一性/导入策略；`002` ERP 成本中心、项目来源与维护责任；`003` 各业务场景选择规则。 | WMS 科目账户配置事实页 | 待确认 |
| WMS-LABEL | 标签与条码 | `WMS-LABEL-001` 五类标签入口映射；`002` 模板/规则版本与在用保护；`003` 补打和打印服务边界；`004` 平台能力迁移方案。 | WMS 标签与条码事实页 | 待确认 |
| WMS-RET | 采购退货 | `WMS-RET-001` 状态与动作前置；`002` 可退数量、上架/检验后控制与扣减时点；`003` 外部回写范围。 | WMS 采购退货事实页 | 待确认 |
| WMS-PUT | 采购上架 | `WMS-PUT-001` 接收/执行/批量/取消接收前置；`002` 部分/拆分/改库位/撤销；`003` 库位及质量校验；`004` PDA 与 Web 边界。 | WMS 采购上架事实页 | 待确认 |
| WMS-INV | 库存核心 | `P0-FIELD-004` 余额粒度与查询键不一致；`005` 预计入展示字段来源；`WMS-INV-001` 预计出生成/释放时点；`002` 人工事务维护权限和审计。另见 GAP-019 至 GAP-022。 | WMS 库存对象与库存粒度事实页 | 待确认 |
| WMS-REC | 采购收货 | `P0-FIELD-002` 供应商名称对象声明；`003` 收货记录状态；`006` 数量、采购数量和转换率。另见 GAP-001、GAP-002、GAP-009 至 GAP-011。 | WMS 采购收货事实页 | 待确认 |
| WMS-PROD | 生产用料闭环 | `WMS-PROD-001` 发料状态前置；`002` 预计出及少发/补料/退料处理；`003` MES 创建收料任务及时点；`004` 生产收料与产品收料映射；`005` 质量/隔离/报废影响；`006` PDA/线边边界。另见 GAP-066、GAP-067。 | WMS 生产用料闭环事实页 | 待确认 |
| MES-BASE | MES 基础建模待确认 | `001` 工位 SOP 字段全量与强制校验；`002` 线边库存事务与 WMS 余额一致性；`003` 开工点检 vs 设备点检职责切分样例；`004` 生产 BOM 与 DBC BOM 同步/孰为准。截图见 `字段证据/MES/P2截图与环境核对任务清单`。 | MES 基础建模正式页/证据，2026-07-17 | 待确认 |
| MES-ROUTE | MES 工艺管理待确认 | `001` 路线 status 字典与启停；`002` 节点扩展↔QMS 检验精确映射（另见 GAP-071）；`003` 路线—产品一头多物料选用规则。运行快照时机已按计划页写为「工单下发时」。 | MES 工艺管理正式页/证据，2026-07-17 | 待确认 |
| MES-PLAN | MES 计划管理待确认 | `001` 生产订单 status/systemStatus 全量文案；`002` 待点检强制与点检完成入口；`003` 已退料↔WMS 发料回冲（联 GAP-066）；`004` 历史工单管理/执行菜单与主链并存关系。 | MES 计划管理正式页/证据，2026-07-17 | 待确认 |
| MES-TRACE | MES 追溯管理待确认 | `001` 正向结果树深度联调截图；`002` 消耗树节点↔WMS 出库单据号；`003` 设备参数追溯覆盖的工序/设备类型。 | MES 追溯管理正式页/证据，2026-07-17 | 待确认 |
| MES-RPT | MES 报表统计待确认 | `001` 多套报工/完工菜单（如 reportRecord vs woWsreportLog）启用关系；`002` 打印记录是否独立菜单；`003` 导出字段以页面模板为准。 | MES 报表统计正式页/证据，2026-07-17 | 待确认 |
| MES-TERM | MES 终端操作待确认 | `001` `/mesprocess` 子页与按钮显隐截图；`002` 通用离线/断网续传未证实；`003` reportForWork Web 与工位作业 API 职责切分；`004` NG→QMS 映射见 GAP-071。 | MES 终端操作正式页/证据，2026-07-17 | 待确认 |
