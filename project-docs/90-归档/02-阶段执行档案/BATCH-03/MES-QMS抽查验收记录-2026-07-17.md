# MES + QMS 第二轮主文档抽查验收记录

> **【归档文件】** 过程记录，默认不必日常关注。日常入口见 [`00-需关注`](../../../00-需关注/README.md)。


> 日期：2026-07-17  
> 仓库：`MomProductDocument`  
> 范围：MES（`docs/06-MES-生产管理`）+ QMS（`docs/07-QMS-质量管理`）第二轮主文档与维护参考  
> 方式：只读抽查为主；发现明显表述歧义时小修并本地 `docs:` 提交（不 push）  
> 校验：`mkdocs build`（exit 0）

## 结论摘要

**可通过抽查验收，允许进入 EAM 主线。**

关键口径（到货确认触发、客户退回≠出货 OQC、库存事务归 WMS、MES NG→QMS 标 `GAP-071`）与证据一致；MES/QMS 交叉链接有效；`mkdocs.yml` 已挂齐两组维护参考；本轮文档未引入新断链（构建告警均来自 EAM/ANDON/SCP/PS 既有相对路径问题）。

---

## 抽查覆盖

| # | 项 | 结果 |
| --- | --- | --- |
| 1 | QMS `index.md` + 五组 index / 维护参考各扫一遍 | 通过 |
| 2 | 来料触发 = 到货确认（非已停用收货建检） | 通过 |
| 3 | 客户页 = 客户退回检验，非出货 OQC | 通过（见一般问题小修） |
| 4 | 库存事务归 WMS；MES NG→QMS 标 `GAP-071` | 通过（见一般问题小修） |
| 5 | MES 计划/终端/工艺交叉链接 | 通过（抽样 15 条相对路径全部命中） |
| 6 | 维护参考验收清单可执行性（抽 3 份） | 通过 |
| 7 | `mkdocs.yml` 挂载 QMS/MES 维护参考 | 通过（MES 6 + QMS 5） |
| 8 | `mkdocs build` 区分既有 vs 本轮断链 | 通过：MES/QMS 源文件 0 条 WARNING |

---

## 通过项

### QMS 口径

- **概述**明确：库存隔离/放行/退货/报废事务以 WMS 为准；生产报工以 MES 为准；学习顺序与齐套表与五组一致。
- **来料检验**：主触发写清为「WMS 到货确认待检行」；采购收货侧「创建检验申请」标明已停用；维护参考验收清单含「采购收货完成不再新增检验申请」。
- **客户检验**：正文与维护参考均写明菜单为**客户退回检验**，明确不是出货 OQC；触发来自 WMS 客户退货规则开关。
- **生产检验**：已证实 WMS 制品收货可建单；MES 报工 NG 自动建单标 **`GAP-071`（未证实）**；库存事务不在本页展开。
- **检验配置 / 质量评审**：方案「改库存状态」与评审结论均强调真正改库存以 WMS 为准；区分 QMS 检验评审 vs WMS「质量评审」菜单。

### MES 口径与交叉链接

- **计划 ↔ 工艺 ↔ 终端**：相互引用路径存在且可解析（含 WMS 生产管理/生产收料、DBC 工序/工艺路线）。
- **终端 / 计划**：报工 NG→QMS 映射均标 `GAP-071`，培训不写成固定自动建单。
- **vs WMS / QMS 边界**：库存事务归 WMS；过程检验细则回 QMS；WMS PDA ≠ MES 终端——三处口径一致。

### 维护参考可执行性（抽查）

| 页 | 清单是否可执行 | 说明 |
| --- | --- | --- |
| `来料检验-维护与查询参考` | 是 | 触发→申请→任务→发布回写→收货不再建检，步骤可对照环境操作 |
| `计划管理-维护与查询参考` | 是 | ERP 转换→拆单→下发快照→状态动作→并行组，可作联调清单 |
| `终端操作-维护与查询参考` | 是 | 登录/领取/报工/门闸/并行组 + 排除 WMS PDA，适合班组培训验收 |

另扫：`检验配置` / `生产检验` / `客户检验` / `工艺管理` 清单结构同类，无空壳或互相矛盾项。

### 导航与构建

- `mkdocs.yml`：MES 六组、QMS 五组均已挂「业务说明 + 维护与查询参考」。
- `mkdocs build`：成功；文档 WARNING **25** 条，按源文件归属：
  - `08-EAM` 8、`09-ANDON` 5、`10-SCP` 6、`11-PS` 6
  - **`06-MES` / `07-QMS` 作为源：0**
- 根因（既有，非本轮）：模块根 `index.md` 使用 `../../模块/...`，相对路径多上跳一层，MkDocs 归一化为 `../模块/...` 后找不到。属 EAM 主线应优先清理项，**不阻挡** MES/QMS 收尾声明。

---

## 问题项

### 严重

无。

### 一般

| ID | 位置 | 问题 | 建议修复 | 是否已随手修 |
| --- | --- | --- | --- | --- |
| G-1 | `07-QMS/.../04-客户检验/index.md` 阅读对象行 | 写「OQC/客退质量」，与正文「非出货 OQC」易混淆 | 改为客退质量表述 | **是**（已改为「客退质量、仓储退货协同」） |
| G-2 | `07-QMS/index.md` 齐套表「生产检验」说明 | 「MES 自动建单见 GAP-071」易被读成“已有自动建单” | 改为「未证实（GAP-071）」 | **是** |
| G-3 | EAM/ANDON/SCP/PS 根页跨模块链接 | `../../` 路径层级错误致 25 条 WARNING | EAM 主线统一改为 `../模块/...`（或绝对 docs 路径约定） | **否**（超出本轮 MES/QMS 范围） |
| G-4 | 各组「截图占位」仍多 | 不影响口径与培训主路径 | P2 实拍；不阻塞齐套 | **否**（已声明 P2） |

---

## 建议修复（后续）

1. **EAM 开线前**：批量修正 `08/09/10/11` 根页 `../../` → `../`，再跑 `mkdocs build` 清零既有断链告警。
2. **P2**：QMS/MES 截图实拍；`GAP-071` 联调闭合后回写生产检验/终端页。
3. 客户退回页对 WMS「销售出库/客户退货」可后续补精确相对链接（当前文字指向正确，非阻塞）。

---

## 是否已随手修

| 变更 | 说明 |
| --- | --- |
| `docs/07-QMS-质量管理/04-客户检验/index.md` | 阅读对象去掉易误导的「OQC/」前缀 |
| `docs/07-QMS-质量管理/index.md` | 齐套表 GAP-071 表述改为「未证实」 |

本地提交信息预期：`docs: QMS 抽查修正客户页与 GAP-071 表述`（若本回合入验收记录一并提交，见 git log）。

---

## 能否进入 EAM 主线

**可以。** MES + QMS 第二轮主文档抽查口径正确、导航齐套、本轮无新增断链；遗留为 EAM/ANDON 等既有链接层级与 P2 截图，不构成 MES/QMS 阻塞。
