# G3 抽样记录 + G4 防回归规则

> **【归档文件】** 过程记录，默认不必日常关注。日常入口见 [`00-需关注`](../../00-需关注/README.md)。


> 对应[分步任务清单](字段校正与中文路径迁移-分步任务清单.md) G3 / G4。
> 完成日期：2026-07-20。
> 用途：内部；不参与 MkDocs 发布。

## G3 抽样结果

| 模块/页 | 反译污染 | 证据一致性 | 正文动作 |
| --- | --- | --- | --- |
| DBC BOM | 未发现 | 与 BOM 证据页一致 | 补 `GAP-023`/`GAP-024` 短列表 |
| WMS 采购收货 | 未发现 | 三段对象与库存挂接口径正确 | 补 `GAP-009`/`GAP-010` |
| SCP 采购订单 | 未发现 | READY/PUBLISHED、累计量线索与证据一致 | 注明枚举/累计名为已取证写法 |
| QMS 来料检验 | 未发现 | 主触发=到货确认、收货建检停用 | **通过，无需改文** |
| 数采 采集点 | 未发现 | 字段为业务中文；仓外网关已声明 | 补 `GAP-072` 引用 |

**G3 结论：** 跨模块抽样未发现新的系统反译污染；本波以「补 GAP 编号短列表」收口边界。其余页继续问题驱动，不铺开全站重扫。

## G4 防回归规则（已固化）

已写入[实体说明页标准模板](../00-需关注/实体说明页标准模板.md) §9 / §10：

1. 正文技术名/枚举名必须能在字段证据页对上。
2. 禁止中文标签反译英文字段名。
3. 高风险 GAP 用编号短列表，不写成可操作功能。
4. 命名迁移须 G5 确认后再 G6。

### 每页改动后最小检查单（复制用）

- [ ] 未新增臆造英文技术名
- [ ] 必要技术名可在证据页检索到
- [ ] 限制段含相关 GAP 编号（若总账已有）
- [ ] 未写入 DDL/源码路径
- [ ] `mkdocs build` 通过

## 下一步

- **G5**：起草中文路径/文件名迁移方案映射表（**须用户确认**后才 G6）。
- 或 G7：处理未挂导航页与断链告警（可与 G5 并行规划）。

## 进度

| 日期 | 动作 |
| --- | --- |
| 2026-07-20 | G3 五页抽样完成；G4 写入标准模板 |
