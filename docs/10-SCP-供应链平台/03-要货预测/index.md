# 要货预测

> 适用基线：测试环境目标 / `dev` 分支 / 2026-07-15。
> 阅读对象：计划员、供应商门户；操作见[要货预测-维护与查询参考](要货预测-维护与查询参考.md)。

## 业务目的与适用范围

供应商说「按旧版本备货了」——多半是没盯紧当前活动版本。要货预测用于中长期需求发布与供应商对齐：计划方维护预测主单与明细（版本、窗口、计划量等），供应商侧有独立查看/确认页面；预测可为后续采购计划提供输入，接口类型枚举含预测主/明细，可登记对外同步。读完本页，你能判断该发新版本还是等旧版本被读取确认。

## 如何使用本组文档

预测发布后要经过供应商确认才汇总进计划，先看清自己在哪一环：

| 你的目的 | 建议阅读 |
| --- | --- |
| 想理解预测如何进计划 | 本页。 |
| 正在发布/确认预测 | [要货预测-维护与查询参考](要货预测-维护与查询参考.md)。 |
| 想汇总成计划 | [要货计划](../04-要货计划/index.md)。 |
| 想配预测周期 | [基础数据](../01-基础数据/index.md)。 |

## 使用前准备

| 需要确认什么 | 为什么重要 |
| --- | --- |
| 供应商、物料、周期窗口 | 预测范围。 |
| 版本号与是否当前活动版本 | 避免对错版本。 |
| 供应商是否已读 | 协同催办线索。 |

!!! example "📷 截图占位"
    计划方预测列表与供应商侧预测列表。

## 协同主线

```mermaid
flowchart LR
    A["计划员维护预测主/明细"] --> B["发布版本"]
    B --> C["供应商查看/确认"]
    C --> D["汇总进入采购计划（按业务配置）"]
```

## 主对象

| 对象 | 业务含义 |
| --- | --- |
| 预测主 | 单号、供应商、版本、发布/窗口时间、状态、计划总量/已发/已收汇总、计划员等。 |
| 预测明细 | 物料、计划量、交期、在途量、版本、是否已读、状态等。 |

### 关键字段业务角色

| 字段/配置点 | 在系统中的作用 | 关键行为要点 | 警惕什么 |
| --- | --- | --- | --- |
| 预测版本 / 是否活动 | 协同读哪一版 | 避免对错版本 | 旧版当现行 |
| 窗口时间 | 预测视界 | — | 窗口错 |
| 供应商是否已读 | 催办线索 | 已读标记 | 未读当已确认 |
| 明细计划量 | 后续计划输入 | 汇总规则 ❓（`GAP-017`） | 自动进计划未证实 |

### 选择器范围（骨架）

通例见[通用选择器过滤惯例](../../02-业务模型/12-通用选择器过滤惯例.md)。下表只写本页差异；门户可见与确认可改字段集见 `GAP-017`。

| 选择字段 | 选择对象 | 可选范围（当前可写） | 范围依赖 | 选不到时通常原因 |
| --- | --- | --- | --- | --- |
| 供应商 | SCP 供应商 | 可用；门户侧仅本供应商绑定范围 ❓（`GAP-017`） | 供应商用户 | 停用、未绑定 |
| 明细物料 | 物料 / 供应商物料 | 可用；可采购用途 ❓（`FSEM-001`） | 供应商、关系 | 停用、无关系 |
| 预测版本 / 活动版本 | 预测主单版本 | 协同宜读活动版本；旧版勿当现行 | 发布状态、窗口 | 未发布、非活动版本 |
| 要货周期（若引用） | 供应商周期配置 | 已维护周期 | 基础数据·供应商周期 | 未配周期 |
| 汇总进计划 | 已发布预测→采购计划 | 自动/手工规则 ❓（`GAP-017`） | 策略配置 | 未发布、策略未开 |

## 与计划 / 订单边界

预测本身不是计划，也不是订单，下表划清它只负责哪一段：

| 协同方 | 本页负责 | 不在本页展开 |
| --- | --- | --- |
| 要货计划 | 提供可汇总的已发布预测 | 计划接受/驳回与推送 WMS |
| 采购订单 | 可带主 PO 线索字段 | 订单状态机 |
| MES | — | 生产计划消费预测：**未证实** |

## 限制与待确认

- 供应商确认数量字段与驳回路径的完整按钮集：以测试环境为准（`GAP-017`）。
- 预测→计划的自动/手工规则：依赖策略配置，待联调（`GAP-017`）。
- 与 MES 是否消费本预测：**未证实**。
