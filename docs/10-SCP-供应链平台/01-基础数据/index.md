# 基础数据

> 适用基线：测试环境目标 / `dev` 分支 / 2026-07-15。
> 阅读对象：测试、实施、运维（主）；采购主数据维护（顺带）。操作细节见[基础数据-维护与查询参考](基础数据-维护与查询参考.md)。

## 业务目的与适用范围

订单建不了、价格算不对、供应商门户一片空白——往往不是流程坏了，而是 SCP 侧基础数据没配齐。本组为协同链（预测→计划→订单/发货→跟踪→结算）提供供应商、物料、价格、包装、仓/月台、单据开关、策略与供应商用户等配置底座；对象落在 SCP 自有表，**与 DBC 主数据并存**，培训时勿默认「改 SCP 等于改 DBC」。

读完本页，应能判断：

1. 协同字段该改 SCP 还是回 DBC；
2. 「选不到供应商/物料」是未建、停用，还是门户未绑定；
3. 本组只负责**可协同范围与入口启停**，不负责订单状态机或仓内库存。

旧页中虚构英文字段 ER、未经取证的信用等级规则等废弃。

## 如何使用本组文档

SCP 与 DBC 两边都有供应商/物料，先按目的挑对系统再动手：

| 你的目的 | 建议阅读 |
| --- | --- |
| 理解开协同前要配什么、与 DBC/WMS 边界 | 本页：准备 → 对象关系 → 写实示例 → 边界 → 建议验证点 |
| 维护供应商/物料/价格/开关或排障「选不到」 | [基础数据-维护与查询参考](基础数据-维护与查询参考.md) |
| 维护企业级供应商/物料权威 | DBC [供应商](../../04-DBC-主数据管理/02-供应商管理/01-供应商.md)、[物料](../../04-DBC-主数据管理/01-物料管理/01-物料基本信息.md) |
| 看下游如何消费这些数据 | [要货预测](../03-要货预测/index.md)、[采购订单](../02-采购订单/index.md)、[发货协同](../05-发货协同/index.md) |

## 使用前准备

| 需要确认什么 | 为什么重要 |
| --- | --- |
| 供应商编码、联系人与可用状态 | 订单、计划、发货按供应商过滤。 |
| 供应商物料与采购价格有效期 | 下单计价与可订物料范围。 |
| 包装规格/物料包装 | 发货包装数量与标签打印。 |
| 仓库、库区、库位、月台（SCP 侧） | 计划到货仓/月台与发货到仓。 |
| 单据开关、策略、要货周期 | 控制入口与计划节奏。 |
| 供应商用户关联 | 门户登录可见范围。 |

!!! example "📷 截图占位"
    SCP 基础数据菜单与供应商列表；脱敏。

## 对象关系

```mermaid
flowchart TB
    SUP["供应商"] --> SI["供应商物料"]
    SUP --> PRICE["采购价格"]
    ITEM["物料基本信息"] --> SI
    ITEM --> PKG["物料包装"]
    PU["包装规格/层级"] --> PKG
    WH["仓库"] --> AREA["库区"]
    AREA --> LG["库位组"] --> LOC["库位"]
    WH --> DOCK["月台"]
    SW["单据开关"] --> BIZ["各业务入口"]
    ST["策略"] --> PLAN["计划/发货策略"]
    CY["供应商周期"] --> FC["要货预测"]
    SU["供应商用户"] --> PORTAL["门户可见"]
```

| 对象 | 业务含义 |
| --- | --- |
| 供应商 | 协同对象编码、名称、联系、可用期。 |
| 供应商物料 | 供应商可供应物料、包装与结算相关属性。 |
| 物料基本信息 / 客户物料 | SCP 侧物料与客户物料关系。 |
| 物料包装 / 包装规格 / 层级 | 发货与标签用的包装定义。 |
| 采购价格 | 供应商+物料+币种价格及生效期。 |
| 仓库 / 库区 / 库位组 / 库位 / 月台 | 协同侧仓储定位（非 WMS 库存权威）。 |
| 单据开关 | 按单据编码控制启用。 |
| 策略 / 供应商发货策略 | 计划与发货策略配置。 |
| 要货预测周期（供应商周期） | 预测节奏。 |
| 供应商用户 | 用户与供应商绑定。 |
| 标签类型/规则/模板、包装打印、接口信息 | 标签与 SCP↔WMS 接口登记入口（能力分散在 SCP，长期可收敛平台）。 |
| ERP 成本中心（只读） | 注释标明来源为 WMS 同步，只读。 |

!!! example "写实示例：给定 → 期望"
    **给定：** 新建供应商 V-SCP-01（可用）；供应商物料挂物料 A；采购价格 币种 CNY、单价有效至下月末；绑定门户用户 U-V01；单据开关中「采购订单」为开。
    **期望：**

    1. 建采购订单/计划时可选到 V-SCP-01 与物料 A；价格在有效期内可带入计价线索。
    2. U-V01 登录门户只见本供应商协同数据（按环境权限验证）。
    3. 若仅在 DBC 建了同名供应商、SCP 侧未建或已停用，SCP 建单仍选不到——勿假定两侧自动一致（同步 ❓ `GAP-017`）。
    4. 关闭对应单据开关后，菜单/入口不可见；与「主数据未配」区分排查。

## 与 DBC / WMS / 其它分组边界

本组处在协同链最前端：只提供**可引用主数据与开关**，不推进预测/计划/订单状态。

| 协同方 | 本页负责 | 不在本页展开 |
| --- | --- | --- |
| DBC 供应商/物料/仓库 | SCP 侧副本维护 | 企业主数据导入与组织级治理 |
| WMS | 包装/条码等可推送；成本中心只读 | 库存余额与库内作业 |
| 要货预测 / 计划 | 周期、策略、仓/月台等前置配置 | 预测发版、计划接受状态机 |
| 采购订单 / 发货 | 提供主数据与开关 | 单据状态机、ASN 推送 |
| 发票结算 | 提供供应商用户与开票日历入口线索 | 对账金额规则 |

## 关键判断

| 判断点 | 应先确认什么 | 影响 |
| --- | --- | --- |
| 订单选不到供应商/物料 | SCP 侧是否维护且可用 | 无法建单 |
| 价格不对 | 采购价格生效期与币种 | 金额错误 |
| 发货包装异常 | 物料包装与包装规格 | ASN 数量/标签不准 |
| 菜单入口消失 | 单据开关是否关闭 | 业务入口不可见 |
| 供应商看不到数据 | 供应商用户是否绑定 | 门户空白 |

### 关键字段业务角色

| 字段/配置点 | 在系统中的作用 | 关键行为要点 | 警惕什么 |
| --- | --- | --- | --- |
| 供应商/物料（SCP 侧） | 协同可选范围 | **与 DBC 并存**；同步 ❓（`GAP-017`） | 改 SCP≠改 DBC |
| 采购价格生效期 | 计价 | 供应商+物料+币种+期间 | 过期价 |
| 仓/月台（SCP 侧） | 计划/发货定位 | 非 WMS 库存权威 | 当库存主数据 |
| 单据开关 | 入口启停 | 关则菜单/能力不可见 | 入口消失 |
| 供应商用户绑定 | 门户可见范围 | 用户↔供应商；门户投影 ❓（`GAP-017`） | 门户空白 |

### 选择器范围（骨架）

通例见[通用选择器过滤惯例](../../02-业务模型/12-通用选择器过滤惯例.md)。下表只写本页差异；SCP↔DBC 同步与门户权限投影见 `GAP-017` / `FSEM-006`。

| 选择字段 | 选择对象 | 可选范围（当前可写） | 范围依赖 | 选不到时通常原因 |
| --- | --- | --- | --- | --- |
| 供应商 | SCP 侧供应商 | 宜为可用；停用后是否一律过滤 ❓（`FSEM-002`） | — | 未建、已停用、权限外 |
| 物料（供应商物料/价格） | SCP 侧物料 | 可用；可采购用途过滤时点 ❓（`FSEM-001`） | 供应商物料关系 ❓（`GAP-045` 线索） | 停用、无关系、用途关闭 |
| 采购价格键 | 供应商+物料+币种 | 须在有效期内可引用 | 生效/失效期 | 过期、未建价、币种不符 |
| 仓库 / 月台 | SCP 侧仓储建模 | 有效仓；仓→月台级联；**非** WMS 库存权威 | 上游仓库 | 未建模、跨仓选错 |
| 包装规格 / 物料包装 | 包装主数据 | 可用包装；常先物料再包装 | 物料 | 未维护物料包装 |
| 供应商用户 | 用户↔供应商 | 已绑定且可用；门户可见范围 ❓（`GAP-017`） | 用户、供应商 | 未绑定、用户停用 |

## 建议验证点

- 至少一家可用供应商 + 一条供应商物料 + 一条有效期内采购价格，建单可选且计价线索正确。
- 仅 DBC 有、SCP 无（或已停用）时，SCP 建单选不到——与「改 SCP≠改 DBC」口径一致（同步规则 ❓ `GAP-017`）。
- 供应商用户绑定后门户可见本供应商数据；未绑定则门户空白。
- 关闭单据开关后对应入口不可见；重新打开后恢复。
- 仓/月台可选且级联正确；勿用 SCP 仓当 WMS 库存余额权威。

## 限制与待确认

- SCP 与 DBC 双侧供应商/物料/仓库是否双向同步：**未证实**（`GAP-017`），维护以现场实际入口为准。
- 供应商物料关系存在性/导入范围未全闭合（`GAP-045`）；门户数据权限投影待测（`GAP-017`）。
- 标签/打印能力在 SCP 与平台/WMS 并存，归属收敛见基础设施标签能力页。
- 精确状态过滤与选择器投影矩阵：见 `FSEM-006`。

!!! example "📝 示例数据占位"
    新建供应商 + 一条供应商物料 + 一条有效采购价格，供订单引用。
