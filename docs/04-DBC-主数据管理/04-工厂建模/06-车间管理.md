# 车间管理

## BATCH-01 标准占位

> 状态：首轮占位，待基于 DDL、DO、DTO、前端页面、后端服务和测试环境继续核验。下方历史字段和流程说明未完成字段真实性校正前，不作为接口、导入或测试依据。

车间属于 DBC 工厂建模主数据，通常不直接适用[申请、任务与记录模型](../../02-业务模型/01-申请任务记录模型.md)。本页后续重点补齐工厂/组织归属、车间状态、产线关联、MES/WMS 生产链路引用和权限取证。

| 主题 | 当前占位 | 后续取证 |
| --- | --- | --- |
| 字段真实性 | 保留历史草稿，新增字段事实需待核验。 | 从 DBC DDL、DO、DTO、VO、前端配置校正真实字段名。 |
| 新增约束 | 待补车间编码、名称、工厂/组织、状态、负责人等规则。 | 前端表单、选择器/字典、后端校验。 |
| 编辑限制 | 待补被产线、工位、MES 工单、QMS 检验、ANDON 异常引用后的可编辑范围。 | 后端更新服务、引用检查、测试环境。 |
| 导入规则 | 待补模板字段、系统填充字段、车间唯一性和错误处理。 | Excel VO、导入服务、导入模板。 |
| 列表与详情 | 待补默认列、查询条件、详情分组和快速跳转。 | 前端列表配置、详情组件。 |
| 关联影响 | 待补与生产线、工位、工序、工单、质量、异常的关系。 | 源码调用、菜单、业务确认。 |
| 权限与日志 | 按 [RBAC 与动作权限取证模板](../../15-版本路线图/RBAC与动作权限取证模板.md) 逐项补。 | 菜单权限、按钮权限、接口权限、数据范围。 |

## 概述

车间是生产制造的物理组织单元，定义生产工单执行的具体场所。

## 字段说明

| 字段名 | 中文名 | 类型 | 约束 | 影响业务 | 备注 |
|--------|--------|------|------|----------|------|
| workshopCode | 车间编码 | VARCHAR(50) | 必填、唯一 | MES工单派工（工单需指定执行车间）、报工归属（报工记录归属车间） | 车间的唯一身份标识，用于标识和引用具体车间，系统主键 |
| workshopName | 车间名称 | VARCHAR(200) | 必填 | MES报工显示（展示车间名称）、报表（按车间统计产量） | 车间的显示名称，如"沈阳工厂-装配车间A" |
| workshopType | 车间类型 | ENUM | 字典项：装配/加工/检测/包装 | （待补充） | 车间的主要生产类型 |
| manager | 车间主任 | VARCHAR(50) | 非必填，来源表：用户 | 职责归属（车间异常问题找对应负责人）、审批流（车间级审批找车间主任） | 车间负责人账号，用于车间级异常问题联络和审批路由 |
| parentWorkshop | 上级车间 | VARCHAR(50) | 非必填，来源表：车间 | 车间层级（支持多级组织结构） | 上级车间的编码，用于构建车间层级关系 |
| status | 状态 | ENUM | 字典项：启用/禁用 | MES工单派工（禁用车间不可派工）、报工（禁用车间的报工记录仍可查） | 车间启用/禁用状态，禁用后不可新建派工到此车间，但历史报工数据保留 |

## 关联关系

```
车间（1）←→（N）生产线
车间（1）←→（N）工位
车间（1）←→（N）设备
车间 → MES工单
```

## 字段约束说明

| 约束类型 | 说明 |
|----------|------|
| 字典项 | workshopType（装配/加工/检测/包装）、status（启用/禁用） |
| 来源表 | manager→用户表，parentWorkshop→车间表（自关联） |
