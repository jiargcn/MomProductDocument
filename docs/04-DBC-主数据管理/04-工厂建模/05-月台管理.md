# 月台管理

## BATCH-01 标准占位

> 状态：首轮占位，待基于 DDL、DO、DTO、前端页面、后端服务和测试环境继续核验。下方历史字段和流程说明未完成字段真实性校正前，不作为接口、导入或测试依据。

月台属于 DBC 工厂/仓储建模主数据，通常不直接适用[申请、任务与记录模型](../../02-业务模型/01-申请任务记录模型.md)。本页后续重点补齐仓库、月台、收发货能力、装卸规则、导入模板、采购收货/销售出库引用和权限取证。

| 主题 | 当前占位 | 后续取证 |
| --- | --- | --- |
| 字段真实性 | 保留历史草稿，新增字段事实需待核验。 | 从 DBC DDL、DO、DTO、VO、前端配置校正真实字段名。 |
| 新增约束 | 待补仓库、月台编码、月台类型、状态、装卸能力等规则。 | 前端表单、选择器/字典、后端校验。 |
| 编辑限制 | 待补被采购收货、销售出库、发运计划、客户月台引用后的可编辑范围。 | 后端更新服务、引用检查、测试环境。 |
| 导入规则 | 待补模板字段、系统填充字段、仓库+月台唯一性和错误处理。 | Excel VO、导入服务、导入模板。 |
| 列表与详情 | 待补默认列、查询条件、详情分组和快速跳转。 | 前端列表配置、详情组件。 |
| 关联影响 | 待补与仓库、客户月台、采购到货、销售发货、承运商的关系。 | 源码调用、菜单、业务确认。 |
| 权限与日志 | 按 [RBAC 与动作权限取证模板](../../15-版本路线图/RBAC与动作权限取证模板.md) 逐项补。 | 菜单权限、按钮权限、接口权限、数据范围。 |

## 概述

月台管理定义仓库的收货/发货月台信息，用于物流调度和到货预排。

## 字段说明

| 字段名 | 中文名 | 类型 | 约束 | 影响业务 | 备注 |
|--------|--------|------|------|----------|------|
| platformCode | 月台代码 | VARCHAR(50) | 必填 | 物流调度（确定月台地址） | 月台的唯一标识 |
| platformName | 月台名称 | VARCHAR(200) | 必填 | 月台显示 | 月台的显示名称 |
| warehouseCode | 仓库代码 | VARCHAR(50) | 必填 | 月台归属（确定属于哪个仓库） | 所属仓库 |
| address | 地址 | VARCHAR(500) | 非必填 | 发货单据打印（打印发货地址） | 月台地址 |
| contactPerson | 联系人 | VARCHAR(100) | 非必填 | 物流跟踪联络 | 联系人姓名 |
| contactPhone | 联系电话 | VARCHAR(50) | 非必填 | 物流跟踪联络 | 联系电话 |
| isEnabled | 是否可用 | BOOLEAN | 默认是 | 月台分配（不可用时不可选） | 是否可用 |
| effectiveDate | 生效时间 | DATE | 非必填 | 月台启用时间 | 生效时间 |
| invalidDate | 失效时间 | DATE | 非必填 | 月台停用时间 | 失效时间 |
| remark | 备注 | VARCHAR(500) | 非必填 | | 补充说明 |

## 关联关系

```
仓库（1）←→（N）月台
月台 → 发货计划（到货地址）
```

## 字段约束说明

| 约束类型 | 说明 |
|----------|------|
| 唯一约束 | platformCode 唯一 |
