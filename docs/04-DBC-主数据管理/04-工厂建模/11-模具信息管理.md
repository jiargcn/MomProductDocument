# 模具信息管理

> 当前基线：`dev`（测试环境目标基线），菜单、源码与 DDL 取证日期：2026-07-16。
> 页面定位：本页保留为 DBC 历史菜单/文档入口的校正页，不作为已确认的 DBC 模具主数据功能说明。当前模具/工装台账、保养、巡检、维修等运行入口应以 [EAM 工装管理](../../08-EAM-设备管理/04-工装管理/index.md) 为准。

## 当前实现摘要

菜单基线同时存在“模具管理”父菜单以及 DBC 路由 `dbc/pattern/index`（模具信息）、`dbc/patternType/index`（模具类型）；但当前提供的 `ui` 前端源码和 `win-module-dbc` 后端源码中未发现对应 `pattern`/`patternType` 实现，也未在 `base.sql` 找到可对应的 `basic_mold` 或 `basic_pattern` 持久化表。

相反，当前菜单存在 EAM 工装台账路由 `dbc/toolAccounts/index`，EAM 后端存在 `/eam/item-accounts` 台账接口，EAM 移动端“模具”页面调用 `/eam/mold-accounts/*` 路径，但提供的后端 Controller 路径与其并不一致。`eam.sql` 可证实工装台账表为 `basic_tool_accounts`，并存在工装入库、出库、验收、变更、改制等记录表。

因此，原页面中 `moldCode`、`moldType`、设计寿命、使用次数、状态枚举、自动借还/点检规则等内容均缺少当前 DBC 实现证据，已不再作为培训、接口、导入或测试依据。详见产品差距总账 GAP-056。

## 当前可证实的边界

| 主题 | 可证实事实 | 不得据此推断 |
| --- | --- | --- |
| 历史 DBC 菜单 | `menu.csv` 中仍有模具类型与模具信息两个 DBC 路由。 | 路由代表当前前后端功能可用，或字段/状态仍按历史草稿实现。 |
| EAM 工装台账 | `eam.sql` 存在 `basic_tool_accounts`；该表含代码、名称、规格、类型、库存库位、使用部门/负责人、状态、采购/生产/验收信息、寿命/使用次数、区域/车间/产线/工序/工位等字段。 | 这些字段都已由同一个 Web 页面开放维护，或所有字段都对“模具”对象生效。 |
| EAM 流程记录 | DDL 可见工装入库、出库、验收、变更、改制及工装与物料关系等对象。 | 当前状态机、审批前置条件、寿命计算和跨模块回写已经按原历史文档运行。 |
| 移动端模具查询 | EAM 移动端存在模具列表，按名称搜索并展示名称、编号、厂区和图片。 | `/eam/mold-accounts/*` 已与当前提供的服务端路由完成联调。 |

## 数据、编辑与导入规则

当前无法从 DBC 源码确认模具信息页的真实表、DO、VO、服务、导入类、选择器或权限配置，故本页不输出字段表、可编辑字段、导入模板或状态规则。

在后续 EAM 工装台账专项中，应分别取证并补齐：

1. `basic_tool_accounts` 与实际 UI/VO 的字段映射，特别是“工装”“模具”“备件台账”三者的对象边界；
2. 代码的业务唯一性、创建/更新/删除引用保护、状态迁移和寿命/使用次数的写入来源；
3. 工装入库、出库、验收、变更、改制、保养、巡检、维修与申请/任务/记录模型的挂接；
4. PC、PDA、EAM 移动端的接口路径、权限与数据一致性；
5. 人工导入字段与系统回填字段的边界。

## 列表、详情与快速跳转建议

DBC 历史模具页面的列表与详情布局不再作为设计依据。EAM 专项落地时，建议按以下分组规划：

| 分组 | 内容 | 建议跳转 |
| --- | --- | --- |
| 台账身份 | 代码、名称、工装/模具类型、规格、状态、可用状态 | 工装台账、模具类型（待确认当前入口） |
| 使用与位置 | 厂区、车间、产线、工序、工位、库位、使用部门/负责人 | 对应主数据与位置查询 |
| 寿命与维护 | 使用次数、寿命、保养、巡检、维修、变更、改制 | EAM 工装维护记录/任务（待取证） |
| 业务流转 | 到货验收、入库、出库、签收/归还、申请与审批 | 各工装记录及申请任务查询（待取证） |
| 审计 | 采购/生产/验收信息、附件、操作日志 | 附件、操作日志（待取证） |

## 证据与待验证项

- 菜单：`reference/menu.csv` 的 `patternType`、`pattern`、`tool-accounts`、工装记录及 PDA 工装菜单。
- DDL：`reference/dbddl/eam.sql` 的 `basic_tool_accounts`、`record_tool_*`、`relation_tool_mod` 等表；`base.sql` 未发现对应 DBC 模具表。
- 后端：`win-module-eam` 的 `ItemAccountsController` 等；当前 Controller 标注为备件台账，需继续核对其是否承载工装/模具。
- 前端：`eam/src/pages/mold/index.vue`、`eam/src/api/mold.ts`；当前路径与提供的 EAM Controller 映射待联调验证。
- 待验证：DBC 历史路由是否已废弃、是否有未纳入工作区的前端/后端仓库、模具与工装台账的正式模型及迁移归属、接口与 RBAC、终端状态流转。
