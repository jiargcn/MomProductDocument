# 潜伏式 AGV 点位配置

> 当前基线：`dev`（测试环境目标基线），源码与 DDL 取证日期：2026-07-16。
> 页面定位：维护潜伏式 AGV 的配置点位、基础点位与类型/优先级映射。它是独立于[点位管理](12-点位管理.md)的配置对象，不等同于库位—AGV 点位映射，也不直接适用[申请、任务与记录模型](../../02-业务模型/01-申请任务记录模型.md)。

## 当前实现摘要

持久化表为 `basic_latent_agv_point`。当前模型维护 `pointCodeConfig`（配置点位）、`pointCode`（点位代码）、`type`、`priority`、有效期和备注；历史草稿中的名称、库位、坐标、朝向、可用状态不是当前对象字段。

服务和导入的既有记录查找使用 `pointCode + pointCodeConfig + type`，但新增/编辑的查重方法把传入的配置点位条件写到旧字段 `port_code`，而当前表使用非空 `point_code_config`，`port_code` 为可空旧列。DDL 亦未设复合唯一约束。详见[产品差距总账](../../15-版本路线图/03-问题与待确认/产品差距总账.md) GAP-058。

## 字段与新增约束

| 技术字段 | 中文名 | DDL 类型/约束 | 当前页面规则 | 当前服务证据 |
| --- | --- | --- | --- | --- |
| `pointCodeConfig` | 配置点位 | `varchar(64)`，非空 | 必填；点位选择器，数据源为通用点位分页接口，筛选 `available=true`。 | 导入必填；新增/编辑查重错误地使用 `port_code` 条件。 |
| `type` | 类型 | `varchar(64)`，非空 | `LATENT_AGV_TYPE` 字典，必填。 | 导入必填；作为既有记录查找和查重条件之一。 |
| `pointCode` | 点位代码 | `varchar(64)`，可空 | 必填；同样从通用点位选择器选择，筛选 `available=true`。 | 导入必填；作为既有记录查找和查重条件之一。 |
| `priority` | 优先级 | `varchar(64)`，可空 | `PRIORITY` 字典，必填。 | 导入必填。 |
| `activeTime` / `expireTime` | 生效/失效时间 | `timestamp(6)`，可空 | 日期时间选择器。 | 将 `1970` 年时间归空；未见区间先后校验。 |
| `remark` | 备注 | `text`，可空 | 普通输入字段。 | 无额外校验。 |
| `portCode` / `portType` | 料口/料口类型 | `portCode` 仍为 DDL 可空列；`portType` 现不在 DDL 主表字段中。 | 页面与导入字段已注释。 | 服务查重仍使用 `port_code`，料口关联查询已注释。 |

## 编辑、删除与权限

| 动作 | 当前可证实行为 | 使用口径 |
| --- | --- | --- |
| 编辑 | Web 未锁定配置点位、点位代码或类型；服务校验存在后执行查重并整体更新。 | 不应在 AGV 路径或任务使用后直接变更关键映射，当前未见引用拦截。 |
| 删除 | 服务校验存在后删除并写趋势记录；未见 AGV 任务、路径或调度引用检查。 | 在用配置不应视为可安全删除。 |
| 权限 | 当前前端页面/按钮权限及后端强制鉴权尚未完成专项取证。 | 不以接口存在推断授权已落实。 |

## 导入规则

`LatentAgvPointImportExcelVO` 模板字段顺序为：配置点位、类型、点位代码、优先级、生效时间、失效时间、备注。

| 校验/模式 | 当前规则 |
| --- | --- |
| 必填 | 配置点位、类型、点位代码、优先级。 |
| 字典 | 类型使用 `LATENT_AGV_TYPE`，优先级使用 `PRIORITY`。 |
| 既有记录 | 按 `pointCode + pointCodeConfig + type` 查询；不存在且非覆盖模式时插入，存在且非仅新增模式时更新。 |
| 更新内容 | 更新类型、优先级、生效/失效时间、备注；配置点位和点位代码作为匹配键保留。 |
| 关联验证 | 未见对两个点位代码是否存在、可用或彼此关系正确的有效校验；料口验证已注释。 |
| 错误处理 | 返回逐行错误；当前实现未见“有错误且未选择部分更新时整批回滚”。 |

模板不含料口、料口类型、审计字段和系统内部 ID。应先确认“配置点位”与“点位代码”是否允许指向同一通用点位，以及正式业务键后再调整模板与唯一索引。

## 列表、详情与快速跳转

当前默认列顺序为：配置点位、类型、点位代码、优先级、生效时间、失效时间、备注、审计列、操作。当前筛选为配置点位、类型、点位代码、优先级。

建议主列表保持：配置点位、点位代码、类型、优先级、有效期；将审计字段置于补充列。

当前采用通用详情组件，未见专用关联页签。后续建议：

| 分组 | 内容 | 建议跳转 |
| --- | --- | --- |
| 映射关系 | 配置点位、点位代码、类型、优先级 | 通用点位详情 |
| 调度影响 | AGV 型号、路径、任务、搬运配置 | AGV 管理与 WMS 搬运任务（待取证） |
| 生命周期与审计 | 有效期、备注、创建/更新、趋势记录 | 操作趋势/日志（待公共能力取证） |

## 关联关系

```
通用点位（basic_point）
  ├─ 配置点位（pointCodeConfig）
  └─ 点位代码（pointCode）
          ↓
潜伏式 AGV 点位配置（basic_latent_agv_point）
          ↓
AGV 路径、任务与调度引用（待取证）
```

## 证据与待验证项

- DDL：`reference/dbddl/base.sql` 的 `basic_latent_agv_point`；仅主键及普通索引，未见业务复合唯一约束。
- 后端：`LatentAgvPointServiceImpl`、`latentAgvPointController`、`LatentAgvPointImportExcelVO`。
- 前端：`ui/src/views/dbc/agvPoint/agvPoint.data.ts`。
- 待测试验证：查重条件是否应由 `port_code` 更正为 `point_code_config`、双点位映射规则、编辑/删除引用拦截、导入模式和错误回滚、AGV 接口/RBAC。
