# 库位组管理

## BATCH-01 标准占位

> 状态：首轮占位，待基于 DDL、DO、DTO、前端页面、后端服务和测试环境继续核验。下方历史字段和流程说明未完成字段真实性校正前，不作为接口、导入或测试依据。

库位组属于 DBC 工厂/仓储建模主数据，通常不直接适用[申请、任务与记录模型](../../02-业务模型/01-申请任务记录模型.md)。本页后续重点补齐库位组编码、仓库/库区关系、库位归集规则、导入模板、WMS 查询/权限引用和权限取证。

| 主题 | 当前占位 | 后续取证 |
| --- | --- | --- |
| 字段真实性 | 保留历史草稿，新增字段事实需待核验。 | 从 DBC DDL、DO、DTO、VO、前端配置校正真实字段名。 |
| 新增约束 | 待补库位组编码、名称、仓库/库区、状态等规则。 | 前端表单、选择器/字典、后端校验。 |
| 编辑限制 | 待补被库存余额、库位权限、上架/发料策略引用后的可编辑范围。 | 后端更新服务、引用检查、测试环境。 |
| 导入规则 | 待补模板字段、系统填充字段、唯一性和成员关系校验。 | Excel VO、导入服务、导入模板。 |
| 列表与详情 | 待补默认列、查询条件、详情分组和快速跳转。 | 前端列表配置、详情组件。 |
| 关联影响 | 待补与库位、库存余额、仓库权限、WMS 查询过滤的关系。 | 源码调用、菜单、业务确认。 |
| 权限与日志 | 按 [RBAC 与动作权限取证模板](../../15-版本路线图/RBAC与动作权限取证模板.md) 逐项补。 | 菜单权限、按钮权限、接口权限、数据范围。 |

## 概述

库位组管理定义库位的分组信息，用于库位分类管理。

## 字段说明

| 字段名 | 中文名 | 类型 | 约束 | 影响业务 | 备注 |
|--------|--------|------|------|----------|------|
| groupCode | 库位组代码 | VARCHAR(50) | 必填 | 库位归属（库位归属于哪个组） | 库位组的唯一标识 |
| groupName | 库位组名称 | VARCHAR(200) | 必填 | 库位组显示 | 库位组的显示名称 |
| warehouseCode | 仓库代码 | VARCHAR(50) | 非必填 | 库位组归属 | 所属仓库 |
| isEnabled | 是否可用 | BOOLEAN | 默认是 | 库位分配（不可用时不出现在推荐列表） | 是否可用 |
| effectiveDate | 生效时间 | DATE | 非必填 | 库位组启用时间 | 生效时间 |
| invalidDate | 失效时间 | DATE | 非必填 | 库位组停用时间 | 失效时间 |
| remark | 备注 | VARCHAR(500) | 非必填 | | 补充说明 |

## 关联关系

```
仓库（1）←→（N）库位组
库位组（1）←→（N）库位
```

## 字段约束说明

| 约束类型 | 说明 |
|----------|------|
| 唯一约束 | groupCode 唯一 |
