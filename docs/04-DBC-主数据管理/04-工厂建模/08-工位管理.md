# 工位管理

## BATCH-01 标准占位

> 状态：首轮占位，待基于 DDL、DO、DTO、前端页面、后端服务和测试环境继续核验。下方历史字段和流程说明未完成字段真实性校正前，不作为接口、导入或测试依据。

工位属于 DBC 工厂建模主数据，是 MES 现场执行、线边端操作、QMS 检验和 ANDON 异常定位的重要维度，通常不直接适用[申请、任务与记录模型](../../02-业务模型/01-申请任务记录模型.md)。

| 主题 | 当前占位 | 后续取证 |
| --- | --- | --- |
| 字段真实性 | 保留历史草稿，新增字段事实需待核验。 | 从 DBC DDL、DO、DTO、VO、前端配置校正真实字段名。 |
| 新增约束 | 待补工位编码、名称、所属产线/车间、工位类型、状态等规则。 | 前端表单、选择器/字典、后端校验。 |
| 编辑限制 | 待补被工艺路线、工单、线边端、设备、质量、ANDON 引用后的可编辑范围。 | 后端更新服务、引用检查、测试环境。 |
| 导入规则 | 待补模板字段、系统填充字段、工位唯一性和错误处理。 | Excel VO、导入服务、导入模板。 |
| 列表与详情 | 待补默认列、查询条件、详情分组和快速跳转。 | 前端列表配置、详情组件。 |
| 关联影响 | 待补与产线、工序、设备、工装、点位、现场终端的关系。 | 源码调用、菜单、业务确认。 |
| 权限与日志 | 按 [RBAC 与动作权限取证模板](../../15-版本路线图/RBAC与动作权限取证模板.md) 逐项补。 | 菜单权限、按钮权限、接口权限、数据范围。 |

## 概述

工位是生产线上的具体作业点，定义工人或设备执行工序的具体位置。

## 字段说明

| 字段名 | 中文名 | 类型 | 约束 | 影响业务 | 备注 |
|--------|--------|------|------|----------|------|
| stationCode | 工位编码 | VARCHAR(50) | 必填、唯一 | MES报工（报工时选择具体工位）、工序流转（物料在工位间转移） | 工位的唯一身份标识，用于标识具体工位，系统主键 |
| stationName | 工位名称 | VARCHAR(200) | 必填 | MES显示（工位名称展示） | 工位的显示名称，如"装配工位A1-01" |
| productionLineCode | 所属生产线 | VARCHAR(50) | 必填，来源表：生产线 | 组织归属（明确工位属于哪条产线）、报工归属（报工记录归属产线） | 工位所属生产线的编码 |
| stationType | 工位类型 | ENUM | 字典项：加工/装配/检测/包装 | 工序分配（不同类型工位分配对应的工序） | 工位的主要作业类型，影响可分配的工序类型 |
| deviceCode | 绑定设备 | VARCHAR(50) | 非必填，来源表：设备台账 | （待补充） | 工位绑定的设备编码，绑定后此工位开工前需先确认设备点检完成状态 |
| workerCapacity | 人员配置数 | INT | 默认1 | （待补充） | 同一工位可同时容纳的作业人员数量 |
| status | 状态 | ENUM | 字典项：启用/禁用 | MES派工（禁用工位不可派工）、报工（禁用工位的报工记录仍可查） | 工位的启用/禁用状态，禁用后不可派工 |

## 关联关系

```
生产线（1）←→（N）工位
工位 → 设备（绑定关系）
工位 → MES报工记录
工位 → 工序配置（工位-工序关系）
```

## 字段约束说明

| 约束类型 | 说明 |
|----------|------|
| 字典项 | stationType（加工/装配/检测/包装）、status（启用/禁用） |
| 来源表 | productionLineCode→生产线表，deviceCode→设备台账表 |
