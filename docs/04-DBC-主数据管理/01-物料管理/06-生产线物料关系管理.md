# 生产线物料关系管理

## BATCH-01 标准占位

> 状态：首轮占位，待基于 DDL、DO、DTO、前端页面、后端服务和测试环境继续核验。下方历史字段和流程说明未完成字段真实性校正前，不作为接口、导入或测试依据。

生产线物料关系属于 DBC 主数据/配置类页面，通常不直接适用[申请、任务与记录模型](../../02-业务模型/01-申请任务记录模型.md)。本页后续重点补齐产线、工位、物料选择器，投料/收料引用，导入模板和权限取证。

| 主题 | 当前占位 | 后续取证 |
| --- | --- | --- |
| 字段真实性 | 保留历史草稿，新增字段事实需待核验。 | 从 DBC DDL、DO、DTO、VO、前端配置校正真实字段名。 |
| 新增约束 | 待补产线、工位、物料、用量、状态、生效时间等规则。 | 前端表单、选择器筛选、后端校验。 |
| 编辑限制 | 待补被 MES 工单、WMS 发料、线边端操作引用后的可编辑范围。 | 后端更新服务、引用检查、测试环境。 |
| 导入规则 | 待补模板字段、系统填充字段、产线物料唯一性和错误处理。 | Excel VO、导入服务、导入模板。 |
| 列表与详情 | 待补默认列、查询条件、详情分组和快速跳转。 | 前端列表配置、详情组件。 |
| 关联影响 | 待补与物料、产线、工位、发料、投料、MES 执行的关系。 | 源码调用、菜单、业务确认。 |
| 权限与日志 | 按 [RBAC 与动作权限取证模板](../../15-版本路线图/RBAC与动作权限取证模板.md) 逐项补。 | 菜单权限、按钮权限、接口权限、操作日志。 |

## 概述

生产线物料关系定义物料与生产线的关联，用于MES工单展开和产线库存管理。

## 字段说明

| 字段名 | 中文名 | 类型 | 约束 | 影响业务 | 备注 |
|--------|--------|------|------|----------|------|
| materialCode | 物料代码 | VARCHAR(50) | 必填 | 物料归属 | 关联物料 |
| productionLineCode | 生产线代码 | VARCHAR(50) | 必填 | 产线归属 | 关联生产线 |
| isPrimary | 是否主产线 | BOOLEAN | 默认否 | 工单展开（主产线优先） | 是否主产线 |
| defaultQty | 默认用量 | DECIMAL(18,4) | 非必填 | 工单展开（默认带出用量） | 默认用量 |
| isEnabled | 是否可用 | BOOLEAN | 默认是 | 关系可用性 | 是否可用 |
| effectiveDate | 生效时间 | DATE | 非必填 | 关系启用时间 | 生效时间 |
| invalidDate | 失效时间 | DATE | 非必填 | 关系停用时间 | 失效时间 |
| remark | 备注 | VARCHAR(500) | 非必填 | | 补充说明 |

## 关联关系

```
物料（1）←→（N）生产线物料关系 → 生产线
```

## 字段约束说明

| 约束类型 | 说明 |
|----------|------|
| 唯一约束 | materialCode + productionLineCode 唯一 |
| 联动影响 | isPrimary=true 时，同物料其他主产线自动取消 |
