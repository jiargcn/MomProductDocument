# 班次管理

## BATCH-01 标准占位

> 状态：首轮占位，待基于 DDL、DO、DTO、前端页面、后端服务和测试环境继续核验。下方历史字段和流程说明未完成字段真实性校正前，不作为接口、导入或测试依据。

班次属于 DBC 策略/排班类主数据，通常不直接适用[申请、任务与记录模型](../../02-业务模型/01-申请任务记录模型.md)。本页后续重点补齐班次时间、跨天规则、生效范围、MES 排班/报工引用和权限取证。

| 主题 | 当前占位 | 后续取证 |
| --- | --- | --- |
| 字段真实性 | 保留历史草稿，新增字段事实需待核验。 | 从 DBC/MES DDL、DO、DTO、VO、前端配置校正真实字段名。 |
| 新增约束 | 待补班次编码、名称、开始/结束时间、跨天、生效日期、状态等规则。 | 前端表单、时间控件、后端校验。 |
| 编辑限制 | 待补被班组、排班、报工、考勤引用后的可编辑范围。 | 后端更新服务、引用检查、测试环境。 |
| 导入规则 | 待补模板字段、系统填充字段、时间区间和跨天校验。 | Excel VO、导入服务、导入模板。 |
| 列表与详情 | 待补默认列、查询条件、详情分组和快速跳转。 | 前端列表配置、详情组件。 |
| 关联影响 | 待补与班组、MES 排班、报工、考勤统计的关系。 | 源码调用、菜单、业务确认。 |
| 权限与日志 | 按 [RBAC 与动作权限取证模板](../../15-版本路线图/02-标准与方法/RBAC与动作权限取证模板.md) 逐项补。 | 菜单权限、按钮权限、接口权限、操作日志。 |

## 概述

班次管理定义生产班次的时间安排，用于MES排班和考勤统计。

## 字段说明

| 字段名 | 中文名 | 类型 | 约束 | 影响业务 | 备注 |
|--------|--------|------|------|----------|------|
| shiftCode | 班次代码 | VARCHAR(50) | 必填 | MES排班（排班时选择班次） | 班次的唯一标识 |
| shiftName | 班次名称 | VARCHAR(200) | 非必填 | MES显示（显示班次名称） | 班次的显示名称 |
| startTime | 开始时间 | TIME | 必填 | 考勤统计（上班刷卡时间比对） | 班次上班时间 |
| endTime | 结束时间 | TIME | 必填 | 考勤统计（下班刷卡时间比对） | 班次下班时间 |
| isCrossDay | 是否跨天 | BOOLEAN | 默认否 | 跨天判断（22:00-06:00型跨天班次） | 是否跨越午夜0点 |
| isEnabled | 是否可用 | BOOLEAN | 默认是 | 排班可用性 | 是否可用 |
| effectiveDate | 生效时间 | DATE | 非必填 | 班次启用时间 | 生效时间 |
| invalidDate | 失效时间 | DATE | 非必填 | 班次停用时间 | 失效时间 |
| remark | 备注 | VARCHAR(500) | 非必填 | | 补充说明 |

## 关联关系

```
班次 → 班组（班次分配）
班次 → 考勤记录
```

## 字段约束说明

| 约束类型 | 说明 |
|----------|------|
| 唯一约束 | shiftCode 唯一 |
| 联动影响 | isCrossDay=true 时，跨天00:00后归属次日班次 |
