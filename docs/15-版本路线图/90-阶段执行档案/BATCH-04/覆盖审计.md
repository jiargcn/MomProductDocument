# BATCH-04 覆盖审计

> 状态：进行中。
> 范围：SCP + PS + API。
> 目的：记录供应链协同、排程管理和接口参考的首轮占位覆盖情况，确保业务文档最终能说明外部协同、排程输入输出和 API 挂接关系。

## 1. 当前结论

BATCH-04 已具备首轮结构：SCP 和 PS 页面已有较多业务内容，API 参考页已补充接口取证模板、第三方挂接占位和后续任务。

但当前不能认为字段、状态、算法参数、接口路径和第三方同步规则已确认。后续必须逐项取证：

- SCP 订单、预测、计划、发货、跟踪、结算的字段、状态和供应商权限；
- PS 排程输入、约束、算法参数、执行记录、版本和输出结果；
- API 的真实路径、方法、权限、请求/响应字段、错误码、幂等和日志；
- ERP/SAP/SCP/WMS/MES/报表/排程引擎之间的接口方向、触发时机、失败补偿和对账。

## 2. 覆盖分类

| 分类 | 页面范围 | 当前状态 |
| --- | --- | --- |
| SCP | 首页、基础数据、采购订单、要货预测、要货计划、发货协同、采购跟踪、发票结算 | 已覆盖首轮结构，后续重点校正字段和状态。 |
| PS | 首页、基础数据、维护订单、执行生产排程、排程结果对比、查询排程结果、生产计划查询 | 已覆盖首轮结构，后续重点校正算法输入输出和版本规则。 |
| API | API 参考 | 已补接口取证模板和第三方挂接占位。 |

## 3. 尚未完成的逐页取证任务

| 任务 | 范围 | 状态 |
| --- | --- | --- |
| 字段真实性校正 | SCP/PS/API 全部页面 | 待从 DDL、DO、DTO、VO、前端配置和接口 DTO 取证。 |
| 状态动作矩阵 | 采购订单、发货协同、结算、排程执行、结果发布 | 待逐业务核验。 |
| 外部接口挂接 | ERP/SAP/SCP/WMS/MES/报表/排程引擎 | 待建立接口矩阵。 |
| 权限与数据范围 | 内部用户、供应商账号、接口账号、计划员 | 待结合 System/RBAC 取证。 |
| 图示与示例 | 供应链协同、排程输入输出、接口请求响应 | 已有占位，待生成或人工补充。 |

## 4. 本批完成判断

| 条件 | 当前判断 |
| --- | --- |
| BATCH-04 主要页面均有结构占位 | 已满足首轮占位要求。 |
| 无法确认内容保留待核验标记 | 已满足，具体实现仍待逐项取证。 |
| 接口挂接相关差距已记录 | 已记录 GAP-017。 |
| 本轮构建通过 | 已通过 `mkdocs build` 校验；仍存在既有跨模块链接警告，未作为本轮阻塞。 |

若本轮构建通过，大范围首轮占位四个批次均已完成；后续应转入“逐页取证与样板回填”阶段，而不是继续扩大占位范围。
