# NodeRed

> 适用基线：测试环境目标 / `dev` 分支 / 2026-07-15。
> **资料状态：薄弱。** 本组只写清菜单意图、与 MES 的残留线索及缺口；不能当作已覆盖的 Node-RED 运维手册。

## 业务目的与适用范围

菜单里能看到 Node-RED，不代表本仓库已有可培训的管理台与日志页。本页说明产品意图、盘点结论与边界，帮助测试/实施/运维决定「要不要讲、从哪核验」，避免把空白菜单当成已交付能力。

读完本页，应能：判断当前环境菜单是否可达；知道报工/前序校验仍以 MES 为准；在取证完成前不编造字段表。

## 如何使用本组文档

| 你的目的 | 建议怎么做 |
| --- | --- |
| 判断培训要不要讲 Node-RED | 本页「当前可确认状态」+ 下方验证点 |
| 菜单打不开 / 404 | 本页关键判断；先核前端组件是否部署 |
| 配采集点 / 查采集日志 | 去 [采集点](01-采集点/index.md)、[设备管理](02-设备管理/index.md) |
| 理解边缘与业务分层 | [数采与边缘接入模型](04-数采与边缘接入模型.md) |
| 前序校验类报错 | 查 MES 错误码/i18n，勿在本页编造报文 |

## 业务目的（产品意图）

在数采根菜单下提供 Node-RED 相关入口，用于边缘流程编排、与产线校验/报工类动作协同（历史设计方向）。菜单上可见：

| 菜单 | path / 组件线索 | 盘点结论 |
| --- | --- | --- |
| NodeRed管理 | 父菜单 `nodered`（无独立 component） | 仅目录级入口 |
| NodeRed日志管理 | `mes/noderedlog/index`，路由名 `noderedlog` | **本仓库前端无** `views/mes/noderedlog` 目录 |

## 当前可确认状态

| 检查项 | 结论 |
| --- | --- |
| `reference/menu.csv` | 有上述两项菜单 |
| `sourcecode/frontend/ui/src/views` | **无** Node-RED 管理/日志页面实现 |
| MES 表 `mes_node_red_log` | 出现在遗留表下线脚本/审计清单中；现行 Java **无**稳定 CRUD 命中 |
| MES 错误码 / i18n | 仍保留 Node-RED 前序校验类文案（产线码空、请求体空、自动线料口等） |
| `NodeRedResultEnum` | 枚举仍在 MES，不等同于管理台页面存在 |

**因此本轮不做**字段级维护与查询参考，避免编造不存在的表单字段。

!!! example "读完自检（环境核验）"
    **给定：** 测试环境已登录，侧栏可见 NodeRed 相关菜单。
    **期望先核验：** 点击后是空白/404，还是外部 Node-RED 实例；现场是否仍部署 Node-RED。若无实例、无页面，培训只保留本页缺口说明，不要按「完整运维台」讲字段。

## 与数采主链、MES 的边界

| 协同方 | 本页可写 | 不要写成事实 |
| --- | --- | --- |
| 数采采集点/日志 | Node-RED 可能作为边缘编排消费采集或触发业务 | 已证实的固定流程拓扑 |
| MES | 历史/残留「前序校验」错误码说明曾有 Node-RED 入参校验 | 现行 Controller 与日志表完整可用 |
| 终端报工 | 线边报工以 MES 终端为准 | Node-RED 日志页可查全部报工 |

## 关键判断

| 判断点 | 应先确认什么 |
| --- | --- |
| 菜单点开 404 / 空白 | 前端组件是否未部署；是否仅有父菜单 |
| 培训要不要讲 Node-RED | 先看现场是否仍部署 Node-RED 实例；无实例则只保留缺口说明 |
| 前序校验报错 | 查 MES i18n 对应错误含义与产线/料口入参，勿在本页编造接口报文 |

### 建议验证点

- 测试环境点击「NodeRed管理 / 日志」：记录可达性（空白、404、外链或第三方控制台）。
- 确认现场是否部署 Node-RED 实例；无实例则文档与培训只保留缺口口径。
- 若出现前序校验类报错：在 MES 错误文案侧定位，不在本页臆造接口路径。

## 限制与待确认（缺口）

详见内部清单：内部资料（含 Node-RED 子项，不参与站点发布）。

关联总账：`GAP-072`。

取证完成后的回填顺序建议：

1. 测试环境证实菜单是否启用、组件是否可达。
2. 确认 Node-RED 是否仍部署；日志落在网关、MES 还是外部。
3. 再补维护参考与证据页；**禁止**在取证前恢复假字段表。
