# WMS 基础数据

> 第一阶段状态：本分组已建立学习导航和页面大纲；具体字段、状态、权限和测试截图按各叶页的后续任务补充。

## 这一组业务解决什么问题

汇总 WMS 业务使用的基础资料、标签相关入口及成本/科目相关入口，并说明它们与 DBC 主数据、平台能力之间的使用边界。

## 建议学习与操作顺序

| 顺序 | 建议先看什么 | 为什么 |
| --- | --- | --- |
| 1 | 本页的对象关系和边界 | 确认基础资料从哪里维护、在 WMS 中怎样被使用。 |
| 2 | 本分组的主业务页或查询页 | 理解一笔典型业务如何开始、执行和结束。 |
| 3 | 对应的库存管理和终端操作页面 | 追溯库存结果，并理解 Web 与现场执行差异。 |

## 关键业务对象与关系

【图示占位：基础数据入口 → WMS 业务页面 → 任务/记录/库存结果。需以业务对象表达，不使用表名、字段或服务调用；状态和分支待测试验证后补充。】

## 页面清单与写作状态

| 页面 | 文档形态 | 已说明内容 | 后续需补 |
| --- | --- | --- | --- |
| 本分组业务说明 | 一页完成或主文档 | 已建立业务定位、阅读路径和大纲。 | 按业务补典型流程、状态、异常、查询和截图。 |
| 详细参考（如适用） | 仅复杂高频页面拆分 | 采购收货已采用主文档 + 详细参考样板。 | 其它页面按复杂度决定是否拆分，不机械新增。 |
| 终端与库存追溯 | 关联说明 | 仅说明与本分组相关的执行入口和结果对象。 | 补扫码规则、查询过滤、状态验证和示例。 |

## 常见问题与相关分组

【待补充：说明本分组中最常见的“找不到任务、数量/地点不一致、完成后查不到结果、需要转后续处理”等问题，以及应转到哪个分组继续处理。】

## 图示、截图与示例任务

【图示占位：补充本分组从来源、任务、记录到库存或后续处理的业务流程/关系图。】

【截图占位：补充业务列表、详情、现场执行或异常提示页面；使用脱敏测试数据。】

【示例数据占位：补充一笔正常业务和一笔常见异常的脱敏样例，串联来源、执行、结果和查询。】
## BATCH-01 标准占位

> 状态：首轮占位，待基于菜单、前端页面、后端服务、DDL 和测试环境继续核验。当前页为 WMS 基础数据分组入口，不直接作为单一业务实体页。

本页用于汇总 WMS 内与仓储业务相关的基础数据入口。若具体数据对象实际归属于 DBC、System 或 Infra，应在本页保留入口说明，并在对应模块页面维护字段、导入、权限和业务规则。

| 主题 | 当前占位 | 后续取证 |
| --- | --- | --- |
| 页面范围 | 待核验 WMS 基础数据菜单下实际包含哪些页面。 | `reference/menu.csv`、前端路由、WMS/DBC 后端模块。 |
| 归属边界 | 待区分 WMS 自有基础数据、DBC 主数据、平台公共能力。 | 菜单、源码、业务确认。 |
| 字段真实性 | 不在分组页写字段事实；字段在具体实体页取证。 | DDL、DO、DTO、前端配置。 |
| 导入导出 | 待确认各基础数据是否支持导入、导出和异步日志。 | 前端按钮、Excel VO、导入导出服务。 |
| 列表与详情 | 待为每个具体页面补列表字段、详情分组和快速跳转。 | 前端列表配置、详情组件。 |
| 权限与日志 | 按 RBAC 与动作权限取证模板 逐页补。 | 菜单权限、按钮权限、接口权限、数据范围。 |

## 当前菜单与归属事实（BATCH-01 首轮取证）

> 取证依据：`reference/menu.csv`（`dev` 基线）和 Web 源码目录 `ui/src/views/wms/basicDataManage`。本节说明当前可见入口和归属边界，不替代每个实体页的字段、导入与权限规则。

### 当前 WMS 基础数据菜单

| 当前菜单分组 | 已确认页面 | 当前文档处理原则 |
| --- | --- | --- |
| 客户管理 | 销售价格单、客户发货预测。 | 属于 WMS 使用入口；客户、物料等被引用主数据仍应在 DBC 页面说明维护规则。 |
| 系统设置 | 系统日历、账期日历。 | 作为 WMS 运行配置保留在 [系统设置](../02-系统设置/index.md)；需进一步核验其对业务日期、关账和事务的实际影响。 |
| 单据设置管理 | 事务类型、计划设置。 | 是申请、任务、记录与库存事务之间的重要配置入口；字段、状态和引用关系后续按实体单独取证。 |
| 标签管理 | 标签规则设置、标签信息、采购件标签、制造件标签、器具标签、库位标签、叫料标签、标签模板。 | 当前菜单位于 WMS，但长期目标归属为 Infra 平台能力；本页只说明 WMS 使用入口，平台能力的通用规则应在基础设施文档维护。 |
| 科目账户管理 | 产品类、科目账户配置、ERP 成本中心、ERP 项目信息。 | 当前为 WMS 菜单入口；是否由财务/ERP 主数据主导、以及实际接口方向，待后续取证。 |

### 与 DBC / Infra 的边界

- 菜单数据同时显示：DBC 下有“业务类型”“单据设置管理”“系统全局规则”等配置入口；WMS 下也存在自己的单据设置、标签及日历入口。当前不能仅按菜单名称推定唯一维护责任。
- 标签、条码、模板和打印按已确认的产品方向应逐步收敛为 Infra 平台能力；在完成迁移前，WMS 页面仍是实际业务使用入口。后续文档必须标明“当前入口”与“目标归属”，避免把目标结构误写为现状。
- 采购、上架、发料、收货、盘点等业务页引用本分组配置时，应链接到具体配置实体页；本分组入口不重复维护字段字典。

### 后续实体取证顺序

1. 事务类型：入/出库存动作、业务类型、库存事务和余额更新的挂接。
2. 计划设置与日历：申请/任务生成、业务日期、账期控制和关账影响。
3. 标签与条码：模板、规则、打印任务、业务单据与终端扫描的关联；同步补 Infra 平台归属说明。
4. 科目与 ERP 映射：当前保存字段、导入规则、第三方接口与失败追溯。

## 待补充内容

| 内容 | 说明 |
| --- | --- |
| 菜单清单 | 从 `reference/menu.csv` 提取 WMS 基础数据分组下的实际页面。 |
| 页面归属 | 判断是否需要跳转到 DBC、System、Infra 的对应主数据或公共能力页。 |
| 引用关系 | 说明这些基础数据被采购、上架、发料、盘点、库存查询等 WMS 业务如何引用。 |
