# 组织架构

> 状态：占位页已建立。组织关系的参考设计来自旧版规划书；当前实体、编码及隔离机制待核验。

## 页面定位

组织架构定义业务责任与数据边界所依赖的组织对象，不以菜单层级替代组织模型。

## 参考设计

旧版规划书提出企业/组织、工厂（站点）、部门三级关系：工厂作为业务与基础数据隔离的重要维度，部门以树形结构承接人员责任与岗位归属。当前系统是否采用相同层级，必须以 `dev` 基线确认。

## 待核验内容

1. 组织、工厂、仓库、生产线、部门、岗位之间的真实关系。
2. 每一对象的维护权限、启停用和历史数据保留规则。
3. 数据权限与任务分配如何引用组织对象。
