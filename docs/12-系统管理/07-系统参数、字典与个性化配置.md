# 系统参数、字典与个性化配置

> 状态：占位页已建立。当前配置归属与生效机制待核验。

## 页面定位

本页说明跨业务模块复用的系统参数、字典和用户个性化设置；业务含义强的策略和业务类型配置仍归入 DBC/业务模型。

## 参考设计线索

旧版规划书提出用户可配置字段显示权限、保存自定义视图并提供默认视图。该能力是否已实现、由哪个模块承载，待前后端源码核验。

## 待核验内容

| 类别 | 需要确认的内容 |
| --- | --- |
| 系统参数 | 层级、默认值、生效范围、变更权限与审计。 |
| 字典 | 主责模块、编码唯一性、引用检查、启停用和导入规则。 |
| 个性化 | 列表视图、字段显示、排序、筛选保存及角色共享规则。 |
